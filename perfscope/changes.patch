diff --git a/.gitignore b/.gitignore
index 5f04104..2f5fde2 100644
--- a/.gitignore
+++ b/.gitignore
@@ -6,3 +6,10 @@ config.status
 Makefile.common
 *.swp
 .hg
+llvm-9.0.1.src
+mysql-server
+clang
+cmake
+*.o
+*.S
+*.a
\ No newline at end of file
diff --git a/configure b/configure
index 068949d..29fc4d7 100755
--- a/configure
+++ b/configure
@@ -1651,7 +1651,7 @@ ac_compiler_gnu=$ac_cv_c_compiler_gnu
 
 # Check whether --with-llvmsrc was given.
 if test "${with_llvmsrc+set}" = set; then :
-  if ! test -d "$with_llvmsrc/autoconf/m4" ; then
+  if false ; then
     echo "\`$with_llvmsrc' is not a valid LLVM source tree." >&2
     exit 1
   fi
@@ -1703,7 +1703,7 @@ for ac_dir in $LLVM_SRC/autoconf "$srcdir"/$LLVM_SRC/autoconf; do
     break
   fi
 done
-if test -z "$ac_aux_dir"; then
+ac_aux_dir=.; ac_install_sh="./install-sh -c"; if false; then
   as_fn_error $? "cannot find install-sh, install.sh, or shtool in $LLVM_SRC/autoconf \"$srcdir\"/$LLVM_SRC/autoconf" "$LINENO" 5
 fi
 
@@ -2880,14 +2880,14 @@ $as_echo "$as_me: executing $ac_file commands" >&6;}
 
 
   case $ac_file$ac_mode in
-    "lib/Makefile":C) ${llvm_src}/autoconf/mkinstalldirs `dirname lib/Makefile`
-   ${SHELL} ${llvm_src}/autoconf/install-sh -m 0644 -c ${srcdir}/lib/Makefile lib/Makefile ;;
-    "tools/Makefile":C) ${llvm_src}/autoconf/mkinstalldirs `dirname tools/Makefile`
-   ${SHELL} ${llvm_src}/autoconf/install-sh -m 0644 -c ${srcdir}/tools/Makefile tools/Makefile ;;
-    "test/Makefile":C) ${llvm_src}/autoconf/mkinstalldirs `dirname test/Makefile`
-   ${SHELL} ${llvm_src}/autoconf/install-sh -m 0644 -c ${srcdir}/test/Makefile test/Makefile ;;
-    "third-party/Makefile":C) ${llvm_src}/autoconf/mkinstalldirs `dirname third-party/Makefile`
-   ${SHELL} ${llvm_src}/autoconf/install-sh -m 0644 -c ${srcdir}/third-party/Makefile third-party/Makefile ;;
+    "lib/Makefile":C) mkdir -p `dirname lib/Makefile`
+   ${SHELL} ./install-sh -m 0644 -c ${srcdir}/lib/Makefile lib/Makefile ;;
+    "tools/Makefile":C) mkdir -p `dirname tools/Makefile`
+   ${SHELL} ./install-sh -m 0644 -c ${srcdir}/tools/Makefile tools/Makefile ;;
+    "test/Makefile":C) mkdir -p `dirname test/Makefile`
+   ${SHELL} ./install-sh -m 0644 -c ${srcdir}/test/Makefile test/Makefile ;;
+    "third-party/Makefile":C) mkdir -p `dirname third-party/Makefile`
+   ${SHELL} ./install-sh -m 0644 -c ${srcdir}/third-party/Makefile third-party/Makefile ;;
 
   esac
 done # for ac_tag
diff --git a/include/analyzer/CFGDAG.h b/include/analyzer/CFGDAG.h
index 67becca..2d25e05 100644
--- a/include/analyzer/CFGDAG.h
+++ b/include/analyzer/CFGDAG.h
@@ -33,10 +33,10 @@
 #include <map>
 #include <stack>
 
-#include "llvm/Instructions.h"
-#include "llvm/Function.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Function.h"
 
-#include "llvm/Support/CFG.h"
+#include "llvm/IR/CFG.h"
 
 namespace llvm {
 
diff --git a/include/analyzer/CostModel.h b/include/analyzer/CostModel.h
index c43c2a2..80fbc55 100644
--- a/include/analyzer/CostModel.h
+++ b/include/analyzer/CostModel.h
@@ -35,14 +35,14 @@
 #ifndef __COSTMODEL_H_
 #define __COSTMODEL_H_
 
-#include "llvm/Function.h"
-#include "llvm/Instructions.h"
-#include "llvm/Intrinsics.h"
-#include "llvm/Operator.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Intrinsics.h"
+#include "llvm/IR/Operator.h"
 
 #include "llvm/Analysis/LoopInfo.h"
 #include "llvm/CodeGen/ValueTypes.h"
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/CallSite.h"
 
 
 namespace llvm {
@@ -197,7 +197,7 @@ class CostModel {
   protected:
     inline bool terminatingBlock(const BasicBlock *BB) const
     {
-      const TerminatorInst * terminator  = BB->getTerminator();
+      const Instruction * terminator  = BB->getTerminator();
       return (isa<ReturnInst>(terminator) || isa<UnreachableInst>(terminator) ||
         isa<ResumeInst>(terminator)); 
     }
diff --git a/include/analyzer/Evaluator.h b/include/analyzer/Evaluator.h
index 17ed810..f6a8f3d 100644
--- a/include/analyzer/Evaluator.h
+++ b/include/analyzer/Evaluator.h
@@ -31,12 +31,15 @@
 #define __EVALUATOR_H_
 
 #include "llvm/Pass.h"
-#include "llvm/Function.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Transforms/Scalar.h"
-#include "llvm/Instruction.h"
-#include "llvm/Instructions.h"
-#include "llvm/PassManager.h"
+#include "llvm/IR/Instruction.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/PassManager.h"
+
+#include "llvm/IR/LegacyPassManager.h"
+#include "llvm/Analysis/ScalarEvolution.h"
 
 #include "llvm/Analysis/AliasAnalysis.h"
 #include "llvm/Analysis/DominanceFrontier.h"
@@ -45,7 +48,7 @@
 #include "llvm/Analysis/ScalarEvolution.h"
 
 #include "llvm/Support/raw_ostream.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/InstIterator.h"
 
 #include "llvm/ADT/SmallPtrSet.h"
 #include "llvm/ADT/SmallVector.h"
@@ -125,10 +128,10 @@ struct DummyLoopInfo : public FunctionPass {
   }
 
   virtual bool runOnFunction(Function &F);
-  virtual const char * getPassName() const { return PassName; }
+  virtual StringRef getPassName() const { return PassName; }
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.setPreservesAll();
-    AU.addRequired<LoopInfo>();
+    AU.addRequired<LoopInfoWrapperPass>();
   }
 };
 
@@ -143,7 +146,7 @@ class RiskEvaluator: public FunctionPass {
     slicing::StaticSlicer *slicer;
     CostModel * cost_model;
     Profile * profile;
-    FunctionPassManager * func_manager;
+    llvm::legacy::FunctionPassManager * func_manager;
     Module * module;
     SmallPtrSet<const Function *, 4> loop_analyzed;
     LoopInfo * LocalLI;
@@ -168,7 +171,7 @@ class RiskEvaluator: public FunctionPass {
       memset(AllRiskStat, 0, sizeof(AllRiskStat));
       memset(FuncRiskStat, 0, sizeof(FuncRiskStat));
       if (module) {
-        func_manager = new FunctionPassManager(module);
+        func_manager = new llvm::legacy::FunctionPassManager(module);
         GlobalLI = new DummyLoopInfo();
         func_manager->add(GlobalLI);
         func_manager->doInitialization();
@@ -185,7 +188,7 @@ class RiskEvaluator: public FunctionPass {
       }
     }
 
-    virtual const char *getPassName() const { return PassName;}
+    virtual StringRef getPassName() const { return PassName;}
 
     virtual bool runOnFunction(Function &F); 
 
@@ -210,8 +213,8 @@ class RiskEvaluator: public FunctionPass {
 
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
       AU.setPreservesAll();
-      AU.addRequired<LoopInfo>();
-      AU.addRequired<ScalarEvolution>(); 
+      AU.addRequired<LoopInfoWrapperPass>();
+      AU.addRequired<ScalarEvolutionWrapperPass>(); 
       //AU.addRequired<DepGraphBuilder>();
     }
   
diff --git a/include/analyzer/TargetTransformStub.h b/include/analyzer/TargetTransformStub.h
index c64d115..7946ed5 100644
--- a/include/analyzer/TargetTransformStub.h
+++ b/include/analyzer/TargetTransformStub.h
@@ -33,7 +33,7 @@
 #include <utility>
 
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetLowering.h"
+#include "llvm/CodeGen/TargetLowering.h"
 
 #include "llvm/Support/Casting.h"
 
diff --git a/include/analyzer/X86CostModel.h b/include/analyzer/X86CostModel.h
index e111241..dc32086 100644
--- a/include/analyzer/X86CostModel.h
+++ b/include/analyzer/X86CostModel.h
@@ -34,7 +34,7 @@
 #include <utility>
 
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetLowering.h"
+#include "llvm/CodeGen/TargetLowering.h"
 
 #include "analyzer/TargetTransformStub.h"
 #include "analyzer/X86SubtargetStub.h"
diff --git a/include/analyzer/X86SubtargetStub.h b/include/analyzer/X86SubtargetStub.h
index 8480784..227b4d1 100644
--- a/include/analyzer/X86SubtargetStub.h
+++ b/include/analyzer/X86SubtargetStub.h
@@ -32,6 +32,7 @@
 
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/raw_ostream.h"
+#include "llvm/MC/SubtargetFeature.h"
 
 namespace llvm {
 
@@ -39,35 +40,35 @@ class X86SubtargetStub {
 protected:
 
   enum FeatureEncoding {
-    Feature3DNow =  1ULL << 0,
-    Feature3DNowA =  1ULL << 1,
-    Feature64Bit =  1ULL << 2,
-    FeatureAES =  1ULL << 3,
-    FeatureAVX =  1ULL << 4,
-    FeatureBMI =  1ULL << 5,
-    FeatureCLMUL =  1ULL << 6,
-    FeatureCMOV =  1ULL << 7,
-    FeatureCMPXCHG16B =  1ULL << 8,
-    FeatureF16C =  1ULL << 9,
-    FeatureFMA3 =  1ULL << 10,
-    FeatureFMA4 =  1ULL << 11,
-    FeatureFastUAMem =  1ULL << 12,
-    FeatureLZCNT =  1ULL << 13,
-    FeatureMMX =  1ULL << 14,
-    FeatureMOVBE =  1ULL << 15,
-    FeaturePOPCNT =  1ULL << 16,
-    FeatureRDRAND =  1ULL << 17,
-    FeatureSSE1 =  1ULL << 18,
-    FeatureSSE2 =  1ULL << 19,
-    FeatureSSE3 =  1ULL << 20,
-    FeatureSSE4A =  1ULL << 21,
-    FeatureSSE41 =  1ULL << 22,
-    FeatureSSE42 =  1ULL << 23,
-    FeatureSSSE3 =  1ULL << 24,
-    FeatureSlowBTMem =  1ULL << 25,
-    FeatureVectorUAMem =  1ULL << 26,
-    Mode64Bit =  1ULL << 27,
-    ModeNaCl =  1ULL << 28
+    Feature3DNow =  0,
+    Feature3DNowA =  1,
+    Feature64Bit =  2,
+    FeatureAES =  3,
+    FeatureAVX =  4,
+    FeatureBMI =  5,
+    FeatureCLMUL =  6,
+    FeatureCMOV =  7,
+    FeatureCMPXCHG16B =  8,
+    FeatureF16C =  9,
+    FeatureFMA3 =  10,
+    FeatureFMA4 =  11,
+    FeatureFastUAMem =  12,
+    FeatureLZCNT =  13,
+    FeatureMMX =  14,
+    FeatureMOVBE =  15,
+    FeaturePOPCNT =  16,
+    FeatureRDRAND =  17,
+    FeatureSSE1 =  18,
+    FeatureSSE2 =  19,
+    FeatureSSE3 =  20,
+    FeatureSSE4A =  21,
+    FeatureSSE41 =  22,
+    FeatureSSE42 =  23,
+    FeatureSSSE3 =  24,
+    FeatureSlowBTMem =  25,
+    FeatureVectorUAMem =  26,
+    Mode64Bit =  27,
+    ModeNaCl =  28
   };
 
   enum X86SSEEnum {
@@ -153,7 +154,7 @@ private:
   bool InNaClMode;
 
 public:
-  X86SubtargetStub(uint64_t bits) 
+  X86SubtargetStub(FeatureBitset bits) 
   : X86SSELevel(NoMMXSSE)
   , X863DNowLevel(NoThreeDNow)
   , HasCMov(false)
@@ -214,36 +215,36 @@ public:
   bool hasVectorUAMem() const { return HasVectorUAMem; }
   bool hasCmpxchg16b() const { return HasCmpxchg16b; }
 
-  void ParseSubtargetFeatures(uint64_t Bits) {
-    if ((Bits & Feature3DNow) != 0 && X863DNowLevel < ThreeDNow) X863DNowLevel = ThreeDNow;
-    if ((Bits & Feature3DNowA) != 0 && X863DNowLevel < ThreeDNowA) X863DNowLevel = ThreeDNowA;
-    if ((Bits & Feature64Bit) != 0) HasX86_64 = true;
-    if ((Bits & FeatureAES) != 0) HasAES = true;
-    if ((Bits & FeatureAVX) != 0) HasAVX = true;
-    if ((Bits & FeatureBMI) != 0) HasBMI = true;
-    if ((Bits & FeatureCLMUL) != 0) HasCLMUL = true;
-    if ((Bits & FeatureCMOV) != 0) HasCMov = true;
-    if ((Bits & FeatureCMPXCHG16B) != 0) HasCmpxchg16b = true;
-    if ((Bits & FeatureF16C) != 0) HasF16C = true;
-    if ((Bits & FeatureFMA3) != 0) HasFMA3 = true;
-    if ((Bits & FeatureFMA4) != 0) HasFMA4 = true;
-    if ((Bits & FeatureFastUAMem) != 0) IsUAMemFast = true;
-    if ((Bits & FeatureLZCNT) != 0) HasLZCNT = true;
-    if ((Bits & FeatureMMX) != 0 && X86SSELevel < MMX) X86SSELevel = MMX;
-    if ((Bits & FeatureMOVBE) != 0) HasMOVBE = true;
-    if ((Bits & FeaturePOPCNT) != 0) HasPOPCNT = true;
-    if ((Bits & FeatureRDRAND) != 0) HasRDRAND = true;
-    if ((Bits & FeatureSSE1) != 0 && X86SSELevel < SSE1) X86SSELevel = SSE1;
-    if ((Bits & FeatureSSE2) != 0 && X86SSELevel < SSE2) X86SSELevel = SSE2;
-    if ((Bits & FeatureSSE3) != 0 && X86SSELevel < SSE3) X86SSELevel = SSE3;
-    if ((Bits & FeatureSSE4A) != 0) HasSSE4A = true;
-    if ((Bits & FeatureSSE41) != 0 && X86SSELevel < SSE41) X86SSELevel = SSE41;
-    if ((Bits & FeatureSSE42) != 0 && X86SSELevel < SSE42) X86SSELevel = SSE42;
-    if ((Bits & FeatureSSSE3) != 0 && X86SSELevel < SSSE3) X86SSELevel = SSSE3;
-    if ((Bits & FeatureSlowBTMem) != 0) IsBTMemSlow = true;
-    if ((Bits & FeatureVectorUAMem) != 0) HasVectorUAMem = true;
-    if ((Bits & Mode64Bit) != 0) In64BitMode = true;
-    if ((Bits & ModeNaCl) != 0) InNaClMode = true;
+  void ParseSubtargetFeatures(FeatureBitset Bits) {
+    if ((Bits & FeatureBitset{Feature3DNow}) != 0 && X863DNowLevel < ThreeDNow) X863DNowLevel = ThreeDNow;
+    if ((Bits & FeatureBitset{Feature3DNowA}) != 0 && X863DNowLevel < ThreeDNowA) X863DNowLevel = ThreeDNowA;
+    if ((Bits & FeatureBitset{Feature64Bit}) != 0) HasX86_64 = true;
+    if ((Bits & FeatureBitset{FeatureAES}) != 0) HasAES = true;
+    if ((Bits & FeatureBitset{FeatureAVX}) != 0) HasAVX = true;
+    if ((Bits & FeatureBitset{FeatureBMI}) != 0) HasBMI = true;
+    if ((Bits & FeatureBitset{FeatureCLMUL}) != 0) HasCLMUL = true;
+    if ((Bits & FeatureBitset{FeatureCMOV}) != 0) HasCMov = true;
+    if ((Bits & FeatureBitset{FeatureCMPXCHG16B}) != 0) HasCmpxchg16b = true;
+    if ((Bits & FeatureBitset{FeatureF16C}) != 0) HasF16C = true;
+    if ((Bits & FeatureBitset{FeatureFMA3}) != 0) HasFMA3 = true;
+    if ((Bits & FeatureBitset{FeatureFMA4}) != 0) HasFMA4 = true;
+    if ((Bits & FeatureBitset{FeatureFastUAMem}) != 0) IsUAMemFast = true;
+    if ((Bits & FeatureBitset{FeatureLZCNT}) != 0) HasLZCNT = true;
+    if ((Bits & FeatureBitset{FeatureMMX}) != 0 && X86SSELevel < MMX) X86SSELevel = MMX;
+    if ((Bits & FeatureBitset{FeatureMOVBE}) != 0) HasMOVBE = true;
+    if ((Bits & FeatureBitset{FeaturePOPCNT}) != 0) HasPOPCNT = true;
+    if ((Bits & FeatureBitset{FeatureRDRAND}) != 0) HasRDRAND = true;
+    if ((Bits & FeatureBitset{FeatureSSE1}) != 0 && X86SSELevel < SSE1) X86SSELevel = SSE1;
+    if ((Bits & FeatureBitset{FeatureSSE2}) != 0 && X86SSELevel < SSE2) X86SSELevel = SSE2;
+    if ((Bits & FeatureBitset{FeatureSSE3}) != 0 && X86SSELevel < SSE3) X86SSELevel = SSE3;
+    if ((Bits & FeatureBitset{FeatureSSE4A}) != 0) HasSSE4A = true;
+    if ((Bits & FeatureBitset{FeatureSSE41}) != 0 && X86SSELevel < SSE41) X86SSELevel = SSE41;
+    if ((Bits & FeatureBitset{FeatureSSE42}) != 0 && X86SSELevel < SSE42) X86SSELevel = SSE42;
+    if ((Bits & FeatureBitset{FeatureSSSE3}) != 0 && X86SSELevel < SSSE3) X86SSELevel = SSSE3;
+    if ((Bits & FeatureBitset{FeatureSlowBTMem}) != 0) IsBTMemSlow = true;
+    if ((Bits & FeatureBitset{FeatureVectorUAMem}) != 0) HasVectorUAMem = true;
+    if ((Bits & FeatureBitset{Mode64Bit}) != 0) In64BitMode = true;
+    if ((Bits & FeatureBitset{ModeNaCl}) != 0) InNaClMode = true;
   }
 };
 
diff --git a/include/commons/CallSiteFinder.h b/include/commons/CallSiteFinder.h
index 3adeb8f..f486b5b 100644
--- a/include/commons/CallSiteFinder.h
+++ b/include/commons/CallSiteFinder.h
@@ -30,7 +30,7 @@
 #ifndef __CALL_SITE_FINDER__H_
 #define __CALL_SITE_FINDER__H_
 
-#include "llvm/Function.h"
+#include "llvm/IR/Function.h"
 #include "llvm/ADT/SmallVector.h"
 
 #include <utility>
diff --git a/include/commons/LLVMHelper.h b/include/commons/LLVMHelper.h
index 0400da4..1bebc08 100644
--- a/include/commons/LLVMHelper.h
+++ b/include/commons/LLVMHelper.h
@@ -35,7 +35,7 @@
 #include <vector>
 #include <map>
 
-#include "llvm/Module.h"
+#include "llvm/IR/Module.h"
 #include "llvm/PassRegistry.h"
 
 #include "llvm/Target/TargetMachine.h"
@@ -80,9 +80,9 @@ TargetMachine * getTargetMachine();
 /// Initialize the given registry with common passes
 void initPassRegistry(PassRegistry & Registry);
 
-/// Get the TargetData in the given Module
+/// Get the DataLayout in the given Module
 /// Return NULL if it cannot be created
-TargetData * getTargetData(Module *M);
+DataLayout * getDataLayout(Module *M);
 
 void indent(raw_ostream & OS, int space);
 
diff --git a/include/dependence/DepGraph.h b/include/dependence/DepGraph.h
index 9dce8da..12295a7 100644
--- a/include/dependence/DepGraph.h
+++ b/include/dependence/DepGraph.h
@@ -34,7 +34,7 @@
 #ifndef __DEPENDENCEGRAPH_H_
 #define __DEPENDENCEGRAPH_H_
 
-#include "llvm/Instruction.h"
+#include "llvm/IR/Instruction.h"
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/Support/Casting.h"
 
diff --git a/include/dependence/DepGraphBuilder.h b/include/dependence/DepGraphBuilder.h
index e2c41f9..7e67e53 100644
--- a/include/dependence/DepGraphBuilder.h
+++ b/include/dependence/DepGraphBuilder.h
@@ -31,15 +31,15 @@
 #define __DEPGRAPHBUILDER_H_
 
 #include "llvm/Pass.h"
-#include "llvm/Function.h"
-#include "llvm/Instruction.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Instruction.h"
 
 #include "llvm/Analysis/AliasAnalysis.h"
 #include "llvm/Analysis/MemoryDependenceAnalysis.h"
 #include "llvm/ADT/SmallVector.h"
 #include "llvm/ADT/Statistic.h"
 #include "llvm/Support/raw_ostream.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/InstIterator.h"
 
 #include "dependence/Dependence.h"
 #include "dependence/DepGraph.h"
@@ -79,8 +79,8 @@ class DepGraphBuilder : public FunctionPass {
 
     // We don't modify the program, so we preserve all analyses
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
-      AU.addRequiredTransitive<AliasAnalysis>();
-      AU.addRequiredTransitive<MemoryDependenceAnalysis>();
+      AU.addRequiredTransitive<AAResultsWrapperPass>();
+      AU.addRequiredTransitive<MemoryDependenceWrapperPass>();
       AU.setPreservesAll();
     }
 
diff --git a/include/dependence/DependenceGraph.h b/include/dependence/DependenceGraph.h
index 9da4e7d..1fae563 100644
--- a/include/dependence/DependenceGraph.h
+++ b/include/dependence/DependenceGraph.h
@@ -32,7 +32,7 @@
 #include <set>
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/ADT/SmallSet.h"
-#include "llvm/Instruction.h"
+#include "llvm/IR/Instruction.h"
 
 
 namespace llvm {
diff --git a/include/dependence/PgmDependenceGraph.h b/include/dependence/PgmDependenceGraph.h
index daf59b3..a5d7f9a 100644
--- a/include/dependence/PgmDependenceGraph.h
+++ b/include/dependence/PgmDependenceGraph.h
@@ -49,7 +49,7 @@
 #include "llvm/ADT/SmallPtrSet.h"
 #include "llvm/ADT/SmallSet.h"
 #include "llvm/ADT/SmallVector.h"
-#include "llvm/Instruction.h"
+#include "llvm/IR/Instruction.h"
 #include "llvm/Pass.h"
 
 #include "dsa/DataStructure.h"
@@ -278,7 +278,7 @@ public:
     AU.setPreservesAll();
     //AU.addRequired<PostDominatorTree>();
     //AU.addRequired<AliasAnalysis>();
-    AU.addRequired<MemoryDependenceAnalysis>();
+    AU.addRequired<MemoryDependenceWrapperPass>();
     //AU.addRequired<IPModRef>();
     //AU.addRequired<LocalDataStructures>();
     //AU.addRequired<MemoryDepAnalysis>();
diff --git a/include/dsa/AllocatorIdentification.h b/include/dsa/AllocatorIdentification.h
index 232eaa3..a561f51 100644
--- a/include/dsa/AllocatorIdentification.h
+++ b/include/dsa/AllocatorIdentification.h
@@ -14,7 +14,8 @@
 
 #include <string>
 #include "llvm/Pass.h"
-#include "llvm/Value.h"
+#include "llvm/IR/Value.h"
+#include "llvm/ADT/StringRef.h"
 
 namespace llvm {
   class Function;
@@ -45,7 +46,7 @@ namespace llvm {
     virtual ~AllocIdentify();
     bool runOnModule(llvm::Module&);
     virtual void getAnalysisUsage(llvm::AnalysisUsage &Info) const;
-    virtual const char * getPassName() const {
+    virtual llvm::StringRef getPassName() const {
       return "Allocator Identification Analysis (find malloc/free wrappers)";
     }
   };
diff --git a/include/dsa/CallTargets.h b/include/dsa/CallTargets.h
index dfc573f..d47e329 100644
--- a/include/dsa/CallTargets.h
+++ b/include/dsa/CallTargets.h
@@ -20,7 +20,7 @@
 #include <map>
 
 #include "llvm/Pass.h"
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/CallSite.h"
 #include "dsa/DataStructure.h"
 
 using namespace llvm;
diff --git a/include/dsa/DSCallGraph.h b/include/dsa/DSCallGraph.h
index c307ab1..5b0870b 100644
--- a/include/dsa/DSCallGraph.h
+++ b/include/dsa/DSCallGraph.h
@@ -19,7 +19,7 @@
 #include <map>
 
 #include "llvm/ADT/EquivalenceClasses.h"
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/CallSite.h"
 
 #include "dsa/svset.h"
 #include "dsa/keyiterator.h"
diff --git a/include/dsa/DSGraph.h b/include/dsa/DSGraph.h
index 6e8d3d9..f9ae304 100644
--- a/include/dsa/DSGraph.h
+++ b/include/dsa/DSGraph.h
@@ -20,14 +20,14 @@
 #include <set>
 
 #include "llvm/ADT/EquivalenceClasses.h"
-#include "llvm/Function.h"
+#include "llvm/IR/Function.h"
 
 #include "dsa/DSNode.h"
 #include "dsa/DSCallGraph.h"
 
 namespace llvm {
 
-class TargetData;
+class DataLayout;
 class GlobalValue;
 
 //===----------------------------------------------------------------------===//
@@ -250,7 +250,7 @@ private:
 
   /// TD - This is the target data object for the machine this graph is
   /// constructed for.
-  const TargetData &TD;
+  const DataLayout &TD;
 
   SuperSet<Type*>& TypeSS;
 
@@ -258,7 +258,7 @@ private:
   DSGraph(const DSGraph&);         // DO NOT IMPLEMENT
 public:
   // Create a new, empty, DSGraph.
-  DSGraph(EquivalenceClasses<const GlobalValue*> &ECs, const TargetData &td,
+  DSGraph(EquivalenceClasses<const GlobalValue*> &ECs, const DataLayout &td,
           SuperSet<Type*>& tss,
           DSGraph *GG = 0) 
     :GlobalsGraph(GG), UseAuxCalls(false), 
@@ -294,9 +294,9 @@ public:
     return TypeSS;
   }
 
-  /// getTargetData - Return the TargetData object for the current target.
+  /// getDataLayout - Return the DataLayout object for the current target.
   ///
-  const TargetData &getTargetData() const { return TD; }
+  const DataLayout &getDataLayout() const { return TD; }
 
   /// setUseAuxCalls - If you call this method, the auxillary call vector will
   /// be used instead of the standard call vector to the dot file.
diff --git a/include/dsa/DSGraphTraits.h b/include/dsa/DSGraphTraits.h
index 78ea603..3216ae7 100644
--- a/include/dsa/DSGraphTraits.h
+++ b/include/dsa/DSGraphTraits.h
@@ -80,7 +80,16 @@ inline DSNode::const_iterator DSNode::end() const {
   return DSNode::const_iterator(this, false);
 }
 
+struct DSNodeAddressOf {
+  DSNode *operator()(DSNode &N) const { return &N; }
+};
+
+struct ConstDSNodeAddressOf {
+  const DSNode *operator()(const DSNode &N) const { return &N; }
+};
+
 template <> struct GraphTraits<DSNode*> {
+  using NodeRef = DSNode *;
   typedef DSNode NodeType;
   typedef DSNode::iterator ChildIteratorType;
 
@@ -90,6 +99,7 @@ template <> struct GraphTraits<DSNode*> {
 };
 
 template <> struct GraphTraits<const DSNode*> {
+  using NodeRef = const DSNode *;
   typedef const DSNode NodeType;
   typedef DSNode::const_iterator ChildIteratorType;
 
@@ -101,18 +111,17 @@ template <> struct GraphTraits<const DSNode*> {
 static       DSNode &dereference (      DSNode *N) { return *N; }
 
 template <> struct GraphTraits<DSGraph*> {
+  using NodeRef = DSNode *;
   typedef DSNode NodeType;
   typedef DSNode::iterator ChildIteratorType;
 
-  typedef std::pointer_to_unary_function<DSNode *, DSNode&> DerefFun;
-
   // nodes_iterator/begin/end - Allow iteration over all nodes in the graph
-  typedef mapped_iterator<DSGraph::node_iterator, DerefFun> nodes_iterator;
+  typedef mapped_iterator<DSGraph::node_iterator, DSNodeAddressOf> nodes_iterator;
   static nodes_iterator nodes_begin(DSGraph *G) {
-    return map_iterator(G->node_begin(), DerefFun(dereference));
+    return map_iterator(G->node_begin(), DSNodeAddressOf());
   }
   static nodes_iterator nodes_end(DSGraph *G) {
-    return map_iterator(G->node_end(), DerefFun(dereference));
+    return map_iterator(G->node_end(), DSNodeAddressOf());
   }
 
   static ChildIteratorType child_begin(NodeType *N) { return N->begin(); }
@@ -120,16 +129,18 @@ template <> struct GraphTraits<DSGraph*> {
 };
 
 template <> struct GraphTraits<const DSGraph*> {
+  using NodeRef = const DSNode *;
   typedef const DSNode NodeType;
   typedef DSNode::const_iterator ChildIteratorType;
 
   // nodes_iterator/begin/end - Allow iteration over all nodes in the graph
-  typedef DSGraph::node_const_iterator nodes_iterator;
+  typedef mapped_iterator<DSGraph::node_const_iterator, ConstDSNodeAddressOf> nodes_iterator;
+
   static nodes_iterator nodes_begin(const DSGraph *G) {
-    return G->node_begin();
+    return map_iterator(G->node_begin(), ConstDSNodeAddressOf());
   }
   static nodes_iterator nodes_end(const DSGraph *G) {
-    return G->node_end();
+    return map_iterator(G->node_end(), ConstDSNodeAddressOf());
   }
 
   static ChildIteratorType child_begin(const NodeType *N) { return N->begin(); }
diff --git a/include/dsa/DSNode.h b/include/dsa/DSNode.h
index cf569e3..45fbe7f 100644
--- a/include/dsa/DSNode.h
+++ b/include/dsa/DSNode.h
@@ -46,7 +46,7 @@ public:
   typedef std::map<unsigned, DSNodeHandle> LinkMapTy;
 
 private:
-  friend struct ilist_sentinel_traits<DSNode>;
+  //friend struct ilist_sentinel_traits<DSNode>;
   //Sentinel
   DSNode() : NumReferrers(0), Size(0), NodeType(0) {}
   
diff --git a/include/dsa/DSSupport.h b/include/dsa/DSSupport.h
index 26837e9..810e4e2 100644
--- a/include/dsa/DSSupport.h
+++ b/include/dsa/DSSupport.h
@@ -20,7 +20,7 @@
 #include <set>
 
 #include "llvm/ADT/DenseSet.h"
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/CallSite.h"
 
 namespace llvm {
 
diff --git a/include/dsa/DataStructure.h b/include/dsa/DataStructure.h
index abfe066..b78c68e 100644
--- a/include/dsa/DataStructure.h
+++ b/include/dsa/DataStructure.h
@@ -21,8 +21,8 @@
 #include "dsa/AllocatorIdentification.h"
 
 #include "llvm/Pass.h"
-#include "llvm/Target/TargetData.h"
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/DataLayout.h"
+#include "llvm/IR/CallSite.h"
 #include "llvm/ADT/EquivalenceClasses.h"
 #include "llvm/ADT/DenseSet.h"
 
@@ -44,8 +44,8 @@ FunctionPass *createDataStructureGraphCheckerPass();
 class DataStructures : public ModulePass {
   typedef std::map<const Function*, DSGraph*> DSInfoTy;
 
-  /// TargetData, comes in handy
-  TargetData* TD;
+  /// DataLayout, comes in handy
+  DataLayout* TD;
 
   /// Pass to get Graphs from
   DataStructures* GraphSource;
@@ -87,7 +87,7 @@ protected:
   std::vector<const Function*> GlobalFunctionList; 
 
   void init(DataStructures* D, bool clone, bool useAuxCalls, bool copyGlobalAuxCalls, bool resetAux);
-  void init(TargetData* T);
+  void init(DataLayout* T);
 
   void formGlobalECs();
   
@@ -139,7 +139,7 @@ public:
 
   EquivalenceClasses<const GlobalValue*> &getGlobalECs() { return GlobalECs; }
 
-  TargetData& getTargetData() const { return *TD; }
+  DataLayout& getDataLayout() const { return *TD; }
 
   const DSCallGraph& getCallGraph() const { return callgraph; }
 
@@ -165,7 +165,7 @@ public:
   /// getAnalysisUsage - This obviously provides a data structure graph.
   ///
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
-    AU.addRequired<TargetData>();
+    //AU.addRequired<DataLayout>();
     AU.setPreservesAll();
   }
 };
@@ -188,7 +188,7 @@ public:
   /// getAnalysisUsage - This obviously provides a data structure graph.
   ///
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
-    AU.addRequired<TargetData>();
+    //AU.addRequired<DataLayout>();
     AU.addRequired<AddressTakenAnalysis>();
     AU.setPreservesAll();
   }
diff --git a/include/dsa/IPModRef.h b/include/dsa/IPModRef.h
index d8e71a2..3e3c204 100644
--- a/include/dsa/IPModRef.h
+++ b/include/dsa/IPModRef.h
@@ -48,7 +48,7 @@
 
 #include <map>
 
-#include "llvm/Value.h"
+#include "llvm/IR/Value.h"
 #include "llvm/ADT/DenseMap.h"
 #include "llvm/Pass.h"
 
@@ -67,23 +67,23 @@ class InvokeInst;
 class DSNode;
 class DSGraph;
 class DSNodeHandle;
-class ModRefInfo;               // Result of IP Mod/Ref for one entity
+class DSAModRefInfo;               // Result of IP Mod/Ref for one entity
 class FunctionModRefInfo;       // ModRefInfo for a func and all calls in it
 class IPModRef;                 // Pass that computes IP Mod/Ref info
 
 //----------------------------------------------------------------------------
-/// ModRefInfo Class - Representation of Mod/Ref information for a single
+/// DSAModRefInfo Class - Representation of Mod/Ref information for a single
 /// function or callsite. This is represented as a pair of bit vectors, one each
 /// for Mod and Ref. Each bit vector is indexed by the node id of the DS graph
 /// node index.
 ///
-class ModRefInfo {
+class DSAModRefInfo {
   BitSetVector   modNodeSet;            // set of modified nodes
   BitSetVector   refNodeSet;            // set of referenced nodes
   
 public:
-  // Methods to construct ModRefInfo objects.
-  ModRefInfo(unsigned int numNodes)
+  // Methods to construct DSAModRefInfo objects.
+  DSAModRefInfo(unsigned int numNodes)
     : modNodeSet(numNodes),
       refNodeSet(numNodes) { }
 
@@ -124,8 +124,8 @@ class FunctionModRefInfo {
   const Function&       F;                  // The function
   IPModRef&             IPModRefObj;        // The IPModRef Object owning this
   DSGraph*              funcTDGraph;        // Top-down DS graph for function
-  ModRefInfo            funcModRefInfo;     // ModRefInfo for the function body
-  std::map<const Instruction*, ModRefInfo*>
+  DSAModRefInfo            funcModRefInfo;     // ModRefInfo for the function body
+  std::map<const Instruction*, DSAModRefInfo*>
                         callSiteModRefInfo; // ModRefInfo for each callsite
   std::map<const DSNode*, unsigned> NodeIds;
 
@@ -147,16 +147,16 @@ public:
 
   // Retrieve Mod/Ref results for a single call site and for the function body
   // 
-  const ModRefInfo* getModRefInfo(const Function& func) const {
+  const DSAModRefInfo* getModRefInfo(const Function& func) const {
     return &funcModRefInfo;
   }
-  const ModRefInfo* getModRefInfo(const CallInst& callInst) const {
-    std::map<const Instruction*, ModRefInfo*>::const_iterator I = 
+  const DSAModRefInfo* getModRefInfo(const CallInst& callInst) const {
+    std::map<const Instruction*, DSAModRefInfo*>::const_iterator I = 
       callSiteModRefInfo.find((Instruction*)&callInst);
     return (I == callSiteModRefInfo.end()) ? NULL : I->second;
   }
-  const ModRefInfo* getModRefInfo(const InvokeInst& II) const {
-    std::map<const Instruction*, ModRefInfo*>::const_iterator I = 
+  const DSAModRefInfo* getModRefInfo(const InvokeInst& II) const {
+    std::map<const Instruction*, DSAModRefInfo*>::const_iterator I = 
       callSiteModRefInfo.find((Instruction*)&II);
     return (I == callSiteModRefInfo.end()) ? NULL : I->second;
   }
diff --git a/include/dsa/TypeSafety.h b/include/dsa/TypeSafety.h
index 21d4c7b..a4d251d 100644
--- a/include/dsa/TypeSafety.h
+++ b/include/dsa/TypeSafety.h
@@ -49,7 +49,7 @@ struct TypeSafety : public ModulePass {
     bool typeFieldsOverlap (const DSNode * N);
 
     // Pointers to prerequisite passes
-    TargetData * TD;
+    DataLayout * TD;
     dsa * dsaPass;
 
     // Data structures
@@ -60,12 +60,12 @@ struct TypeSafety : public ModulePass {
     TypeSafety() : ModulePass(ID) {}
     virtual bool runOnModule (Module & M);
 
-    const char *getPassName() const {
+    llvm::StringRef getPassName() const {
       return "DSA Type-Safety Analysis";
     }
 
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
-      AU.addRequired<TargetData>();
+      //AU.addRequired<DataLayout>();
       AU.addRequired<dsa>();
       AU.setPreservesAll();
     }
diff --git a/include/graph/Graph.h b/include/graph/Graph.h
index 11d7792..571f6dd 100644
--- a/include/graph/Graph.h
+++ b/include/graph/Graph.h
@@ -125,8 +125,8 @@ class MapGraphBase: public GraphBase<NodeValTy>,
     }
     virtual bool addEdge(NodeValTy &fval, NodeValTy &tval)
     {
-      NodeType * from = get(fval, true);
-      NodeType * to = get(tval, true);
+      NodeType * from = this->get(fval, true);
+      NodeType * to = this->get(tval, true);
       return addEdge(from, to);
     }
     virtual bool addEdge(NodeType *from, NodeType *to)
diff --git a/include/llvmslicer/Callgraph.h b/include/llvmslicer/Callgraph.h
index 2f962f8..b98f15b 100644
--- a/include/llvmslicer/Callgraph.h
+++ b/include/llvmslicer/Callgraph.h
@@ -5,11 +5,12 @@
 #define CALLGRAPH_CALLGRAPH_H
 
 #include <map>
+#include <tuple>
 #include <algorithm>
 #include <iterator>
 #include <utility>
 
-#include "llvm/Function.h"
+#include "llvm/IR/Function.h"
 #include "llvm/ADT/STLExtras.h" /* tie */
 
 #include "llvmslicer/LLVM.h"
@@ -94,7 +95,7 @@ namespace llvm { namespace callgraph { namespace detail {
 
         for (typename Set::const_iterator it = S.begin(); it != S.end(); ++it) {
           typename Dict::const_iterator b,e;
-          llvm::tie(b,e) = D.equal_range(it->second);
+          std::tie(b,e) = D.equal_range(it->second);
           for ( ; b != e; ++b)
             S.insert(typename Set::value_type(it->first,b->second));
         }
diff --git a/include/llvmslicer/FunctionStaticSlicer.h b/include/llvmslicer/FunctionStaticSlicer.h
index b3fb216..e62f86c 100644
--- a/include/llvmslicer/FunctionStaticSlicer.h
+++ b/include/llvmslicer/FunctionStaticSlicer.h
@@ -6,10 +6,10 @@
 
 #include <map>
 
-#include "llvm/Value.h"
-#include "llvm/IntrinsicInst.h"
+#include "llvm/IR/Value.h"
+#include "llvm/IR/IntrinsicInst.h"
 #include "llvm/ADT/SetVector.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/InstIterator.h"
 
 #include "llvmslicer/PointsTo.h"
 #include "llvmslicer/Modifies.h"
@@ -18,7 +18,7 @@
 
 namespace llvm { namespace slicing {
 
-  typedef llvm::SmallSetVector<const llvm::Value *, 10> ValSet;
+  typedef llvm::SmallSetVector<const llvm::Value *, 16> ValSet;
 
   void printVal(const llvm::Value *val);
 
@@ -164,7 +164,7 @@ namespace llvm { namespace slicing {
       llvm::Function &fun;
       llvm::ModulePass *MP;
       InsInfoMap insInfoMap;
-      llvm::SmallSetVector<const llvm::CallInst *, 10> skipAssert;
+      llvm::SmallSetVector<const llvm::CallInst *, 16> skipAssert;
       bool forward;
 
       static bool sameValues(const Value *val1, const Value *val2);
diff --git a/include/llvmslicer/LLVM.h b/include/llvmslicer/LLVM.h
index 90d15e8..14d1f94 100644
--- a/include/llvmslicer/LLVM.h
+++ b/include/llvmslicer/LLVM.h
@@ -4,18 +4,18 @@
 #ifndef LANGUAGES_LLVM_H
 #define LANGUAGES_LLVM_H
 
-#include <llvm/Module.h>
-#include <llvm/Function.h>
-#include <llvm/BasicBlock.h>
-#include <llvm/Instruction.h>
-#include <llvm/Instructions.h>
-#include <llvm/Value.h>
-#include <llvm/GlobalValue.h>
-#include <llvm/Type.h>
-#include <llvm/DerivedTypes.h>
-#include <llvm/Intrinsics.h>
+#include <llvm/IR/Module.h>
+#include <llvm/IR/Function.h>
+#include <llvm/IR/BasicBlock.h>
+#include <llvm/IR/Instruction.h>
+#include <llvm/IR/Instructions.h>
+#include <llvm/IR/Value.h>
+#include <llvm/IR/GlobalValue.h>
+#include <llvm/IR/Type.h>
+#include <llvm/IR/DerivedTypes.h>
+#include <llvm/IR/Intrinsics.h>
 #include <llvm/ADT/SetVector.h>
-#include <llvm/Support/InstIterator.h>
+#include <llvm/IR/InstIterator.h>
 #include <llvm/Support/raw_os_ostream.h>
 
 namespace llvm {
diff --git a/include/llvmslicer/LLVMSupport.h b/include/llvmslicer/LLVMSupport.h
index 37c899d..2264db9 100644
--- a/include/llvmslicer/LLVMSupport.h
+++ b/include/llvmslicer/LLVMSupport.h
@@ -4,8 +4,8 @@
 #ifndef LANGUAGES_LLVMSUPPORT_H
 #define LANGUAGES_LLVMSUPPORT_H
 
-#include <llvm/Function.h>
-#include <llvm/Instructions.h>
+#include <llvm/IR/Function.h>
+#include <llvm/IR/Instructions.h>
 
 #include "llvmslicer/PointsTo.h"
 
diff --git a/include/llvmslicer/Modifies.h b/include/llvmslicer/Modifies.h
index 33f8d75..c16bec7 100644
--- a/include/llvmslicer/Modifies.h
+++ b/include/llvmslicer/Modifies.h
@@ -8,8 +8,8 @@
 #include <set>
 #include <vector>
 
-#include "llvm/Function.h"
-#include "llvm/Value.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Value.h"
 
 #include "llvmslicer/LLVM.h"
 #include "llvmslicer/Callgraph.h"
diff --git a/include/llvmslicer/PointsTo.h b/include/llvmslicer/PointsTo.h
index c4e7643..1c90351 100644
--- a/include/llvmslicer/PointsTo.h
+++ b/include/llvmslicer/PointsTo.h
@@ -8,7 +8,7 @@
 #include <set>
 #include <vector>
 
-#include "llvm/Value.h"
+#include "llvm/IR/Value.h"
 
 #include "llvmslicer/RuleExpressions.h"
 
diff --git a/include/llvmslicer/PostDominanceFrontier.h b/include/llvmslicer/PostDominanceFrontier.h
index f3d92c5..a82d0e0 100644
--- a/include/llvmslicer/PostDominanceFrontier.h
+++ b/include/llvmslicer/PostDominanceFrontier.h
@@ -10,7 +10,9 @@
 #include "llvm/Analysis/LoopInfo.h"
 #include "llvm/Analysis/DominanceFrontier.h"
 #include "llvm/Analysis/PostDominators.h"
-
+#include "llvm/Pass.h"
+#include "llvm/IR/CFG.h"
+#include "llvm/IR/Function.h"
 namespace llvm {
 
   struct CreateHammockCFG : public FunctionPass {
@@ -21,55 +23,116 @@ namespace llvm {
     virtual bool runOnFunction(Function &F);
 
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
-      AU.addRequired<LoopInfo>();
+      AU.addRequired<LoopInfoWrapperPass>();
     }
   };
 
-  /// PostDominanceFrontier Class - Concrete subclass of DominanceFrontier that is
-  /// used to compute the a post-dominance frontier.
-  ///
-  struct PostDominanceFrontier : public DominanceFrontierBase {
+  // 手动实现 PostDominanceFrontier Pass
+  class PostDominanceFrontier : public FunctionPass {
+  public:
     static char ID;
-    PostDominanceFrontier()
-      : DominanceFrontierBase(ID, true) { }
-
-    virtual bool runOnFunction(Function &F) {
-      Frontiers.clear();
-      PostDominatorTree &DT = getAnalysis<PostDominatorTree>();
-#ifdef CONTROL_DEPENDENCE_GRAPH
-      calculate(DT, F);
-#else
-      Roots = DT.getRoots();
-      if (const DomTreeNode *Root = DT.getRootNode()) {
+
+    // 使用组合模式：内部持有一个数据结构来存储结果
+    // DominanceFrontierBase<BlockT, IsPostDom> 是 LLVM 提供的基类容器
+    using BaseType = DominanceFrontierBase<BasicBlock, true>;
+    BaseType Base;
+
+    // 对外暴露的类型定义
+    using DomSetType = BaseType::DomSetType;
+    using iterator = BaseType::iterator;
+    using const_iterator = BaseType::const_iterator;
+
+    PostDominanceFrontier() : FunctionPass(ID) {}
+
+    // Pass 入口
+    bool runOnFunction(Function &F) override {
+      Base.releaseMemory();
+      // 获取 PostDominatorTree
+      PostDominatorTree &DT = getAnalysis<PostDominatorTreeWrapperPass>().getPostDomTree();
+      
+      // 手动计算 Frontier
+      if (DomTreeNode *Root = DT.getRootNode())
         calculate(DT, Root);
-#ifdef PDF_DUMP
-	errs() << "=== DUMP:\n";
-	dump();
-	errs() << "=== EOD\n";
-#endif
-      }
-#endif
+        
       return false;
     }
 
-    virtual void getAnalysisUsage(AnalysisUsage &AU) const {
+    void getAnalysisUsage(AnalysisUsage &AU) const override {
       AU.setPreservesAll();
-      AU.addRequired<PostDominatorTree>();
+      AU.addRequired<PostDominatorTreeWrapperPass>();
     }
 
-  private:
-#ifdef CONTROL_DEPENDENCE_GRAPH
-    typedef std::pair<DomTreeNode *, DomTreeNode *> Ssubtype;
-    typedef std::set<Ssubtype> Stype;
-
-    void calculate(const PostDominatorTree &DT, Function &F);
-    void constructS(const PostDominatorTree &DT, Function &F, Stype &S);
-    const DomTreeNode *findNearestCommonDominator(const PostDominatorTree &DT,
-		    DomTreeNode *A, DomTreeNode *B);
-#else
-    const DomSetType &calculate(const PostDominatorTree &DT,
-                                const DomTreeNode *Node);
-#endif
+    // 核心计算逻辑：递归计算 Post Dominance Frontier
+    void calculate(PostDominatorTree &DT, DomTreeNode *Node) {
+      BasicBlock *BB = Node->getBlock();
+      
+      // 处理虚拟根节点（Block 为空的情况），直接递归子节点
+      if (!BB) {
+        for (auto I = Node->begin(), E = Node->end(); I != E; ++I)
+          calculate(DT, *I);
+        return;
+      }
+
+      // 1. DF_local: 计算本地 Frontier
+      // PostDom 的 Frontier 是 CFG 前驱中，不被当前节点严格 PostDom 的节点
+      DomSetType Local;
+      for (auto P : predecessors(BB)) {
+        if (DT.getNode(P)->getIDom() != Node)
+          Local.insert(P);
+      }
+
+      // 将 Local 集合加入 Base，并获取引用以便后续合并
+      Base.addBasicBlock(BB, Local);
+      DomSetType &S = Base.find(BB)->second;
+
+      // 2. DF_up: 递归合并子节点的 Frontier
+      for (auto I = Node->begin(), E = Node->end(); I != E; ++I) {
+        DomTreeNode *Child = *I;
+        calculate(DT, Child);
+        
+        if (BasicBlock *ChildBB = Child->getBlock()) {
+          // 查找子节点的 Frontier
+          auto ChildIt = Base.find(ChildBB);
+          if (ChildIt != Base.end()) {
+              for (BasicBlock *PB : ChildIt->second) {
+                  // 如果当前节点 Node 不严格 PostDom PB，则 PB 属于当前节点的 Frontier
+                  if (!DT.properlyDominates(Node, DT.getNode(PB)))
+                      S.insert(PB);
+              }
+          }
+        }
+      }
+    }
+
+    // 兼容性 API：重载 [] 操作符
+    // 如果不存在，则创建一个空的并返回
+    DomSetType &operator[](BasicBlock *BB) {
+      auto It = Base.find(BB);
+      if (It == Base.end())
+        // addBasicBlock 返回的是迭代器，需要通过 ->second 获取 Set 的引用
+        return Base.addBasicBlock(BB, DomSetType())->second;
+      return It->second;
+    }
+
+    // const 版本
+    const DomSetType &operator[](BasicBlock *BB) const { 
+      auto It = Base.find(BB);
+      if (It != Base.end())
+          return It->second;
+      static DomSetType Empty;
+      return Empty;
+    }
+
+    // 迭代器转发
+    iterator begin() { return Base.begin(); }
+    iterator end() { return Base.end(); }
+    const_iterator begin() const { return Base.begin(); }
+    const_iterator end() const { return Base.end(); }
+
+    iterator find(BasicBlock *BB) { return Base.find(BB); }
+    const_iterator find(BasicBlock *BB) const { return Base.find(BB); }
+    
+    bool empty() const { return Base.begin() == Base.end(); }
   };
 }
 
diff --git a/include/llvmslicer/Prepare.h b/include/llvmslicer/Prepare.h
index ffa96c3..01c644d 100644
--- a/include/llvmslicer/Prepare.h
+++ b/include/llvmslicer/Prepare.h
@@ -4,9 +4,9 @@
 #ifndef SLICING_PREPARE_H
 #define SLICING_PREPARE_H
 
-#include "llvm/Constants.h"
-#include "llvm/GlobalVariable.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/GlobalVariable.h"
+#include "llvm/IR/Module.h"
 
 static inline const llvm::ConstantArray *getInitFuns(const llvm::Module &M) {
   llvm::GlobalVariable *initFunsVar =
diff --git a/include/llvmslicer/RuleExpressions.h b/include/llvmslicer/RuleExpressions.h
index 57206cb..d063837 100644
--- a/include/llvmslicer/RuleExpressions.h
+++ b/include/llvmslicer/RuleExpressions.h
@@ -7,9 +7,9 @@
 #include <map>
 #include <iterator>
 
-#include "llvm/Constants.h"
-#include "llvm/Function.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Instructions.h"
 
 #include "llvmslicer/LLVM.h"
 
diff --git a/include/llvmslicer/StaticSlicer.h b/include/llvmslicer/StaticSlicer.h
index 8f88de4..7c18461 100644
--- a/include/llvmslicer/StaticSlicer.h
+++ b/include/llvmslicer/StaticSlicer.h
@@ -3,6 +3,7 @@
 
 #include <map>
 #include <set>
+#include <tuple>
 #include <vector>
 #include <iterator>
 #include <algorithm>
@@ -44,12 +45,12 @@ namespace llvm { namespace slicing {
       ~StaticSlicer();
 
       virtual bool runOnModule(Module &M);
-      virtual const char *getPassName() const { return PassName;}
+      virtual llvm::StringRef getPassName() const { return PassName;}
       virtual void getAnalysisUsage(AnalysisUsage &AU) const {
         AU.setPreservesAll();
-        AU.addRequired<PostDominatorTree>();
+        AU.addRequired<PostDominatorTreeWrapperPass>();
         AU.addRequired<PostDominanceFrontier>();
-        AU.addRequired<CallGraph>();
+        AU.addRequired<CallGraphWrapperPass>();
       }
 
       template<typename OutIterator>
@@ -176,7 +177,8 @@ namespace llvm { namespace slicing { namespace detail {
 #ifdef DEBUG_EMIT
 #endif
           errs() << "\trel != callinst: add orig";
-          (*b)->dump();
+          (*b)->print(llvm::errs());
+          llvm::errs() << "\n";
           *out++ = *b;
         }
     }
@@ -215,7 +217,7 @@ namespace llvm { namespace slicing {
       if (!returnAffected) 
         return;
       FuncsToCalls::const_iterator c, e;
-      llvm::tie(c,e) = m_funcsToCalls.equal_range(f);
+      std::tie(c,e) = m_funcsToCalls.equal_range(f);
       for ( ; c != e; ++c) {
         const llvm::CallInst *CI = c->second;
         // if a call is void call
@@ -254,7 +256,7 @@ namespace llvm { namespace slicing {
       const ValSet::const_iterator relEnd =
         fss->relevant_end(getFunctionEntry(f));
       FuncsToCalls::const_iterator c, e;
-      llvm::tie(c,e) = m_funcsToCalls.equal_range(f);
+      std::tie(c,e) = m_funcsToCalls.equal_range(f);
       for ( ; c != e; ++c) {
         const llvm::CallInst *CI = c->second;
         const llvm::Function *g = CI->getParent()->getParent();
@@ -301,7 +303,7 @@ namespace llvm { namespace slicing {
           continue;
         }
         CallsToFuncs::const_iterator g, e;
-        llvm::tie(g, e) = m_callsToFuncs.equal_range(*c);
+        std::tie(g, e) = m_callsToFuncs.equal_range(*c);
         for ( ; g != e; ++g) {
           const Function *callee = g->second;
           FunctionStaticSlicer * FSS = getFSS(callee);
@@ -384,7 +386,7 @@ namespace llvm { namespace slicing {
           fss->relevant_end(getSuccInBlock(*c));
         CallsToFuncs::const_iterator g, e;
         // each call instruction might have multiple possible targets
-        llvm::tie(g, e) = m_callsToFuncs.equal_range(*c);
+        std::tie(g, e) = m_callsToFuncs.equal_range(*c);
         for ( ; g != e; ++g) {
           // for each possible callee
           typedef std::vector<const llvm::ReturnInst *> ExitsVec;
diff --git a/include/mapper/Matcher.h b/include/mapper/Matcher.h
index 7396dae..88a8301 100644
--- a/include/mapper/Matcher.h
+++ b/include/mapper/Matcher.h
@@ -5,22 +5,22 @@
 #define ___MATCHER__H_
 
 #include "llvm/Pass.h"
-#include "llvm/Function.h"
+#include "llvm/IR/Function.h"
 #include "llvm/ADT/Statistic.h"
 
-#include "llvm/Analysis/DebugInfo.h"
+#include "llvm/IR/DebugInfo.h"
 #include "llvm/Analysis/LoopInfo.h"
-#include "llvm/Analysis/Dominators.h"
-#include "llvm/Metadata.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Dominators.h"
+#include "llvm/IR/Metadata.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/Support/CommandLine.h"
-#include "llvm/Support/InstIterator.h"
-#include "llvm/Support/Dwarf.h"
-#include "llvm/Support/CFG.h"
-#include "llvm/Constants.h"
-#include "llvm/Instruction.h"
-#include "llvm/InstrTypes.h"
+#include "llvm/IR/InstIterator.h"
+#include "llvm/BinaryFormat/Dwarf.h"
+#include "llvm/IR/CFG.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Instruction.h"
+#include "llvm/IR/InstrTypes.h"
 
 #include <vector>
 #include <deque>
@@ -46,7 +46,7 @@ template <class T1, class T2> struct Pair
 
 typedef struct MetadataElement {
   unsigned key;
-  DIDescriptor value;
+  llvm::DINode* value;
 } MetadataElement;
 
 typedef struct MetadataNode {
@@ -76,7 +76,7 @@ class DISPCopy {
     Function *function;
 
   public:
-    DISPCopy(DISubprogram & DISP)
+    DISPCopy(DISubprogram & DISP, Function* f)
     {
       filename = DISP.getFilename();
       //TODO
@@ -88,9 +88,9 @@ class DISPCopy {
       // if (!(filename.size() > 0 && filename.at(0) == '/'))
       directory = DISP.getDirectory();
       name = DISP.getName();
-      linenumber = DISP.getLineNumber();
+      linenumber = DISP.getLine();
       lastline = 0;
-      function = DISP.getFunction();
+      function = f;
     }
 };
 
@@ -113,7 +113,7 @@ class ScopeInfoFinder {
 class Matcher {
   public:
     typedef std::vector<DISPCopy>::iterator sp_iterator;
-    typedef std::vector<DICompileUnit>::iterator cu_iterator;
+    typedef std::vector<DICompileUnit*>::iterator cu_iterator;
 
   protected:
     bool initialized;
@@ -124,7 +124,7 @@ class Matcher {
 
   public:
     std::vector<DISPCopy> MySPs;
-    std::vector<DICompileUnit> MyCUs;
+    std::vector<DICompileUnit*> MyCUs;
 
     int patchstrips;
     int debugstrips;
diff --git a/include/slicer/PDGSlicer.h b/include/slicer/PDGSlicer.h
index 200c95b..e2bf464 100644
--- a/include/slicer/PDGSlicer.h
+++ b/include/slicer/PDGSlicer.h
@@ -19,7 +19,7 @@
 #include "llvm/ADT/SmallPtrSet.h"
 #include "llvm/ADT/SmallSet.h"
 #include "llvm/ADT/SmallVector.h"
-#include "llvm/Instruction.h"
+#include "llvm/IR/Instruction.h"
 
 #include "dependence/DependenceGraph.h"
 #include "dependence/PgmDependenceGraph.h"
diff --git a/include/slicer/Slicer.h b/include/slicer/Slicer.h
index 15e55b2..d8bbcfc 100644
--- a/include/slicer/Slicer.h
+++ b/include/slicer/Slicer.h
@@ -39,7 +39,7 @@
 #include "llvm/ADT/SmallSet.h"
 #include "llvm/ADT/SmallVector.h"
 #include "llvm/Support/Debug.h"
-#include "llvm/Instruction.h"
+#include "llvm/IR/Instruction.h"
 #include "llvm/Support/raw_ostream.h"
 
 #include "dependence/DepGraph.h"
diff --git a/lib/analyzer/CostModel/CFGDAG.cpp b/lib/analyzer/CostModel/CFGDAG.cpp
index 73807b5..bc537b9 100644
--- a/lib/analyzer/CostModel/CFGDAG.cpp
+++ b/lib/analyzer/CostModel/CFGDAG.cpp
@@ -64,7 +64,7 @@ void BBDAG::addEdge(BBNode * from, BBNode * to)
 
 void BBDAG::init()
 {
-  _root = getNode(_f.begin());
+  _root = getNode(&*_f.begin());
   _exit = new BBNode(NULL);
   std::stack<BBNode *> dfsStack;
   dfsStack.push(_root);
@@ -73,7 +73,7 @@ void BBDAG::init()
     if (node->color != WHITE) {
       node->color = BLACK;
       dfsStack.pop();
-      const TerminatorInst * terminator  = node->bb->getTerminator();
+      const Instruction * terminator  = node->bb->getTerminator();
       if (isa<ReturnInst>(terminator) || isa<UnreachableInst>(terminator) ||
         isa<ResumeInst>(terminator)) 
         addEdge(node, _exit); // connect exit block to exit node
diff --git a/lib/analyzer/CostModel/CostModel.cpp b/lib/analyzer/CostModel/CostModel.cpp
index 2cf178a..3425b66 100644
--- a/lib/analyzer/CostModel/CostModel.cpp
+++ b/lib/analyzer/CostModel/CostModel.cpp
@@ -31,13 +31,13 @@
 #include <utility>
 #include <queue>
 
-#include "llvm/IntrinsicInst.h"
+#include "llvm/IR/IntrinsicInst.h"
 
 #include "llvm/ADT/DepthFirstIterator.h"
 #include "llvm/ADT/SmallPtrSet.h"
-#include "llvm/Analysis/PathNumbering.h"
+//#include "llvm/Analysis/PathNumbering.h"
 #include "llvm/Support/raw_ostream.h"
-#include "llvm/Support/CFG.h"
+#include "llvm/IR/CFG.h"
 
 #include "analyzer/CostModel.h"
 #include "analyzer/CFGDAG.h"
@@ -279,7 +279,7 @@ unsigned CostModel::getBasicBlockCost(const BasicBlock *BB) const
   unsigned cost = 0;
   unsigned c;
   for (BasicBlock::const_iterator BI = BB->begin(), BE = BB->end(); BI != BE; BI++) {
-    c = getInstructionCost(BI);
+    c = getInstructionCost(&*BI);
     if (c != (unsigned) -1)
       cost += c;
   }
diff --git a/lib/analyzer/CostModel/Makefile b/lib/analyzer/CostModel/Makefile
index ad118f4..dc82e15 100644
--- a/lib/analyzer/CostModel/Makefile
+++ b/lib/analyzer/CostModel/Makefile
@@ -1,8 +1,7 @@
 ##===- lib/Transforms/MDMap/Makefile -----------------------*- Makefile -*-===##
 
 LEVEL = ../../..
-LIBRARYNAME = costmodel 
-
+LIBRARYNAME = costmodel
 USEDLIBS = commons.a
 
 include $(LEVEL)/Makefile.common
diff --git a/lib/analyzer/CostModel/TargetTransformStub.cpp b/lib/analyzer/CostModel/TargetTransformStub.cpp
index d2c80b0..19e579b 100644
--- a/lib/analyzer/CostModel/TargetTransformStub.cpp
+++ b/lib/analyzer/CostModel/TargetTransformStub.cpp
@@ -35,10 +35,10 @@ int VectorTargetTransformStub::InstructionOpcodeToISD(unsigned Opcode) {
   enum InstructionOpcodes {
 #define HANDLE_INST(NUM, OPCODE, CLASS) OPCODE = NUM,
 #define LAST_OTHER_INST(NUM) InstructionOpcodesCount = NUM
-#include "llvm/Instruction.def"
+#include "llvm/IR/Instruction.def"
   };
   switch (static_cast<InstructionOpcodes>(Opcode)) {
-  case Unwind:         return 0;
+  //case Unwind:         return 0;
   case Ret:            return 0;
   case Br:             return 0;
   case Switch:         return 0;
@@ -106,7 +106,8 @@ std::pair<unsigned, MVT>
 VectorTargetTransformStub::getTypeLegalizationCost(Type *Ty) const {
 
   LLVMContext &C = Ty->getContext();
-  EVT MTy = TLI->getValueType(Ty, true);
+  const DataLayout &DL = TLI->getTargetMachine().createDataLayout();
+  EVT MTy = TLI->getValueType(DL, Ty, true);
 
   unsigned Cost = 1;
   // We keep legalizing the type until we find a legal kind. We assume that
diff --git a/lib/analyzer/CostModel/X86CostModel.cpp b/lib/analyzer/CostModel/X86CostModel.cpp
index fcce256..666007f 100644
--- a/lib/analyzer/CostModel/X86CostModel.cpp
+++ b/lib/analyzer/CostModel/X86CostModel.cpp
@@ -29,11 +29,14 @@
 
 #include <string>
 
-#include "llvm/LLVMContext.h"
-#include "llvm/Target/TargetData.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/Function.h"
+
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/DataLayout.h"
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetSubtargetInfo.h"
-#include "llvm/Target/TargetLowering.h"
+#include "llvm/CodeGen/TargetSubtargetInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
 
 #include "llvm/Support/TargetSelect.h"
 #include "llvm/Support/TargetRegistry.h"
@@ -65,12 +68,20 @@ using namespace llvm;
 X86CostModel::X86CostModel(TargetMachine *TM)
 {
   assert (TM && "Target machine cannot be NULL");
-  TLI = TM->getTargetLowering();
+  
+  LLVMContext Ctx;
+  Module M("dummy", Ctx);
+  M.setDataLayout(TM->createDataLayout());
+  Function *F = Function::Create(FunctionType::get(Type::getVoidTy(Ctx), false),
+                                 GlobalValue::ExternalLinkage, "dummy", &M);
+  // 通过 Subtarget 获取 TargetLowering
+  TLI = TM->getSubtargetImpl(*F)->getTargetLowering();
+
   assert(TLI && "No associated target lowering");
   VTT = new VectorTargetTransformStub(TLI);
 
-  const MCSubtargetInfo &TSI = TM->getSubtarget<MCSubtargetInfo>();
-  uint64_t Bits = TSI.getFeatureBits();
+  const MCSubtargetInfo *TSI = TM->getMCSubtargetInfo();
+  FeatureBitset Bits = TSI->getFeatureBits();
   #ifdef X86COSTMODEL_DEBUG
   errs() << "Feature bits: " << Bits << "\n";
   #endif
@@ -186,8 +197,9 @@ unsigned X86CostModel::getCastInstrCost(unsigned Opcode, Type *Dst,
   int ISD = VectorTargetTransformStub::InstructionOpcodeToISD(Opcode);
   assert(ISD && "Invalid opcode");
 
-  EVT SrcTy = TLI->getValueType(Src);
-  EVT DstTy = TLI->getValueType(Dst);
+  const DataLayout &DL = TLI->getTargetMachine().createDataLayout();
+  EVT SrcTy = TLI->getValueType(DL, Src);
+  EVT DstTy = TLI->getValueType(DL, Dst);
 
   if (!SrcTy.isSimple() || !DstTy.isSimple()) {
     WARN_DEFAULT_COST(cast);
diff --git a/lib/analyzer/DiffEngine/DiffConsumer.cpp b/lib/analyzer/DiffEngine/DiffConsumer.cpp
index ca27f61..a2ef389 100644
--- a/lib/analyzer/DiffEngine/DiffConsumer.cpp
+++ b/lib/analyzer/DiffEngine/DiffConsumer.cpp
@@ -11,8 +11,8 @@
 //
 //===----------------------------------------------------------------------===//
 
-#include "llvm/Module.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/Support/ErrorHandling.h"
 
 #include "analyzer/DiffConsumer.h"
diff --git a/lib/analyzer/DiffEngine/DiffLog.cpp b/lib/analyzer/DiffEngine/DiffLog.cpp
index cfc4c0d..0e814a6 100644
--- a/lib/analyzer/DiffEngine/DiffLog.cpp
+++ b/lib/analyzer/DiffEngine/DiffLog.cpp
@@ -11,7 +11,7 @@
 //
 //===----------------------------------------------------------------------===//
 
-#include "llvm/Instructions.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/ADT/SmallVector.h"
 #include "llvm/ADT/StringRef.h"
 
diff --git a/lib/analyzer/DiffEngine/DifferenceEngine.cpp b/lib/analyzer/DiffEngine/DifferenceEngine.cpp
index 5c18387..c187ef0 100644
--- a/lib/analyzer/DiffEngine/DifferenceEngine.cpp
+++ b/lib/analyzer/DiffEngine/DifferenceEngine.cpp
@@ -14,17 +14,17 @@
 
 #include <utility>
 
-#include "llvm/Constants.h"
-#include "llvm/Function.h"
-#include "llvm/Instructions.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Module.h"
 #include "llvm/ADT/DenseMap.h"
 #include "llvm/ADT/DenseSet.h"
 #include "llvm/ADT/SmallVector.h"
 #include "llvm/ADT/StringRef.h"
 #include "llvm/ADT/StringSet.h"
-#include "llvm/Support/CallSite.h"
-#include "llvm/Support/CFG.h"
+#include "llvm/IR/CallSite.h"
+#include "llvm/IR/CFG.h"
 #include "llvm/Support/ErrorHandling.h"
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/Support/type_traits.h"
@@ -71,7 +71,7 @@ public:
     unsigned NewSize = Storage.size() - 1;
     if (NewSize) {
       // Move the slot at the end to the beginning.
-      if (isPodLike<T>::value)
+      if (std::is_trivially_copyable<T>::value)
         Storage[0] = Storage[NewSize];
       else
         std::swap(Storage[0], Storage[NewSize]);
@@ -321,13 +321,14 @@ class FunctionDifferenceEngine {
       bool Difference = false;
 
       DenseMap<ConstantInt*,BasicBlock*> LCases;
-      for (unsigned I = 1, E = LI->getNumCases(); I != E; ++I)
-        LCases[LI->getCaseValue(I)] = LI->getSuccessor(I);
-      for (unsigned I = 1, E = RI->getNumCases(); I != E; ++I) {
-        ConstantInt *CaseValue = RI->getCaseValue(I);
+      for (auto Case : LI->cases()) {
+        LCases[Case.getCaseValue()] = Case.getCaseSuccessor();
+      }
+      for (auto Case : RI->cases()) {
+        ConstantInt *CaseValue = Case.getCaseValue();
         BasicBlock *LCase = LCases[CaseValue];
         if (LCase) {
-          if (TryUnify) tryUnify(LCase, RI->getSuccessor(I));
+          if (TryUnify) tryUnify(LCase, Case.getCaseSuccessor());
           LCases.erase(CaseValue);
         } else if (!Difference) {
           if (Complain)
@@ -600,11 +601,11 @@ void FunctionDifferenceEngine::runBlockDiff(BasicBlock::iterator LStart,
   // If the terminators have different kinds, but one is an invoke and the
   // other is an unconditional branch immediately following a call, unify
   // the results and the destinations.
-  TerminatorInst *LTerm = LStart->getParent()->getTerminator();
-  TerminatorInst *RTerm = RStart->getParent()->getTerminator();
+  Instruction *LTerm = LStart->getParent()->getTerminator();
+  Instruction *RTerm = RStart->getParent()->getTerminator();
   if (isa<BranchInst>(LTerm) && isa<InvokeInst>(RTerm)) {
     if (cast<BranchInst>(LTerm)->isConditional()) return;
-    BasicBlock::iterator I = LTerm;
+    BasicBlock::iterator I = LTerm->getIterator();
     if (I == LStart->getParent()->begin()) return;
     --I;
     if (!isa<CallInst>(*I)) return;
@@ -617,7 +618,7 @@ void FunctionDifferenceEngine::runBlockDiff(BasicBlock::iterator LStart,
     tryUnify(LTerm->getSuccessor(0), RInvoke->getNormalDest());
   } else if (isa<InvokeInst>(LTerm) && isa<BranchInst>(RTerm)) {
     if (cast<BranchInst>(RTerm)->isConditional()) return;
-    BasicBlock::iterator I = RTerm;
+    BasicBlock::iterator I = RTerm->getIterator();
     if (I == RStart->getParent()->begin()) return;
     --I;
     if (!isa<CallInst>(*I)) return;
diff --git a/lib/analyzer/RiskEvaluator/Evaluator.cpp b/lib/analyzer/RiskEvaluator/Evaluator.cpp
index 4356bc9..10bae4a 100644
--- a/lib/analyzer/RiskEvaluator/Evaluator.cpp
+++ b/lib/analyzer/RiskEvaluator/Evaluator.cpp
@@ -34,10 +34,10 @@
 #include <string>
 #include <ctype.h>
 
-#include "llvm/ADT/OwningPtr.h"
-#include "llvm/IntrinsicInst.h"
+#include <memory>
+#include "llvm/IR/IntrinsicInst.h"
 
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/CallSite.h"
 #include "llvm/Support/raw_ostream.h"
 
 #include "commons/handy.h"
@@ -135,9 +135,9 @@ const char * toExpStr(Expensiveness exp)
 
 bool DummyLoopInfo::runOnFunction(Function &F)
 {
-  LoopInfo *LI = &getAnalysis<LoopInfo>(); 
+  LoopInfo *LI = &getAnalysis<LoopInfoWrapperPass>().getLoopInfo(); 
   for (Function::iterator FI = F.begin() , FE = F.end(); FI != FE; ++FI) {
-    BasicBlock * BB = FI;
+    BasicBlock * BB = &*FI;
     // Only inserted the BBs in the Loop.
     // This is to mimic the LoopInfo.getLoopFor
     if (LI->getLoopFor(BB) != NULL)
@@ -388,7 +388,7 @@ bool RiskEvaluator::isPerfSensitive(const BranchInst *I)
   for (unsigned i = 0; i < succs; ++i) {
     BasicBlock * BB = I->getSuccessor(i);
     for (BasicBlock::iterator BI = BB->begin(), BE = BB->end(); BI != BE; BI++) {
-      if (calcInstExp(BI) == Expensive)
+      if (calcInstExp(&*BI) == Expensive)
         exps++;
     }
   }
@@ -403,8 +403,8 @@ bool RiskEvaluator::runOnFunction(Function &F)
     return false;
   }
   memset(FuncRiskStat, 0, sizeof(FuncRiskStat));
-  LocalLI = &getAnalysis<LoopInfo>(); 
-  SE = &getAnalysis<ScalarEvolution>(); 
+  LocalLI = &getAnalysis<LoopInfoWrapperPass>().getLoopInfo(); 
+  SE = &getAnalysis<ScalarEvolutionWrapperPass>().getSE();
   INDENT = 4;
   InstVecTy &inst_vec = m_inst_map[&F];
   std::map<Loop *, unsigned> LoopDepthMap;
diff --git a/lib/commons/CallSiteFinder.cpp b/lib/commons/CallSiteFinder.cpp
index d0a839d..a1facca 100644
--- a/lib/commons/CallSiteFinder.cpp
+++ b/lib/commons/CallSiteFinder.cpp
@@ -1,5 +1,5 @@
-#include "llvm/Instruction.h"
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/Instruction.h"
+#include "llvm/IR/CallSite.h"
 
 #include "commons/CallSiteFinder.h"
 
diff --git a/lib/commons/LLVMHelper.cpp b/lib/commons/LLVMHelper.cpp
index 1f560df..27483b4 100644
--- a/lib/commons/LLVMHelper.cpp
+++ b/lib/commons/LLVMHelper.cpp
@@ -31,23 +31,28 @@
 #include <iostream>
 #include <algorithm>
 
-#include "llvm/Analysis/DebugInfo.h"
+#include "llvm/IR/DebugInfo.h"
 
-#include "llvm/Support/IRReader.h"
+#include "llvm/IRReader/IRReader.h"
 #include "llvm/Support/Host.h"
 #include "llvm/Support/TargetSelect.h"
 #include "llvm/Support/TargetRegistry.h"
 
-#include "llvm/Target/TargetData.h"
+#include "llvm/IR/DataLayout.h"
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetLibraryInfo.h"
-#include "llvm/Target/TargetLowering.h"
-#include "llvm/Target/TargetSubtargetInfo.h"
-#include "llvm/Target/TargetLowering.h"
+#include "llvm/Analysis/TargetLibraryInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
+#include "llvm/CodeGen/TargetSubtargetInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
 
 #include "commons/handy.h"
 #include "commons/LLVMHelper.h"
 
+#include "llvm/Support/SourceMgr.h"
+#include "llvm/IRReader/IRReader.h"
+#include "llvm/Support/Host.h" // 用于 sys::getDefaultTargetTriple
+#include "llvm/Target/TargetOptions.h" // 用于 TargetOptions
+
 //#define LLVMHELPER_DEBUG
 
 gen_dbg(helper)
@@ -110,24 +115,24 @@ size_t count_strips(Module * M)
     size_t buf_len = 128;
     char *buf = (char *) malloc(buf_len);
     for (unsigned i = 0, e = CU_Nodes->getNumOperands(); i != e; ++i) {
-      DICompileUnit CU1(CU_Nodes->getOperand(i));
+      DICompileUnit* CU1 = dyn_cast<DICompileUnit>(CU_Nodes->getOperand(i));
       if (i == 0) {
         if (i + 1 != e) {
           i++;
-          DICompileUnit CU2(CU_Nodes->getOperand(i));
-          buf = common_prefix(buf, buf_len, CU1.getDirectory().data(), CU2.getDirectory().data());
+          DICompileUnit* CU2 = dyn_cast<DICompileUnit>(CU_Nodes->getOperand(i));
+          buf = common_prefix(buf, buf_len, CU1->getDirectory().data(), CU2->getDirectory().data());
         }
         else {
-          size_t cu_len = CU1.getDirectory().size();
+          size_t cu_len = CU1->getDirectory().size();
           if (cu_len > buf_len) {
             buf = (char *) realloc(buf, cu_len);
             buf_len = cu_len;
           }
-          strcpy(buf, CU1.getDirectory().data()); 
+          strcpy(buf, CU1->getDirectory().data()); 
         }
       }
       else {
-        buf = common_prefix(buf, buf_len, buf, CU1.getDirectory().data());
+        buf = common_prefix(buf, buf_len, buf, CU1->getDirectory().data());
       }
     }
     unsigned cnt = countnchr(buf, -1, '/');
@@ -150,15 +155,15 @@ size_t count_strips(Module * M)
 Module *ReadModule(LLVMContext &Context, StringRef Name)
 {
   SMDiagnostic Diag;
-  Module *M = ParseIRFile(Name, Diag, Context);
+  Module *M = parseIRFile(Name, Diag, Context).release();
   if (!M)
-    std::cerr << "IR file parsing failed: " << Diag.getMessage() << std::endl;
+    std::cerr << "IR file parsing failed: " << Diag.getMessage().str() << std::endl;
   return M;
 }
 
 TargetMachine * getTargetMachine()
 {
-  const std::string TripleStr = llvm::sys::getHostTriple();
+  const std::string TripleStr = llvm::sys::getDefaultTargetTriple();
   const std::string CPUStr = llvm::sys::getHostCPUName();
   const std::string FeatureStr;
   helper_debug("Triple: %s CPU: %s\n", TripleStr.c_str(), CPUStr.c_str());
@@ -176,7 +181,8 @@ TargetMachine * getTargetMachine()
     exit(1);
   }
   // Create TargetMachine
-  TargetMachine* TM = T->createTargetMachine(TripleStr, CPUStr, FeatureStr);
+  TargetOptions Options;
+  TargetMachine* TM = T->createTargetMachine(TripleStr, CPUStr, FeatureStr, Options, None, None, CodeGenOpt::Default);
   if(TM == NULL) {
     std::cerr << "Cannot create target machine" << std::endl;
     exit(1);
@@ -190,20 +196,20 @@ void initPassRegistry(PassRegistry & Registry)
   initializeScalarOpts(Registry);
   initializeIPO(Registry);
   initializeAnalysis(Registry);
-  initializeIPA(Registry);
+  //initializeIPA(Registry);
   initializeTransformUtils(Registry);
   initializeInstCombine(Registry);
   initializeInstrumentation(Registry);
   initializeTarget(Registry);
 }
 
-TargetData * getTargetData(Module *M)
+DataLayout * getDataLayout(Module *M)
 {
-  // Add an appropriate TargetData instance for this module.
-  TargetData *TD = 0;
-  const std::string &ModuleDataLayout = M->getDataLayout();
+  // Add an appropriate DataLayout instance for this module.
+  DataLayout *TD = 0;
+  const std::string &ModuleDataLayout = M->getDataLayout().getStringRepresentation();
   if (!ModuleDataLayout.empty())
-    TD = new TargetData(ModuleDataLayout);
+    TD = new DataLayout(ModuleDataLayout);
   return TD;
 }
 
diff --git a/lib/commons/PassRunner.cpp b/lib/commons/PassRunner.cpp
index 3425e32..8651093 100644
--- a/lib/commons/PassRunner.cpp
+++ b/lib/commons/PassRunner.cpp
@@ -12,33 +12,34 @@
 //
 //===----------------------------------------------------------------------===//
 
-#include "llvm/LLVMContext.h"
-#include "llvm/Module.h"
-#include "llvm/PassManager.h"
-#include "llvm/CallGraphSCCPass.h"
-#include "llvm/Bitcode/ReaderWriter.h"
-#include "llvm/Assembly/PrintModulePass.h"
-#include "llvm/Analysis/DebugInfo.h"
-#include "llvm/Analysis/Verifier.h"
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/PassManager.h"
+#include "llvm/Analysis/CallGraphSCCPass.h"
+#include "llvm/Bitcode/BitcodeWriter.h"
+#include "llvm/Bitcode/BitcodeReader.h"
+#include "llvm/IR/IRPrintingPasses.h"
+#include "llvm/IR/DebugInfo.h"
+#include "llvm/IR/DebugInfoMetadata.h"
+#include "llvm/IR/Verifier.h"
 #include "llvm/Analysis/LoopPass.h"
 #include "llvm/Analysis/RegionPass.h"
 #include "llvm/Analysis/CallGraph.h"
-#include "llvm/Target/TargetData.h"
-#include "llvm/Target/TargetLibraryInfo.h"
+#include "llvm/IR/DataLayout.h"
+#include "llvm/Analysis/TargetLibraryInfo.h"
 #include "llvm/Target/TargetMachine.h"
 #include "llvm/ADT/StringSet.h"
 #include "llvm/ADT/Triple.h"
-#include "llvm/Support/PassNameParser.h"
+#include "llvm/IR/LegacyPassNameParser.h"
 #include "llvm/Support/Signals.h"
 #include "llvm/Support/Debug.h"
-#include "llvm/Support/IRReader.h"
+#include "llvm/IRReader/IRReader.h"
 #include "llvm/Support/ManagedStatic.h"
 #include "llvm/Support/PluginLoader.h"
 #include "llvm/Support/PrettyStackTrace.h"
 #include "llvm/Support/SystemUtils.h"
 #include "llvm/Support/ToolOutputFile.h"
 #include "llvm/LinkAllPasses.h"
-#include "llvm/LinkAllVMCore.h"
 #include "llvm/Transforms/IPO/PassManagerBuilder.h"
 
 #include "mapper/Matcher.h"
@@ -49,6 +50,16 @@
 #include <iostream>
 #include <stdio.h>
 
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/LegacyPassManager.h"
+#include "llvm/IRReader/IRReader.h"
+#include "llvm/Support/SourceMgr.h"
+#include "llvm/Support/ToolOutputFile.h"
+#include "llvm/Support/FileSystem.h"
+#include "llvm/IR/DebugInfo.h"
+#include "llvm/IR/DebugInfoMetadata.h"
+#include "llvm/Analysis/TargetLibraryInfo.h"
+#include "llvm/IRReader/IRReader.h"
 
 using namespace llvm;
 
@@ -99,7 +110,7 @@ struct CallGraphSCCPassPrinter : public CallGraphSCCPass {
     return false;
   }
 
-  virtual const char *getPassName() const { return PassName.c_str(); }
+  virtual StringRef getPassName() const { return PassName.c_str(); }
 
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.addRequiredID(PassToPrint->getTypeInfo());
@@ -130,7 +141,7 @@ struct ModulePassPrinter : public ModulePass {
     return false;
   }
 
-  virtual const char *getPassName() const { return PassName.c_str(); }
+  virtual StringRef getPassName() const { return PassName.c_str(); }
 
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.addRequiredID(PassToPrint->getTypeInfo());
@@ -162,7 +173,7 @@ struct FunctionPassPrinter : public FunctionPass {
     return false;
   }
 
-  virtual const char *getPassName() const { return PassName.c_str(); }
+  virtual StringRef getPassName() const { return PassName.c_str(); }
 
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.addRequiredID(PassToPrint->getTypeInfo());
@@ -195,7 +206,7 @@ struct LoopPassPrinter : public LoopPass {
     return false;
   }
 
-  virtual const char *getPassName() const { return PassName.c_str(); }
+  virtual StringRef getPassName() const { return PassName.c_str(); }
 
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.addRequiredID(PassToPrint->getTypeInfo());
@@ -221,7 +232,7 @@ struct RegionPassPrinter : public RegionPass {
     if (!Quiet) {
       Out << "Printing analysis '" << PassToPrint->getPassName() << "' for "
         << "region: '" << R->getNameStr() << "' in function '"
-        << R->getEntry()->getParent()->getNameStr() << "':\n";
+        << R->getEntry()->getParent()->getName().str() << "':\n";
     }
     // Get and print pass...
    getAnalysisID<Pass>(PassToPrint->getTypeInfo()).print(Out,
@@ -229,7 +240,7 @@ struct RegionPassPrinter : public RegionPass {
     return false;
   }
 
-  virtual const char *getPassName() const { return PassName.c_str(); }
+  virtual StringRef getPassName() const { return PassName.c_str(); }
 
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.addRequiredID(PassToPrint->getTypeInfo());
@@ -262,7 +273,7 @@ struct BasicBlockPassPrinter : public BasicBlockPass {
     return false;
   }
 
-  virtual const char *getPassName() const { return PassName.c_str(); }
+  virtual StringRef getPassName() const { return PassName.c_str(); }
 
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.addRequiredID(PassToPrint->getTypeInfo());
@@ -280,18 +291,18 @@ struct BreakpointPrinter : public ModulePass {
     : ModulePass(ID), Out(out) {
     }
 
-  void getContextName(DIDescriptor Context, std::string &N) {
-    if (Context.isNameSpace()) {
-      DINameSpace NS(Context);
-      if (!NS.getName().empty()) {
-        getContextName(NS.getContext(), N);
-        N = N + NS.getName().str() + "::";
+  void getContextName(DIScope* Context, std::string &N) {
+    if (!Context) return;
+
+    if (auto *NS = dyn_cast<DINamespace>(Context)) {
+      if (!NS->getName().empty()) {
+        getContextName(NS->getScope(), N);
+        N = N + NS->getName().str() + "::";
       }
-    } else if (Context.isType()) {
-      DIType TY(Context);
-      if (!TY.getName().empty()) {
-        getContextName(TY.getContext(), N);
-        N = N + TY.getName().str() + "::";
+    } else if (auto *TY = dyn_cast<DIType>(Context)) {
+      if (!TY->getName().empty()) {
+        getContextName(TY->getScope(), N);
+        N = N + TY->getName().str() + "::";
       }
     }
   }
@@ -300,13 +311,26 @@ struct BreakpointPrinter : public ModulePass {
     StringSet<> Processed;
     if (NamedMDNode *NMD = M.getNamedMetadata("llvm.dbg.sp"))
       for (unsigned i = 0, e = NMD->getNumOperands(); i != e; ++i) {
-        std::string Name;
-        DISubprogram SP(NMD->getOperand(i));
-        if (SP.Verify())
-          getContextName(SP.getContext(), Name);
-        Name = Name + SP.getDisplayName().str();
-        if (!Name.empty() && Processed.insert(Name)) {
-          Out << Name << "\n";
+        // 获取 CompileUnit
+        auto *CU = dyn_cast<DICompileUnit>(NMD->getOperand(i));
+        if (!CU) continue;
+
+        // 遍历 CompileUnit 中的 Subprograms
+        // 注意：在 LLVM 9 中，Subprograms 可能不直接挂在 CU 下，而是分散在各处
+        // 但为了兼容旧逻辑，我们尝试遍历 CU 的 subprograms 列表
+        // 或者更通用的方法是遍历 Module 中的所有 Function 并获取其 Subprogram
+        
+        for (auto &F : M) {
+          DISubprogram *SP = F.getSubprogram();
+          if (!SP) continue;
+
+          std::string Name;
+          getContextName(SP->getScope(), Name);
+          Name = Name + SP->getName().str();
+
+          if (!Name.empty() && Processed.insert(Name).second) {
+            Out << Name << "\n";
+          }
         }
       }
     return false;
@@ -333,7 +357,7 @@ struct LoopInfoFinder : public FunctionPass {
         if (F.getName().equals("_ZL23test_if_skip_sort_orderP13st_join_tableP8st_ordermbP6BitmapILj64EE")) {
             errs() << "Function: " << F.getName() << "\n";
             if (!F.isDeclaration())  {
-            LoopInfo &li = getAnalysis<LoopInfo>();
+            LoopInfo &li = getAnalysis<LoopInfoWrapperPass>().getLoopInfo();
             if (li.begin() == li.end()) {
                 errs() << "no loop in this function" << "\n";
             }
@@ -399,12 +423,12 @@ struct LoopInfoFinder : public FunctionPass {
         return false;
     }
 
-    virtual const char *getPassName() const { return PassName.c_str(); }
+    virtual llvm::StringRef getPassName() const { return PassName.c_str(); }
 
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
         AU.setPreservesAll();
-        AU.addRequired<DominatorTree>();
-        AU.addRequired<LoopInfo>();
+        AU.addRequired<DominatorTreeWrapperPass>();
+        AU.addRequired<LoopInfoWrapperPass>();
     }
 };
 
@@ -427,7 +451,7 @@ int Run(int argc, char **argv)
     EnableDebugBuffering = false;
 
     llvm_shutdown_obj Y;  // Call llvm_shutdown() on exit.
-    LLVMContext &Context = getGlobalContext();
+    LLVMContext Context;
 
     // Initialize passes
     PassRegistry &Registry = *PassRegistry::getPassRegistry();
@@ -435,7 +459,6 @@ int Run(int argc, char **argv)
     initializeScalarOpts(Registry);
     initializeIPO(Registry);
     initializeAnalysis(Registry);
-    initializeIPA(Registry);
     initializeTransformUtils(Registry);
     initializeInstCombine(Registry);
     initializeInstrumentation(Registry);
@@ -452,50 +475,44 @@ int Run(int argc, char **argv)
 
     std::cout << InputFilename << std::endl;
     // Load the input module...
-    std::auto_ptr<Module> M;
-    M.reset(ParseIRFile(InputFilename, Err, Context));
+    std::unique_ptr<Module> M = llvm::parseIRFile(
+      InputFilename, Err, Context);
 
     if (M.get() == 0) {
-        Err.Print(argv[0], errs());
+        Err.print(argv[0], errs());
         return 1;
     }
 
     // Figure out what stream we are supposed to write to...
-    OwningPtr<tool_output_file> Out;
+    std::unique_ptr<ToolOutputFile> Out;
     if (OutputFilename.empty())
         OutputFilename = "-";
 
-    std::string ErrorInfo;
-    Out.reset(new tool_output_file(OutputFilename.c_str(), ErrorInfo,
-                raw_fd_ostream::F_Binary));
-    if (!ErrorInfo.empty()) {
-        errs() << ErrorInfo << '\n';
+    std::error_code EC;
+    Out.reset(new ToolOutputFile(OutputFilename, EC, sys::fs::OF_None));
+    if (EC) {
+        errs() << EC.message() << '\n';
         return 1;
     }
 
     // Create a PassManager to hold and optimize the collection of passes we are
     // about to build.
     //
-    PassManager Passes;
+    legacy::PassManager Passes;
 
     // Add an appropriate TargetLibraryInfo pass for the module's triple.
-    TargetLibraryInfo *TLI = new TargetLibraryInfo(Triple(M->getTargetTriple()));
-
-    Passes.add(TLI);
+    TargetLibraryInfoImpl TLII(Triple(M->getTargetTriple()));
+    Passes.add(new TargetLibraryInfoWrapperPass(TLII));
     
-    LoopInfo *li = new LoopInfo();
-    Passes.add(li);
+    Passes.add(new LoopInfoWrapperPass());
 
-    // Add an appropriate TargetData instance for this module.
-    TargetData *TD = 0;
-    const std::string &ModuleDataLayout = M.get()->getDataLayout();
+    // Add an appropriate DataLayout instance for this module.
+    DataLayout *TD = 0;
+    const std::string &ModuleDataLayout = M.get()->getDataLayout().getStringRepresentation();
     if (!ModuleDataLayout.empty())
-        TD = new TargetData(ModuleDataLayout);
-
-    if (TD)
-        Passes.add(TD);
+        TD = new DataLayout(ModuleDataLayout);
 
-    OwningPtr<FunctionPassManager> FPasses;
+    std::unique_ptr<legacy::FunctionPassManager> FPasses;
 
     // Create a new optimization pass for each one specified on the command line
     for (unsigned i = 0; i < PassList.size(); ++i) {
diff --git a/lib/dependence/DepGraphBuilder.cpp b/lib/dependence/DepGraphBuilder.cpp
index 9bbf135..2859d93 100644
--- a/lib/dependence/DepGraphBuilder.cpp
+++ b/lib/dependence/DepGraphBuilder.cpp
@@ -38,8 +38,8 @@ bool DepGraphBuilder::buildMemDepGraph()
   #ifdef DEPBUILDER_DEBUG
   errs() << "building mem dep graph for " << F->getName() << "\n";
   #endif
-  MemoryDependenceAnalysis & MDA = getAnalysis<MemoryDependenceAnalysis>();
-  AliasAnalysis &AA = getAnalysis<AliasAnalysis>();
+  llvm::MemoryDependenceResults & MDA = getAnalysis<MemoryDependenceWrapperPass>().getMemDep();
+  AAResults &AA = getAnalysis<AAResultsWrapperPass>().getAAResults();
   for (inst_iterator I = inst_begin(*F), E = inst_end(*F); I != E; I++) {
     Instruction * inst = &*I;
     // Skip non-memory access instruction
@@ -48,10 +48,11 @@ bool DepGraphBuilder::buildMemDepGraph()
     MemDepResult Res = MDA.getDependency(inst);
     if (!Res.isNonLocal()) {
       AddDepResultToGraph(inst, Res, TrueMemDep);
-    } else if (CallSite CS = cast<Value>(inst)) {
-      const MemoryDependenceAnalysis::NonLocalDepInfo &NLDI =
-        MDA.getNonLocalCallDependency(CS);
-      for (MemoryDependenceAnalysis::NonLocalDepInfo::const_iterator
+    } else if (CallSite CS = CallSite(inst)) {
+      CallBase *CB = cast<CallBase>(CS.getInstruction());
+      const auto &NLDI =
+        MDA.getNonLocalCallDependency(CB);
+      for (auto
           NLI = NLDI.begin(), NLE = NLDI.end(); NLI != NLE; ++NLI) {
         AddDepResultToGraph(inst, NLI->getResult(), AntiMemDep);
       }
@@ -62,17 +63,14 @@ bool DepGraphBuilder::buildMemDepGraph()
         if (!load->isUnordered())
           continue;
         //AliasAnalysis::Location Loc(load->getPointerOperand());
-        AliasAnalysis::Location Loc = AA.getLocation(load);
-        MDA.getNonLocalPointerDependency(Loc, true, load->getParent(), NLDI);
+        MDA.getNonLocalPointerDependency(load, NLDI);
       } else if (StoreInst *store = dyn_cast<StoreInst>(inst)) {
         if (!store->isUnordered())
           continue;
         //AliasAnalysis::Location Loc(store->getPointerOperand());
-        AliasAnalysis::Location Loc = AA.getLocation(store);
-        MDA.getNonLocalPointerDependency(Loc, false, store->getParent(), NLDI);
+        MDA.getNonLocalPointerDependency(store, NLDI);
       } else if (VAArgInst *VI = dyn_cast<VAArgInst>(inst)) {
-        AliasAnalysis::Location Loc = AA.getLocation(VI);
-        MDA.getNonLocalPointerDependency(Loc, false, VI->getParent(), NLDI);
+        MDA.getNonLocalPointerDependency(VI, NLDI);
       } else {
         llvm_unreachable("Unknown memory instruction!");
       }
diff --git a/lib/dependence/Makefile b/lib/dependence/Makefile
index 3ec92c8..e1d2234 100644
--- a/lib/dependence/Makefile
+++ b/lib/dependence/Makefile
@@ -9,7 +9,7 @@ LEVEL=../..
 # Give the name of a library.  This will build a dynamic version.
 #
 BUILD_ARCHIVE = 1
-LIBRARYNAME = dependence 
+LIBRARYNAME = dependence
 
 # ARCHIVE_LIBRARY = 1
 # USEDLIBS = graph.a
diff --git a/lib/mapper/Matcher.cpp b/lib/mapper/Matcher.cpp
index 9f8a83f..0ff6d7e 100644
--- a/lib/mapper/Matcher.cpp
+++ b/lib/mapper/Matcher.cpp
@@ -3,21 +3,21 @@
 
 static bool LOCAL_DEBUG = false;
 
-bool cmpDICU(const DICompileUnit & CU1, const DICompileUnit & CU2) 
+bool cmpDICU(const DICompileUnit * CU1, const DICompileUnit * CU2) 
 { 
-  int cmp = CU1.getDirectory().compare(CU2.getDirectory());
+  int cmp = CU1->getDirectory().compare(CU2->getDirectory());
   if (cmp == 0)
-    cmp = CU1.getFilename().compare(CU2.getFilename());
+    cmp = CU1->getFilename().compare(CU2->getFilename());
   return cmp >= 0 ? false : true;
 }
 
-bool cmpDISP(const DISubprogram & SP1, const DISubprogram & SP2) 
+bool cmpDISP(const DISubprogram * SP1, const DISubprogram * SP2) 
 { 
-  int cmp = SP1.getDirectory().compare(SP2.getDirectory());
+  int cmp = SP1->getDirectory().compare(SP2->getDirectory());
   if (cmp == 0) {
-    cmp = SP1.getFilename().compare(SP2.getFilename());
+    cmp = SP1->getFilename().compare(SP2->getFilename());
     if (cmp == 0) {
-      cmp = SP1.getLineNumber() - SP2.getLineNumber();
+      cmp = SP1->getLine() - SP2->getLine();
     }
   }
   return cmp >= 0 ? false : true;
@@ -48,7 +48,7 @@ bool skipFunction(Function *F)
 unsigned ScopeInfoFinder::getInstLine(const Instruction *I)
 {
   DebugLoc Loc = I->getDebugLoc();
-  if (Loc.isUnknown()) {
+  if (!Loc) {
     if (LOCAL_DEBUG) {
       errs() << "Unknown LOC" << "\n";
     }
@@ -64,7 +64,7 @@ unsigned ScopeInfoFinder::getLastLine(Function *F)
   const BasicBlock & BB = F->back();
   const Instruction & I = BB.back();
   DebugLoc Loc = I.getDebugLoc();
-  if (Loc.isUnknown()) 
+  if (!Loc) 
     return 0;
   return Loc.getLine();
 }
@@ -130,14 +130,13 @@ void Matcher::processInst(Function *F)
     /** Get each instruction's scope information **/
     for (BasicBlock::iterator BI = FI->begin(), BE = FI->end(); BI != BE; BI++) {
       DebugLoc Loc = BI->getDebugLoc();
-      if (Loc.isUnknown())
+      if (!Loc)
         continue;
       LLVMContext & Ctx = BI->getContext();
 
-      DIDescriptor Scope(Loc.getScope(Ctx));
-      if (Scope.isLexicalBlock()) {
-        DILexicalBlock DILB(Scope);
-        errs() << "Block :" << DILB.getLineNumber() << ", " << DILB.getColumnNumber() << "\n";
+      DIScope* Scope = Loc->getScope();
+      if (auto *DILB = dyn_cast<DILexicalBlock>(Scope)) {
+        errs() << "Block :" << DILB->getLine() << ", " << DILB->getColumn() << "\n";
       }
     }
   }
@@ -154,13 +153,13 @@ void Matcher::processBasicBlock(Function *F)
       continue;
     }
     DebugLoc Loc = first->getDebugLoc();
-    if (Loc.isUnknown()) {
+    if (!Loc) {
       errs() << "Unknown LOC information" << "\n";
       continue;
     }
     errs() << "Block :" << Loc.getLine();
     Loc = last->getDebugLoc();
-    if (Loc.isUnknown()) {
+    if (!Loc) {
       errs() << "Unknown LOC information" << "\n";
       continue;
     }
@@ -189,9 +188,9 @@ void Matcher::processCompileUnits(Module &M)
   MyCUs.clear();
   if (NamedMDNode *CU_Nodes = M.getNamedMetadata("llvm.dbg.cu"))
     for (unsigned i = 0, e = CU_Nodes->getNumOperands(); i != e; ++i) {
-      DICompileUnit DICU(CU_Nodes->getOperand(i));
-      if (DICU.getVersion() > LLVMDebugVersion10)
-        MyCUs.push_back(DICU);
+      auto *DICU = dyn_cast<DICompileUnit>(CU_Nodes->getOperand(i));
+      if (!DICU)  continue;
+      MyCUs.push_back(DICU);
     }
 
   /** Sort based on file name, directory and line number **/
@@ -199,18 +198,17 @@ void Matcher::processCompileUnits(Module &M)
   if (LOCAL_DEBUG) {
     cu_iterator I, E;
     for (I = MyCUs.begin(), E = MyCUs.end(); I != E; I++) {
-      errs() << "CU: " << I->getDirectory() << "/" << I->getFilename() << "\n";
+      errs() << "CU: " << (*I)->getDirectory() << "/" << (*I)->getFilename() << "\n";
     }
   }
 }
 
 void Matcher::processSubprograms(DICompileUnit &DICU)
 {
-  if (DICU.getVersion() > LLVMDebugVersion10) {
-    DIArray SPs = DICU.getSubprograms();
-    for (unsigned i = 0, e = SPs.getNumElements(); i != e; i++) {
-      DISubprogram DISP(SPs.getElement(i));
-      DISPCopy Copy(DISP);
+  for (Function& F: module) {
+    DISubprogram* SP = F.getSubprogram();
+    if (SP && SP->getUnit() == &DICU) {
+      DISPCopy Copy(*SP, &F);
       if (Copy.name.empty() || Copy.filename.empty() || Copy.linenumber == 0)
         continue;
       Copy.lastline = ScopeInfoFinder::getLastLine(Copy.function);
@@ -245,18 +243,20 @@ void Matcher::processSubprograms(Module &M)
   MySPs.clear();
   if (NamedMDNode *CU_Nodes = M.getNamedMetadata("llvm.dbg.cu"))
     for (unsigned i = 0, e = CU_Nodes->getNumOperands(); i != e; ++i) {
-      DICompileUnit DICU(CU_Nodes->getOperand(i));
+      auto *DICU = dyn_cast<DICompileUnit>(CU_Nodes->getOperand(i));
+      if (!DICU) continue;
       if (LOCAL_DEBUG)
-        errs() << "CU: " << DICU.getDirectory() << "/" << DICU.getFilename() << "\n";
-      processSubprograms(DICU);
+        errs() << "CU: " << DICU->getDirectory() << "/" << DICU->getFilename() << "\n";
+      processSubprograms(*DICU);
     }
 
   if (NamedMDNode *NMD = M.getNamedMetadata("llvm.dbg.sp"))
     for (unsigned i = 0, e = NMD->getNumOperands(); i != e; ++i) {
-      DISubprogram DIS(NMD->getOperand(i));
+      auto *DIS = dyn_cast<DISubprogram>(NMD->getOperand(i));
+      if (!DIS) continue;
       errs() << "From SP!! \n";
       if (LOCAL_DEBUG)
-        errs() << "DIS: " << DIS.getName() << ", " << DIS.getDisplayName() << "\n";
+        errs() << "DIS: " << DIS->getName() << ", " << DIS->getName().str() << "\n";
     }
 
   /** Sort based on file name, directory and line number **/
@@ -305,10 +305,10 @@ Matcher::cu_iterator Matcher::matchCompileUnit(StringRef fullname)
   while(I != E) {
     std::string debugname;
     // Filename may already contains the path information
-    if (I->getFilename().size() > 0 && I->getFilename()[0] == '/')
-      debugname = I->getFilename();
+    if ((*I)->getFilename().size() > 0 && (*I)->getFilename()[0] == '/')
+      debugname = (*I)->getFilename();
     else
-      debugname = I->getDirectory().str() + "/" + I->getFilename().str();
+      debugname = (*I)->getDirectory().str() + "/" + (*I)->getFilename().str();
     if (pathneq(debugname.c_str(), patchname, debugstrips))
       break;
     I++;
@@ -358,7 +358,7 @@ Matcher::sp_iterator Matcher::resetTarget(StringRef target)
     if (ci == cu_end())
       return sp_end();
     MySPs.clear();
-    processSubprograms(*ci);
+    processSubprograms(**ci);
     std::sort(MySPs.begin(), MySPs.end(), cmpDISPCopy);
     if (LOCAL_DEBUG) 
       dumpSPs();
@@ -684,7 +684,7 @@ void Matcher::succTraversal(Function *F)
   for (Function::iterator FI = F->begin(), FE = F->end(); FI != FE; FI++) {
     /** Traverse the successors **/
     errs() << FI->getName() << "\n";
-    for (succ_iterator SI = succ_begin(FI), SE = succ_end(FI); SI != SE; SI++) {
+    for (succ_iterator SI = succ_begin(&*FI), SE = succ_end(&*FI); SI != SE; SI++) {
       errs() << "\t" << (*SI)->getName() << "\n";
     }
   }
@@ -694,14 +694,15 @@ void Matcher::succTraversal(Function *F)
 void Matcher::preTraversal(Function *F)
 {
   std::deque< Pair<Function::iterator, unsigned> > ques;
-  BasicBlock *BB = F->begin();
+  BasicBlock *BB = &*F->begin();
   SmallPtrSet<const BasicBlock *, 8> Visited;
   Visited.insert(BB);
-  ques.push_back(Pair<Function::iterator, unsigned>(BB, 1));
+  ques.push_back(Pair<Function::iterator, unsigned>(F->begin(), 1));
   /** BFS traversal **/
   while(!ques.empty()) {
     Pair<Function::iterator, unsigned> pair = ques.front();
-    BB = pair.first;
+    Function::iterator FI = pair.first;
+    BB = &*FI;
     ques.pop_front();
     if (BB) {
       errs() << "[" << pair.second << "] " << BB->getName();
@@ -709,10 +710,10 @@ void Matcher::preTraversal(Function *F)
       if (ScopeInfoFinder::getBlockScope(scope, BB)) {
         errs() << ": " << scope << "\n";
       }
-      for (succ_iterator SI = succ_begin(pair.first), SE = succ_end(pair.first); SI != SE; SI++) {
+      for (succ_iterator SI = succ_begin(BB), SE = succ_end(BB); SI != SE; SI++) {
         BB = *SI;
         if (!Visited.count(BB)) {
-          ques.push_back(Pair<Function::iterator, unsigned>(BB, pair.second + 1));
+          ques.push_back(Pair<Function::iterator, unsigned>(BB->getIterator(), pair.second + 1));
           Visited.insert(BB);
         }
       }
diff --git a/lib/parser/PatchCompiler/Makefile b/lib/parser/PatchCompiler/Makefile
index 9ddc5be..eac5421 100644
--- a/lib/parser/PatchCompiler/Makefile
+++ b/lib/parser/PatchCompiler/Makefile
@@ -9,6 +9,9 @@ TOOLNAME = patch-c
 
 USEDLIBS = gnulib.a
 
+LDFLAGS += -L$(LEVEL)/third-party/patch
+LIBS    += -lgnulib
+
 #
 # Include Makefile.common so we know what to do.
 #
diff --git a/lib/parser/PatchCompiler/handy.c b/lib/parser/PatchCompiler/handy.c
index 1e1f01b..134800c 100644
--- a/lib/parser/PatchCompiler/handy.c
+++ b/lib/parser/PatchCompiler/handy.c
@@ -1,4 +1,4 @@
-#define XTERN extern
+#define XTERN
 #include "parser/common.h"
 #undef XTERN
 #include "parser/handy.h"
diff --git a/lib/parser/PatchCompiler/main.cc b/lib/parser/PatchCompiler/main.cc
index a3f5e41..2470c25 100644
--- a/lib/parser/PatchCompiler/main.cc
+++ b/lib/parser/PatchCompiler/main.cc
@@ -1,6 +1,6 @@
 /* @(#) main.cc -- the entry point of parser */
 
-#define XTERN
+#define XTERN extern
 #include "parser/common.h"
 #undef XTERN
 #include "parser/parser.h"
diff --git a/test/cases/ptest.diff.id b/test/cases/ptest.diff.id
index 5e3d84b..f2aad7c 100644
--- a/test/cases/ptest.diff.id
+++ b/test/cases/ptest.diff.id
@@ -1,5 +1,5 @@
 ****
-ptest.diff
+./ptest.diff
 ====
 ptest.new.c
 ####2,2
diff --git a/test/driver/CommonsDriver/Makefile b/test/driver/CommonsDriver/Makefile
index e52f451..df87dd7 100644
--- a/test/driver/CommonsDriver/Makefile
+++ b/test/driver/CommonsDriver/Makefile
@@ -13,6 +13,10 @@ TOOLNAME=commonsdriver
 
 USEDLIBS=commons.a
 
+LDFLAGS += -L../../../lib/commons \
+
+LIBS    += -lcommons \
+
 LINK_COMPONENTS = all
 
 include $(LEVEL)/Makefile.common
diff --git a/test/driver/CommonsDriver/TestCommons.cpp b/test/driver/CommonsDriver/TestCommons.cpp
index 9f0bc26..85a9696 100644
--- a/test/driver/CommonsDriver/TestCommons.cpp
+++ b/test/driver/CommonsDriver/TestCommons.cpp
@@ -31,8 +31,8 @@
 #include <map>
 #include <string>
 
-#include "llvm/LLVMContext.h"
-#include "llvm/Module.h"
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/Module.h"
 
 #include "commons/handy.h"
 #include "commons/CallSiteFinder.h"
diff --git a/test/driver/CostModelDriver/Makefile b/test/driver/CostModelDriver/Makefile
index c434eb2..fcf8ca7 100644
--- a/test/driver/CostModelDriver/Makefile
+++ b/test/driver/CostModelDriver/Makefile
@@ -15,6 +15,10 @@ TOOLNAME=costdriver
 # LOADABLE_MODULE = 1
 USEDLIBS = costmodel.a
 
+LDFLAGS += -L../../../lib/analyzer/CostModel
+
+LIBS    += -lcostmodel
+
 # LLVMLIBS = LLVMSupport.a
 LINK_COMPONENTS = all
 
diff --git a/test/driver/CostModelDriver/TestCostModel.cpp b/test/driver/CostModelDriver/TestCostModel.cpp
index 8188431..ac0fd39 100644
--- a/test/driver/CostModelDriver/TestCostModel.cpp
+++ b/test/driver/CostModelDriver/TestCostModel.cpp
@@ -29,28 +29,34 @@
 
 #include <string>
 
-#include "llvm/LLVMContext.h"
+#include "llvm/IR/LLVMContext.h"
 #include "llvm/Pass.h"
-#include "llvm/PassManager.h"
+#include "llvm/IR/LegacyPassManager.h"
+
+#include "llvm/Analysis/TargetLibraryInfo.h" // [新增]
+#include "llvm/Support/TargetSelect.h"
 
 #include "llvm/ADT/DepthFirstIterator.h"
 
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetLowering.h"
-#include "llvm/Target/TargetData.h"
+#include "llvm/CodeGen/TargetLowering.h"
+#include "llvm/IR/DataLayout.h"
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetSubtargetInfo.h"
-#include "llvm/Target/TargetLowering.h"
+#include "llvm/CodeGen/TargetSubtargetInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
+#include "llvm/Analysis/TargetTransformInfo.h"
 
-#include "llvm/Support/CFG.h"
+#include "llvm/IR/CFG.h"
 #include "llvm/Support/Host.h"
-#include "llvm/Support/InstIterator.h"
-#include "llvm/Support/IRReader.h"
+#include "llvm/IR/InstIterator.h"
+#include "llvm/IRReader/IRReader.h"
 #include "llvm/Support/ManagedStatic.h"
 #include "llvm/Support/TargetSelect.h"
 #include "llvm/Support/TargetRegistry.h"
 #include "llvm/Support/raw_ostream.h"
 
+#include "llvm/Support/SourceMgr.h"
+
 #include "analyzer/X86CostModel.h"
 
 using namespace llvm;
@@ -67,13 +73,13 @@ struct CostModelDriver : public FunctionPass {
 
   virtual bool runOnFunction(Function &F) {
     assert(XCM && "Cost model cannot be NULL");
-    errs() << F.getNameStr() << "\n";
+    errs() << F.getName().str() << "\n";
     unsigned cost = XCM->getFunctionCost(&F);
     errs() << "Cost: " << cost << "\n";
     return false;
   }
 
-  virtual const char *getPassName() const { return PassName.c_str(); }
+  virtual StringRef getPassName() const { return PassName.c_str(); }
 
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.setPreservesAll();
@@ -113,7 +119,9 @@ int main(int argc, char **argv)
     exit(1);
   }
   // Create TargetMachine
-  TargetMachine* TM = T->createTargetMachine(TripleStr, CPUStr, FeatureStr);
+  TargetOptions Options;
+  // 需要填充剩余参数：Options, RelocModel, CodeModel, CodeGenOptLevel, JIT
+  TargetMachine* TM = T->createTargetMachine(TripleStr, CPUStr, FeatureStr, Options, None, None, CodeGenOpt::Default, false);
   if(TM == NULL) {
     errs() << "Cannot create target machine\n";
     exit(1);
@@ -122,23 +130,26 @@ int main(int argc, char **argv)
   XCM = new X86CostModel(TM);
 
   llvm_shutdown_obj Y;  // Call llvm_shutdown() on exit.
-  LLVMContext &Context = getGlobalContext();
+  LLVMContext Context;
 
   // Load the input module...
   const std::string input(argv[1]);
   SMDiagnostic diag;
-  Module * M = ParseIRFile(input, diag, Context);
+  Module * M = parseIRFile(input, diag, Context).release();
   if (M == NULL) {
     errs() << "Invalid module format\n";
     exit(1);
   }
+  M->setDataLayout(TM->createDataLayout());
 
   // Build up all of the passes that we want to do to the module.
-  PassManager PM;
+  legacy::PassManager PM;
 
   // Add the target data from the target machine, if it exists, or the module.
-  if (const TargetData *TD = TM->getTargetData())
-    PM.add(new TargetData(*TD));
+  Triple TargetTriple(TripleStr);
+  TargetLibraryInfoImpl TLII(TargetTriple);
+  PM.add(new TargetLibraryInfoWrapperPass(TLII));
+  PM.add(createTargetTransformInfoWrapperPass(TM->getTargetIRAnalysis()));
   PM.add(new CostModelDriver()); 
   PM.run(*M);
   return 0;
diff --git a/test/driver/DecoderDriver/Makefile b/test/driver/DecoderDriver/Makefile
index 7db7955..cfc9847 100644
--- a/test/driver/DecoderDriver/Makefile
+++ b/test/driver/DecoderDriver/Makefile
@@ -15,6 +15,14 @@ USEDLIBS=parser.a commons.a mappercore.a
 
 #LLVMLIBS=LLVMSupport.a LLVMCore.a LLVMSupport.a LLVMCore.a LLVMBitReader.a LLVMAsmParser.a LLVMAnalysis.a LLVMTransformUtils.a LLVMScalarOpts.a LLVMTarget.a
 
+LDFLAGS += -L../../../lib/mapper \
+           -L../../../lib/commons \
+		   -L../../../lib/parser/PatchIRDecoder
+
+LIBS    += -lparser \
+		   -lmappercore \
+		   -lcommons
+
 LINK_COMPONENTS = all
 
 include $(LEVEL)/Makefile.common
diff --git a/test/driver/MapperDriver/Makefile b/test/driver/MapperDriver/Makefile
index 278bc82..5fe06bb 100644
--- a/test/driver/MapperDriver/Makefile
+++ b/test/driver/MapperDriver/Makefile
@@ -15,6 +15,14 @@ USEDLIBS=commons.a parser.a mappercore.a
 
 #LLVMLIBS=LLVMSupport.a LLVMCore.a LLVMSupport.a LLVMCore.a LLVMBitReader.a LLVMAsmParser.a LLVMAnalysis.a LLVMTransformUtils.a LLVMScalarOpts.a LLVMTarget.a
 
+LDFLAGS += -L../../../lib/mapper \
+           -L../../../lib/commons \
+		   -L../../../lib/parser/PatchIRDecoder
+
+LIBS    += -lparser \
+		   -lmappercore \
+		   -lcommons
+
 LINK_COMPONENTS = all
 
 include $(LEVEL)/Makefile.common
diff --git a/test/driver/MapperDriver/TestMapper.cpp b/test/driver/MapperDriver/TestMapper.cpp
index 0604f90..dd171b4 100644
--- a/test/driver/MapperDriver/TestMapper.cpp
+++ b/test/driver/MapperDriver/TestMapper.cpp
@@ -7,19 +7,20 @@
 #include <unistd.h>
 #include <vector>
 #include <list>
+#include <memory>
 
-#include "llvm/LLVMContext.h"
-#include "llvm/Module.h"
-#include "llvm/PassManager.h"
-#include "llvm/Type.h"
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/LegacyPassManager.h"
+#include "llvm/IR/Type.h"
 #include "llvm/ADT/StringRef.h"
 #include "llvm/ADT/DenseMap.h"
 #include "llvm/Support/SourceMgr.h"
-#include "llvm/Support/IRReader.h"
+#include "llvm/IRReader/IRReader.h"
 #include "llvm/Support/ManagedStatic.h"
-#include "llvm/Support/CallSite.h"
-#include "llvm/Support/InstVisitor.h"
-#include "llvm/Target/TargetData.h"
+#include "llvm/IR/CallSite.h"
+#include "llvm/IR/InstVisitor.h"
+#include "llvm/IR/DataLayout.h"
 
 #include "commons/handy.h"
 #include "commons/CallSiteFinder.h"
@@ -55,7 +56,7 @@ struct LoopInfoPrinter : public FunctionPass {
   }
 
   virtual bool runOnFunction(Function &F) {
-    LoopInfo &li = getAnalysis<LoopInfo>();
+    LoopInfo &li = getAnalysis<LoopInfoWrapperPass>().getLoopInfo();
     funcLoops.clear();
     Scope ls;
     for (LoopInfo::iterator LII = li.begin(),  LIE = li.end(); LII != LIE; LII++) {
@@ -66,12 +67,12 @@ struct LoopInfoPrinter : public FunctionPass {
     return false;
   }
 
-  virtual const char *getPassName() const { return PassName.c_str(); }
+  virtual StringRef getPassName() const { return PassName.c_str(); }
 
   virtual void getAnalysisUsage(AnalysisUsage &AU) const {
     AU.setPreservesAll();
     //AU.addRequired<DominatorTree>();
-    AU.addRequired<LoopInfo>();
+    AU.addRequired<LoopInfoWrapperPass>();
   }
 };
 
@@ -97,7 +98,7 @@ static char objname[MAX_PATH];
 /// and null is returned.
 static Module *ReadModule(LLVMContext &Context, StringRef Name) {
   SMDiagnostic Diag;
-  Module *M = ParseIRFile(Name, Diag, Context);
+  Module *M = parseIRFile(Name, Diag, Context).release();
   if (!M)
     cerr << "IR file parsing failed" << endl;
   return M;
@@ -170,24 +171,24 @@ size_t count_strips(Module & M)
     size_t buf_len = 128;
     char *buf = (char *) malloc(buf_len);
     for (unsigned i = 0, e = CU_Nodes->getNumOperands(); i != e; ++i) {
-      DICompileUnit CU1(CU_Nodes->getOperand(i));
+      auto *CU1 = cast<DICompileUnit>(CU_Nodes->getOperand(i));
       if (i == 0) {
         if (i + 1 != e) {
           i++;
-          DICompileUnit CU2(CU_Nodes->getOperand(i));
-          buf = common_prefix(buf, buf_len, CU1.getDirectory().data(), CU2.getDirectory().data());
+          auto *CU2 = cast<DICompileUnit>(CU_Nodes->getOperand(i));
+          buf = common_prefix(buf, buf_len, CU1->getDirectory().data(), CU2->getDirectory().data());
         }
         else {
-          size_t cu_len = CU1.getDirectory().size();
+          size_t cu_len = CU1->getDirectory().size();
           if (cu_len > buf_len) {
             buf = (char *) realloc(buf, cu_len);
             buf_len = cu_len;
           }
-          strcpy(buf, CU1.getDirectory().data()); 
+          strcpy(buf, CU1->getDirectory().data()); 
         }
       }
       else {
-        buf = common_prefix(buf, buf_len, buf, CU1.getDirectory().data());
+        buf = common_prefix(buf, buf_len, buf, CU1->getDirectory().data());
       }
     }
     unsigned cnt = countnchr(buf, -1, '/');
@@ -217,14 +218,14 @@ void test_Mapper(char *input)
   //
   //PassManager Passes;
 
-  OwningPtr<FunctionPassManager> FPasses;
+  std::unique_ptr<legacy::FunctionPassManager> FPasses;
   // Initialize passes
   PassRegistry &Registry = *PassRegistry::getPassRegistry();
   initializeCore(Registry);
   initializeScalarOpts(Registry);
   initializeIPO(Registry);
   initializeAnalysis(Registry);
-  initializeIPA(Registry);
+  //initializeIPA(Registry);
   initializeTransformUtils(Registry);
   initializeInstCombine(Registry);
   initializeInstrumentation(Registry);
@@ -253,7 +254,7 @@ void test_Mapper(char *input)
         }
         else {
           found = true;
-          FPasses.reset(new FunctionPassManager(module));
+          FPasses.reset(new legacy::FunctionPassManager(module));
           FPasses->add(new LoopInfoPrinter());
           FPasses->doInitialization();
           Function *func = NULL;
@@ -419,7 +420,7 @@ int main(int argc, char *argv[])
     exit(1);
   }
   int i;
-  LLVMContext & Context = getGlobalContext();
+  LLVMContext Context;
   for (i = optind ; i < argc; i++) {
     Module *module = ReadModule(Context, argv[i]);
     if (module == NULL)  {
diff --git a/test/driver/MatcherDriver/Makefile b/test/driver/MatcherDriver/Makefile
index 0ad7ded..82a9914 100644
--- a/test/driver/MatcherDriver/Makefile
+++ b/test/driver/MatcherDriver/Makefile
@@ -5,5 +5,14 @@ LIBRARYNAME = LLVMMatchTest
 LOADABLE_MODULE = 1
 USEDLIBS = commons.a parser.a mappercore.a
 
+LDFLAGS += -L../../../lib/mapper \
+           -L../../../lib/commons \
+		   -L../../../lib/parser/PatchIRDecoder
+
+LIBS    += -lparser \
+		   -lmappercore \
+		   -lcommons
+
+
 include $(LEVEL)/Makefile.common
 
diff --git a/test/driver/MatcherDriver/TestMatcher.cpp b/test/driver/MatcherDriver/TestMatcher.cpp
index f03c638..7f56c1c 100644
--- a/test/driver/MatcherDriver/TestMatcher.cpp
+++ b/test/driver/MatcherDriver/TestMatcher.cpp
@@ -18,22 +18,22 @@
 #include <deque>
 
 #include "llvm/Pass.h"
-#include "llvm/Function.h"
+#include "llvm/IR/Function.h"
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/ADT/Statistic.h"
 
-#include "llvm/Analysis/DebugInfo.h"
+#include "llvm/IR/DebugInfo.h"
 #include "llvm/Analysis/LoopInfo.h"
-#include "llvm/Analysis/Dominators.h"
-#include "llvm/Metadata.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Dominators.h"
+#include "llvm/IR/Metadata.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/Support/CommandLine.h"
-#include "llvm/Support/Dwarf.h"
-#include "llvm/Support/CFG.h"
-#include "llvm/Constants.h"
-#include "llvm/Instruction.h"
-#include "llvm/InstrTypes.h"
+#include "llvm/BinaryFormat/Dwarf.h"
+#include "llvm/IR/CFG.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Instruction.h"
+#include "llvm/IR/InstrTypes.h"
 
 #include "commons/handy.h"
 #include "parser/PatchDecoder.h"
diff --git a/test/driver/RiskEvalDriver/Makefile b/test/driver/RiskEvalDriver/Makefile
index 5c46f55..2431629 100644
--- a/test/driver/RiskEvalDriver/Makefile
+++ b/test/driver/RiskEvalDriver/Makefile
@@ -15,6 +15,36 @@ TOOLNAME=riskevaldriver
 # LOADABLE_MODULE = 1
 USEDLIBS = riskeval.a costmodel.a llvmslicer.a callgraph.a mods.a points.a dependence.a language.a mappercore.a slicer.a dependence.a commons.a 
 
+LDFLAGS += -L../../../lib/slicer \
+           -L../../../lib/mapper \
+		   -L../../../lib/analyzer/CostModel \
+           -L../../../lib/analyzer/RiskEvaluator \
+           -L../../../lib/commons \
+		   -L../../../lib/dependence \
+		   -L../../../lib/graph \
+		   -L../../../lib/parser/PatchIRDecoder \
+		   -L../../../third-party/LLVMSlicer/Callgraph \
+		   -L../../../third-party/LLVMSlicer/Slicing \
+		   -L../../../third-party/LLVMSlicer/Modifies \
+		   -L../../../third-party/LLVMSlicer/PointsTo \
+		   -L../../../third-party/LLVMSlicer/Languages \
+		   -L../../../third-party/patch
+
+LIBS    += -lriskeval \
+		   -lcostmodel \
+		   -lllvmslicer \
+		   -lcallgraph \
+		   -lmods \
+		   -lpoints \
+		   -ldependence \
+		   -lmappercore \
+		   -lcommons \
+		   -lgraph \
+		   -llanguage \
+		   -lslicer \
+		   -lgnulib \
+
+
 # LLVMLIBS = LLVMSupport.a
 LINK_COMPONENTS = all
 
diff --git a/test/driver/RiskEvalDriver/TestRiskEval.cpp b/test/driver/RiskEvalDriver/TestRiskEval.cpp
index 08cd07f..ffaeaf5 100644
--- a/test/driver/RiskEvalDriver/TestRiskEval.cpp
+++ b/test/driver/RiskEvalDriver/TestRiskEval.cpp
@@ -28,27 +28,28 @@
  */
 
 #include <string>
+#include <memory>
 
-#include "llvm/LLVMContext.h"
+#include "llvm/IR/LLVMContext.h"
 #include "llvm/Pass.h"
-#include "llvm/Module.h"
-#include "llvm/PassManager.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/LegacyPassManager.h"
 
 #include "llvm/ADT/DepthFirstIterator.h"
 
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetLowering.h"
-#include "llvm/Target/TargetData.h"
+#include "llvm/CodeGen/TargetLowering.h"
+#include "llvm/IR/DataLayout.h"
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetSubtargetInfo.h"
-#include "llvm/Target/TargetLowering.h"
+#include "llvm/CodeGen/TargetSubtargetInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
 
 #include "llvm/Transforms/Scalar.h"
 
-#include "llvm/Support/CFG.h"
+#include "llvm/IR/CFG.h"
 #include "llvm/Support/Host.h"
-#include "llvm/Support/InstIterator.h"
-#include "llvm/Support/IRReader.h"
+#include "llvm/IR/InstIterator.h"
+#include "llvm/IRReader/IRReader.h"
 #include "llvm/Support/ManagedStatic.h"
 #include "llvm/Support/TargetSelect.h"
 #include "llvm/Support/TargetRegistry.h"
@@ -58,6 +59,12 @@
 #include "analyzer/X86CostModel.h"
 #include "analyzer/Evaluator.h"
 
+// 解决 SMDiagnostic incomplete type 错误
+#include "llvm/Support/SourceMgr.h"
+
+// 解决 createPromoteMemoryToRegisterPass 未声明错误
+#include "llvm/Transforms/Utils.h"
+
 using namespace llvm;
 
 X86CostModel * XCM = NULL;
@@ -75,7 +82,7 @@ struct FooPass : public FunctionPass {
       return false;
     }
 
-    virtual const char *getPassName() const { return PassName.c_str(); }
+    virtual StringRef getPassName() const { return PassName.c_str(); }
 
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
       AU.setPreservesAll();
@@ -99,12 +106,12 @@ int main(int argc, char **argv)
   XCM = new X86CostModel(TM);
 
   llvm_shutdown_obj Y;  // Call llvm_shutdown() on exit.
-  LLVMContext &Context = getGlobalContext();
+  LLVMContext Context;
 
   // Load the input module...
   const std::string input(argv[1]);
   SMDiagnostic diag;
-  Module * M = ParseIRFile(input, diag, Context);
+  Module * M = parseIRFile(input, diag, Context).release();
   if (M == NULL) {
     errs() << "Invalid module format\n";
     exit(1);
@@ -119,8 +126,8 @@ int main(int argc, char **argv)
     exit(1);
   }
 
-  OwningPtr<FunctionPassManager> FPasses;
-  FPasses.reset(new FunctionPassManager(M));
+  std::unique_ptr<legacy::FunctionPassManager> FPasses;
+  FPasses.reset(new legacy::FunctionPassManager(M));
   FPasses->add(createPromoteMemoryToRegisterPass());
   FPasses->doInitialization();
   FPasses->run(*func);
@@ -131,7 +138,7 @@ int main(int argc, char **argv)
     Instruction * inst = &*II;
     map[func].push_back(inst);
   }
-  FPasses.reset(new FunctionPassManager(M));
+  FPasses.reset(new legacy::FunctionPassManager(M));
   FPasses->add(new RiskEvaluator(map));
   FPasses->doInitialization();
   FPasses->run(*func);
diff --git a/test/driver/ScratchDriver/TestDummy.cpp b/test/driver/ScratchDriver/TestDummy.cpp
index 6649d86..1b5cd8a 100644
--- a/test/driver/ScratchDriver/TestDummy.cpp
+++ b/test/driver/ScratchDriver/TestDummy.cpp
@@ -27,7 +27,7 @@
  *
  */
 #include "llvm/Pass.h"
-#include "llvm/DefaultPasses.h"
+//#include "llvm/DefaultPasses.h"
 #include "llvm/Analysis/LoopPass.h"
 #include "llvm/Analysis/LoopInfo.h"
 #include "llvm/Analysis/ScalarEvolution.h"
@@ -48,8 +48,8 @@ struct LoopCountPass : public FunctionPass {
 
     virtual bool runOnFunction(Function &F) {
       errs() << "Func: " << F.getName() << "\n";
-      LoopInfo & li = getAnalysis<LoopInfo>(); 
-      ScalarEvolution & se = getAnalysis<ScalarEvolution>(); 
+      LoopInfo & li = getAnalysis<LoopInfoWrapperPass>().getLoopInfo();
+      ScalarEvolution &se = getAnalysis<ScalarEvolutionWrapperPass>().getSE();
       for (LoopInfo::iterator LII = li.begin(),  LIE = li.end(); LII != LIE; ++LII) {
         Loop * loop = *LII;
         errs() << "Top level loop '" << loop->getHeader()->getName() << "', count: ";
@@ -69,12 +69,12 @@ struct LoopCountPass : public FunctionPass {
       return false;
     }
 
-    virtual const char *getPassName() const { return PassName.c_str(); }
+    virtual StringRef getPassName() const { return PassName.c_str(); }
 
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
       AU.setPreservesAll();
-      AU.addRequired<LoopInfo>();
-      AU.addRequired<ScalarEvolution>(); 
+      AU.addRequired<LoopInfoWrapperPass>();
+      AU.addRequired<ScalarEvolutionWrapperPass>();
     }
 };
 
diff --git a/test/driver/SlicerDriver/TestSlicer.cpp b/test/driver/SlicerDriver/TestSlicer.cpp
index b88d39c..ea0b6a6 100644
--- a/test/driver/SlicerDriver/TestSlicer.cpp
+++ b/test/driver/SlicerDriver/TestSlicer.cpp
@@ -28,13 +28,13 @@
  */
 
 #include "llvm/Pass.h"
-#include "llvm/Function.h"
+#include "llvm/IR/Function.h"
 #include "llvm/Support/raw_ostream.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/InstIterator.h"
 #include "llvm/ADT/Statistic.h"
 #include "llvm/ADT/SmallVector.h"
-#include "llvm/Instruction.h"
-#include "llvm/Value.h"
+#include "llvm/IR/Instruction.h"
+#include "llvm/IR/Value.h"
 
 #include "llvm/Analysis/AliasAnalysis.h"
 #include "llvm/Analysis/MemoryDependenceAnalysis.h"
@@ -85,9 +85,9 @@ namespace {
     // We don't modify the program, so we preserve all analyses
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
       AU.setPreservesAll();
-      AU.addRequired<AliasAnalysis>();
+      AU.addRequired<AAResultsWrapperPass>();
       AU.addRequired<DepGraphBuilder>();
-      AU.addRequired<MemoryDependenceAnalysis>();
+      AU.addRequired<MemoryDependenceWrapperPass>();
     }
   };
 }
diff --git a/third-party/DSA/AddressTakenAnalysis.cpp b/third-party/DSA/AddressTakenAnalysis.cpp
index c14a075..8d80300 100644
--- a/third-party/DSA/AddressTakenAnalysis.cpp
+++ b/third-party/DSA/AddressTakenAnalysis.cpp
@@ -18,15 +18,15 @@
 #include <fstream>
 #include <set>
 
-#include "llvm/Constants.h"
+#include "llvm/IR/Constants.h"
 #include "llvm/Pass.h"
-#include "llvm/Module.h"
-#include "llvm/Function.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/FormattedStream.h"
 #include "llvm/Support/Debug.h"
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/CallSite.h"
 
 #include "dsa/AddressTakenAnalysis.h"
 
@@ -37,7 +37,7 @@ AddressTakenAnalysis::~AddressTakenAnalysis() {}
 
 static bool isAddressTaken(Value* V) {
   for (Value::use_iterator I = V->use_begin(), E = V->use_end(); I != E; ++I) {
-    User *U = *I;
+    User *U = I->getUser();
     if(isa<StoreInst>(U))
       return true;
     if (!isa<CallInst>(U) && !isa<InvokeInst>(U)) {
@@ -61,7 +61,7 @@ static bool isAddressTaken(Value* V) {
       // are never used
     } else {
       llvm::CallSite CS(cast<Instruction>(U));
-      if (!CS.isCallee(I))
+      if (!CS.isCallee(&*I))
         return true;
     }
   }
@@ -70,8 +70,8 @@ static bool isAddressTaken(Value* V) {
 
 bool AddressTakenAnalysis::runOnModule(llvm::Module& M) {
   for (Module::iterator FI = M.begin(), FE = M.end(); FI != FE; ++FI){
-    if(isAddressTaken(FI)) {
-      addressTakenFunctions.insert(FI);
+    if(isAddressTaken(&*FI)) {
+      addressTakenFunctions.insert(&*FI);
     }
   }
 
diff --git a/third-party/DSA/AllocatorIdentification.cpp b/third-party/DSA/AllocatorIdentification.cpp
index 1814b29..b044fa0 100644
--- a/third-party/DSA/AllocatorIdentification.cpp
+++ b/third-party/DSA/AllocatorIdentification.cpp
@@ -12,9 +12,9 @@
 
 #define DEBUG_TYPE "allocator-identify"
 
-#include "llvm/Constants.h"
-#include "llvm/Instructions.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Pass.h"
 #include "llvm/Analysis/LoopInfo.h"
 #include "llvm/Transforms/Utils/Cloning.h"
@@ -42,7 +42,7 @@ bool AllocIdentify::flowsFrom(Value *Dest,Value *Src) {
   }
   if(PHINode *PN = dyn_cast<PHINode>(Dest)) {
     Function *F = PN->getParent()->getParent();
-    LoopInfo &LI = getAnalysis<LoopInfo>(*F);
+    LoopInfo &LI = getAnalysis<LoopInfoWrapperPass>(*F).getLoopInfo();
     // If this is a loop phi, ignore.
     if(LI.isLoopHeader(PN->getParent()))
       return false;
@@ -144,7 +144,7 @@ bool AllocIdentify::runOnModule(Module& M) {
             if(changed) {
               ++numAllocators;
               allocators.insert(WrapperF->getName());
-              DEBUG(errs() << WrapperF->getName().str() << "\n");
+              LLVM_DEBUG(errs() << WrapperF->getName().str() << "\n");
             }
           }
         }
@@ -179,7 +179,7 @@ bool AllocIdentify::runOnModule(Module& M) {
             if(changed) {
               ++numDeallocators;
               deallocators.insert(WrapperF->getName());
-              DEBUG(errs() << WrapperF->getName().str() << "\n");
+              LLVM_DEBUG(errs() << WrapperF->getName().str() << "\n");
             }
           }
         }
@@ -189,7 +189,7 @@ bool AllocIdentify::runOnModule(Module& M) {
   return false;
 }
 void AllocIdentify::getAnalysisUsage(AnalysisUsage &AU) const {
-  AU.addRequired<LoopInfo>();
+  AU.addRequired<LoopInfoWrapperPass>();
   AU.setPreservesAll();
 }
 
diff --git a/third-party/DSA/Basic.cpp b/third-party/DSA/Basic.cpp
index c0db933..195fb62 100644
--- a/third-party/DSA/Basic.cpp
+++ b/third-party/DSA/Basic.cpp
@@ -12,15 +12,14 @@
 //
 //===----------------------------------------------------------------------===//
 
-#include "llvm/Module.h"
-#include "llvm/DerivedTypes.h"
-#include "llvm/Constants.h"
-#include "llvm/Instructions.h"
-#include "llvm/Intrinsics.h"
-#include "llvm/Support/InstIterator.h"
-#include "llvm/Support/InstVisitor.h"
-#include "llvm/Support/GetElementPtrTypeIterator.h"
-#include "llvm/Support/TypeBuilder.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/DerivedTypes.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Intrinsics.h"
+#include "llvm/IR/InstIterator.h"
+#include "llvm/IR/InstVisitor.h"
+#include "llvm/IR/GetElementPtrTypeIterator.h"
 
 #include "dsa/DataStructure.h"
 #include "dsa/DSGraph.h"
@@ -33,7 +32,7 @@ X("dsa-basic", "Basic Data Structure Analysis(No Analysis)");
 char BasicDataStructures::ID = 0;
 
 bool BasicDataStructures::runOnModule(Module &M) {
-  init(&getAnalysis<TargetData>());
+  init(const_cast<llvm::DataLayout*>(&M.getDataLayout()));
 
   //
   // Create a void pointer type.  This is simply a pointer to an 8 bit value.
@@ -62,7 +61,7 @@ bool BasicDataStructures::runOnModule(Module &M) {
 
   for (Module::iterator F = M.begin(), E = M.end(); F != E; ++F) {
     if (!F->isDeclaration()) {
-      DSGraph* G = new DSGraph(GlobalECs, getTargetData(), *TypeSS, GlobalsGraph);
+      DSGraph* G = new DSGraph(GlobalECs, getDataLayout(), *TypeSS, GlobalsGraph);
       DSNode * Node = new DSNode(G);
           
       if (!F->hasInternalLinkage())
@@ -76,7 +75,7 @@ bool BasicDataStructures::runOnModule(Module &M) {
         }
       }
 
-      for (inst_iterator I = inst_begin(F), E = inst_end(F); I != E; ++I) {
+      for (inst_iterator I = inst_begin(&*F), E = inst_end(&*F); I != E; ++I) {
         G->getNodeForValue(&*I).mergeWith(Node);
       }
 
diff --git a/third-party/DSA/BottomUpClosure.cpp b/third-party/DSA/BottomUpClosure.cpp
index 9a2e747..90523e5 100644
--- a/third-party/DSA/BottomUpClosure.cpp
+++ b/third-party/DSA/BottomUpClosure.cpp
@@ -15,8 +15,8 @@
 //===----------------------------------------------------------------------===//
 
 #define DEBUG_TYPE "dsa-bu"
-#include "llvm/Constants.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Module.h"
 #include "llvm/ADT/Statistic.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/FormattedStream.h"
@@ -66,7 +66,7 @@ bool BUDataStructures::runOnModuleInternal(Module& M) {
   //
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())){
-      getOrCreateGraph(F);
+      getOrCreateGraph(&*F);
     }
   }
 
@@ -102,7 +102,7 @@ bool BUDataStructures::runOnModuleInternal(Module& M) {
   //
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())){
-      DSGraph *Graph  = getOrCreateGraph(F);
+      DSGraph *Graph  = getOrCreateGraph(&*F);
       cloneGlobalsInto(Graph, DSGraph::DontCloneCallNodes |
                         DSGraph::DontCloneAuxCallNodes);
       Graph->buildCallGraph(callgraph, GlobalFunctionList, filterCallees);
@@ -117,7 +117,7 @@ bool BUDataStructures::runOnModuleInternal(Module& M) {
   // Once the correct flags have been calculated. Update the callgraph.
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())){
-      DSGraph *Graph = getOrCreateGraph(F);
+      DSGraph *Graph = getOrCreateGraph(&*F);
       Graph->buildCompleteCallGraph(callgraph,
                                     GlobalFunctionList, filterCallees);
     }
@@ -308,11 +308,11 @@ BUDataStructures::postOrderInline (Module & M) {
   // Calculate the graphs for any functions that are unreachable from main...
   //
   for (Module::iterator I = M.begin(), E = M.end(); I != E; ++I)
-    if (!I->isDeclaration() && !ValMap.count(I)) {
+    if (!I->isDeclaration() && !ValMap.count(&*I)) {
       if (MainFunc)
-        DEBUG(errs() << debugname << ": Function unreachable from main: "
+        LLVM_DEBUG(errs() << debugname << ": Function unreachable from main: "
         << I->getName() << "\n");
-      calculateGraphs(I, Stack, NextID, ValMap);     // Calculate all graphs.
+      calculateGraphs(&*I, Stack, NextID, ValMap);     // Calculate all graphs.
       CloneAuxIntoGlobal(getDSGraph(*I));
 
       // Mark this graph as processed.  Do this by finding all functions
@@ -434,13 +434,13 @@ BUDataStructures::calculateGraphs (const Function *F,
   // If this is a new SCC, process it now.
   //
   if (Stack.back() == F) {           // Special case the single "SCC" case here.
-    DEBUG(errs() << "Visiting single node SCC #: " << MyID << " fn: "
+    LLVM_DEBUG(errs() << "Visiting single node SCC #: " << MyID << " fn: "
 	  << F->getName() << "\n");
     Stack.pop_back();
-    DEBUG(errs() << "  [BU] Calculating graph for: " << F->getName()<< "\n");
+    LLVM_DEBUG(errs() << "  [BU] Calculating graph for: " << F->getName()<< "\n");
     DSGraph* G = getOrCreateGraph(F);
     calculateGraph(G);
-    DEBUG(errs() << "  [BU] Done inlining: " << F->getName() << " ["
+    LLVM_DEBUG(errs() << "  [BU] Done inlining: " << F->getName() << " ["
 	  << G->getGraphSize() << "+" << G->getAuxFunctionCalls().size()
 	  << "]\n");
 
@@ -453,7 +453,7 @@ BUDataStructures::calculateGraphs (const Function *F,
     getAllAuxCallees(G, NewCallees);
     if (!NewCallees.empty()) {
       if (hasNewCallees(NewCallees, CalleeFunctions)) {
-        DEBUG(errs() << "Recalculating " << F->getName() << " due to new knowledge\n");
+        LLVM_DEBUG(errs() << "Recalculating " << F->getName() << " due to new knowledge\n");
         ValMap.erase(F);
         ++NumRecalculations;
         return calculateGraphs(F, Stack, NextID, ValMap);
@@ -495,7 +495,7 @@ BUDataStructures::calculateGraphs (const Function *F,
     }
     Stack.pop_back();
 
-    DEBUG(errs() << "Calculating graph for SCC #: " << MyID << " of size: "
+    LLVM_DEBUG(errs() << "Calculating graph for SCC #: " << MyID << " of size: "
 	  << SCCSize << "\n");
 
     // Compute the Max SCC Size.
@@ -508,14 +508,14 @@ BUDataStructures::calculateGraphs (const Function *F,
     // Now that we have one big happy family, resolve all of the call sites in
     // the graph...
     calculateGraph(SCCGraph);
-    DEBUG(errs() << "  [BU] Done inlining SCC  [" << SCCGraph->getGraphSize()
+    LLVM_DEBUG(errs() << "  [BU] Done inlining SCC  [" << SCCGraph->getGraphSize()
 	  << "+" << SCCGraph->getAuxFunctionCalls().size() << "]\n"
 	  << "DONE with SCC #: " << MyID << "\n");
     FuncSet NewCallees;
     getAllAuxCallees(SCCGraph, NewCallees);
     if (!NewCallees.empty()) {
       if (hasNewCallees(NewCallees, CalleeFunctions)) {
-        DEBUG(errs() << "Recalculating SCC Graph " << F->getName() << " due to new knowledge\n");
+        LLVM_DEBUG(errs() << "Recalculating SCC Graph " << F->getName() << " due to new knowledge\n");
         ValMap.erase(F);
         ++NumRecalculations;
         return calculateGraphs(F, Stack, NextID, ValMap);
@@ -624,7 +624,7 @@ void BUDataStructures::CloneAuxIntoGlobal(DSGraph* G) {
 //  dealt with
 //
 void BUDataStructures::calculateGraph(DSGraph* Graph) {
-  DEBUG(Graph->AssertGraphOK(); Graph->getGlobalsGraph()->AssertGraphOK());
+  LLVM_DEBUG(Graph->AssertGraphOK(); Graph->getGlobalsGraph()->AssertGraphOK());
   Graph->buildCallGraph(callgraph, GlobalFunctionList, filterCallees);
 
   // Move our call site list into TempFCs so that inline call sites go into the
@@ -635,7 +635,7 @@ void BUDataStructures::calculateGraph(DSGraph* Graph) {
 
   for(DSGraph::FunctionListTy::iterator I = TempFCs.begin(), E = TempFCs.end();
       I != E; ++I) {
-    DEBUG(Graph->AssertGraphOK(); Graph->getGlobalsGraph()->AssertGraphOK());
+    LLVM_DEBUG(Graph->AssertGraphOK(); Graph->getGlobalsGraph()->AssertGraphOK());
 
     DSCallSite &CS = *I;
 
@@ -684,8 +684,8 @@ void BUDataStructures::calculateGraph(DSGraph* Graph) {
       // Get the data structure graph for the called function.
 
       GI = getDSGraph(*Callee);  // Graph to inline
-      DEBUG(GI->AssertGraphOK(); GI->getGlobalsGraph()->AssertGraphOK());
-      DEBUG(errs() << "    Inlining graph for " << Callee->getName()
+      LLVM_DEBUG(GI->AssertGraphOK(); GI->getGlobalsGraph()->AssertGraphOK());
+      LLVM_DEBUG(errs() << "    Inlining graph for " << Callee->getName()
 	    << "[" << GI->getGraphSize() << "+"
 	    << GI->getAuxFunctionCalls().size() << "] into '"
 	    << Graph->getFunctionNames() << "' [" << Graph->getGraphSize() <<"+"
@@ -703,7 +703,7 @@ void BUDataStructures::calculateGraph(DSGraph* Graph) {
       Graph->mergeInGraph(CS, *Callee, *GI,
                           DSGraph::StripAllocaBit|DSGraph::DontCloneCallNodes);
       ++NumInlines;
-      DEBUG(Graph->AssertGraphOK(););
+      LLVM_DEBUG(Graph->AssertGraphOK(););
     }
   }
   TempFCs.clear();
diff --git a/third-party/DSA/CallTargets.cpp b/third-party/DSA/CallTargets.cpp
index e85868b..df13ae0 100644
--- a/third-party/DSA/CallTargets.cpp
+++ b/third-party/DSA/CallTargets.cpp
@@ -17,14 +17,16 @@
 //
 //===----------------------------------------------------------------------===//
 
+#define DEBUG_TYPE "call-targets"
+
 #include <ostream>
 
-#include "llvm/Module.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/ADT/Statistic.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/FormattedStream.h"
-#include "llvm/Constants.h"
+#include "llvm/IR/Constants.h"
 
 #include "dsa/DataStructure.h"
 #include "dsa/DSGraph.h"
@@ -57,7 +59,7 @@ void CallTargetFinder<dsa>::findIndTargets(Module &M)
       for (Function::iterator F = I->begin(), FE = I->end(); F != FE; ++F)
         for (BasicBlock::iterator B = F->begin(), BE = F->end(); B != BE; ++B)
           if (isa<CallInst>(B) || isa<InvokeInst>(B)) {
-            CallSite cs(B);
+            CallSite cs(&*B);
             AllSites.push_back(cs);
             Function* CF = cs.getCalledFunction();
 
@@ -113,7 +115,7 @@ void CallTargetFinder<dsa>::findIndTargets(Module &M)
                 } 
                 if (!N->isIncompleteNode() && !N->isExternalNode() && !IndMap[cs].size()) {
                   ++CompleteEmpty;
-                  DEBUG(errs() << "Call site empty: '"
+                  LLVM_DEBUG(errs() << "Call site empty: '"
                                 << cs.getInstruction()->getName()
                                 << "' In '"
                                 << cs.getInstruction()->getParent()->getParent()->getName()
@@ -143,7 +145,8 @@ void CallTargetFinder<dsa>::print(llvm::raw_ostream &O, const Module *M) const
       if (!isComplete(ii->first)) {
         O << "* ";
         CallSite cs = ii->first;
-        cs.getInstruction()->dump();
+        cs.getInstruction()->print(llvm::errs());
+        llvm::errs() << "\n";;
         O << cs.getInstruction()->getParent()->getParent()->getName().str() << " "
           << cs.getInstruction()->getName().str() << " ";
       }
diff --git a/third-party/DSA/CompleteBottomUp.cpp b/third-party/DSA/CompleteBottomUp.cpp
index a93e52b..f249f7e 100644
--- a/third-party/DSA/CompleteBottomUp.cpp
+++ b/third-party/DSA/CompleteBottomUp.cpp
@@ -14,7 +14,7 @@
 //===----------------------------------------------------------------------===//
 
 #define DEBUG_TYPE "dsa-cbu"
-#include "llvm/Module.h"
+#include "llvm/IR/Module.h"
 #include "llvm/ADT/Statistic.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/FormattedStream.h"
@@ -53,7 +53,7 @@ CompleteBUDataStructures::runOnModule (Module &M) {
 
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())){
-      getOrCreateGraph(F);
+      getOrCreateGraph(&*F);
     }
   }
 
@@ -61,7 +61,7 @@ CompleteBUDataStructures::runOnModule (Module &M) {
   formGlobalECs();
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())) {
-      if (DSGraph * Graph = getOrCreateGraph(F)) {
+      if (DSGraph * Graph = getOrCreateGraph(&*F)) {
         cloneIntoGlobals(Graph, DSGraph::DontCloneCallNodes |
                         DSGraph::DontCloneAuxCallNodes |
                         DSGraph::StripAllocaBit);
@@ -71,7 +71,7 @@ CompleteBUDataStructures::runOnModule (Module &M) {
 
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())) {
-      if (DSGraph * Graph = getOrCreateGraph(F)) {
+      if (DSGraph * Graph = getOrCreateGraph(&*F)) {
         cloneGlobalsInto(Graph, DSGraph::DontCloneCallNodes |
                         DSGraph::DontCloneAuxCallNodes);
       }
diff --git a/third-party/DSA/DSCallGraph.cpp b/third-party/DSA/DSCallGraph.cpp
index b76bff5..424c3b3 100644
--- a/third-party/DSA/DSCallGraph.cpp
+++ b/third-party/DSA/DSCallGraph.cpp
@@ -13,8 +13,8 @@
 
 #include <algorithm>
 
-#include "llvm/Function.h"
-#include "llvm/DerivedTypes.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/DerivedTypes.h"
 #include "llvm/Support/FormattedStream.h"
 #include "llvm/Support/CommandLine.h"
 
diff --git a/third-party/DSA/DSGraph.cpp b/third-party/DSA/DSGraph.cpp
index 5d74ad5..6cdf22e 100644
--- a/third-party/DSA/DSGraph.cpp
+++ b/third-party/DSA/DSGraph.cpp
@@ -11,16 +11,19 @@
 //
 //===----------------------------------------------------------------------===//
 
+#define DEBUG_TYPE "ds-graph"
+
 #include <iostream>
 #include <algorithm>
 
-#include "llvm/Constants.h"
-#include "llvm/Function.h"
-#include "llvm/GlobalVariable.h"
-#include "llvm/Instructions.h"
-#include "llvm/DerivedTypes.h"
-#include "llvm/Target/TargetData.h"
-#include "llvm/Assembly/Writer.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/GlobalVariable.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/DerivedTypes.h"
+#include "llvm/IR/DataLayout.h"
+#include "llvm/IR/AssemblyAnnotationWriter.h"
+#include "llvm/Support/raw_ostream.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/ADT/DepthFirstIterator.h"
@@ -29,8 +32,8 @@
 #include "llvm/ADT/Statistic.h"
 #include "llvm/Support/Timer.h"
 #include "llvm/Support/raw_ostream.h"
-#include "llvm/Type.h"
-#include "llvm/GlobalAlias.h"
+#include "llvm/IR/Type.h"
+#include "llvm/IR/GlobalAlias.h"
 
 #include "dsa/DSGraphTraits.h"
 #include "dsa/DataStructure.h"
@@ -208,7 +211,7 @@ void DSGraph::cloneInto( DSGraph* G, NodeMapTy &OldNodeMap, unsigned CloneFlags)
            "Forward nodes shouldn't be in node list!");
     DSNode *New = new DSNode(*I, this);
     New->maskNodeTypes(~BitsToClear);
-    OldNodeMap[I] = New;
+    OldNodeMap[&*I] = New;
   }
 
   // Rewrite the links in the new nodes to point into the current graph now.
@@ -675,7 +678,7 @@ void DSGraph::markIncompleteNodes(unsigned Flags) {
   if (Flags & DSGraph::MarkVAStart) {
     for (node_iterator i=node_begin(); i != node_end(); ++i) {
       if (i->isVAStartNode())
-        markIncompleteNode(i);
+        markIncompleteNode(&*i);
     }
   }
 }
@@ -878,7 +881,7 @@ static void removeIdenticalCalls(DSGraph::FunctionListTy &Calls) {
       DSNode *Callee = CS.getCalleeNode();
       if (Callee->getNumReferrers() == 1 && Callee->isCompleteNode() &&
           Callee->isEmptyGlobals()) {  // No useful info?
-        DEBUG(errs() << "WARNING: Useless call site found.\n");
+        LLVM_DEBUG(errs() << "WARNING: Useless call site found.\n");
         I = Calls.erase(I);
         E = Calls.end();
         ++NumDeleted;
@@ -914,7 +917,7 @@ static void removeIdenticalCalls(DSGraph::FunctionListTy &Calls) {
   NumCallNodesMerged += NumDeleted;
 
   if (NumDeleted)
-    DEBUG(errs() << "Merged " << NumDeleted << " call nodes.\n");
+    LLVM_DEBUG(errs() << "Merged " << NumDeleted << " call nodes.\n");
 }
 // removeTriviallyDeadNodes - After the graph has been constructed, this method
 // removes all unreachable nodes that are created because they got merged with
@@ -1059,7 +1062,7 @@ static bool CallSiteUsesAliveArgs(const DSCallSite &CS,
 // This function also clones information about globals back into the globals
 // graph before it deletes the nodes.
 void DSGraph::removeDeadNodes(unsigned Flags) {
-  DEBUG(AssertGraphOK(); if (GlobalsGraph) GlobalsGraph->AssertGraphOK());
+  LLVM_DEBUG(AssertGraphOK(); if (GlobalsGraph) GlobalsGraph->AssertGraphOK());
 
   // Reduce the amount of work we have to do... remove dummy nodes left over by
   // merging...
@@ -1179,7 +1182,7 @@ void DSGraph::removeDeadNodes(unsigned Flags) {
   std::vector<DSNode*> DeadNodes;
   DeadNodes.reserve(Nodes.size());
   for (NodeListTy::iterator NI = Nodes.begin(), E = Nodes.end(); NI != E;) {
-    DSNode *N = NI++;
+    DSNode *N = &*(NI++);
     assert(!N->isForwarding() && "Forwarded node in nodes list?");
 
     if (!Alive.count(N)) {
@@ -1204,7 +1207,7 @@ void DSGraph::removeDeadNodes(unsigned Flags) {
   for (unsigned i = 0, e = DeadNodes.size(); i != e; ++i)
     delete DeadNodes[i];
 
-  DEBUG(AssertGraphOK(); GlobalsGraph->AssertGraphOK());
+  LLVM_DEBUG(AssertGraphOK(); GlobalsGraph->AssertGraphOK());
 }
 
 void DSGraph::AssertNodeContainsGlobal(const DSNode *N, const GlobalValue *GV) const {
@@ -1218,7 +1221,7 @@ void DSGraph::AssertCallSiteInGraph(const DSCallSite &CS) const {
 #if 0
     if (CS.getNumPtrArgs() && CS.getCalleeNode() == CS.getPtrArg(0).getNode() &&
         CS.getCalleeNode() && CS.getCalleeNode()->getGlobals().empty())
-      DEBUG(errs() << "WARNING: WEIRD CALL SITE FOUND!\n");
+      LLVM_DEBUG(errs() << "WARNING: WEIRD CALL SITE FOUND!\n");
 #endif
   }
   AssertNodeInGraph(CS.getRetVal().getNode());
diff --git a/third-party/DSA/DSTest.cpp b/third-party/DSA/DSTest.cpp
index 8df723d..63b8611 100644
--- a/third-party/DSA/DSTest.cpp
+++ b/third-party/DSA/DSTest.cpp
@@ -36,12 +36,13 @@
 
 #define DEBUG_TYPE "dsgraph-test"
 
-#include "llvm/Module.h"
+#include "llvm/IR/Module.h"
 #include "llvm/ADT/SmallVector.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/raw_ostream.h"
-#include "llvm/ValueSymbolTable.h"
-#include "llvm/Assembly/Writer.h"
+#include "llvm/IR/ValueSymbolTable.h"
+#include "llvm/IR/AssemblyAnnotationWriter.h"
+#include "llvm/Support/raw_ostream.h"
 
 #include "dsa/DataStructure.h"
 #include "dsa/DSGraph.h"
@@ -202,7 +203,7 @@ class NodeValue {
       // Now we try to find the value...
       // FIXME: This only works for named values, things like "%1" don't work.
       // That might not be a deal breaker, but should be clear.
-      V = F->getValueSymbolTable().lookup(value);
+      V = F->getValueSymbolTable()->lookup(value);
 
       assert(V && "Unable to find value in specified function!");
 
diff --git a/third-party/DSA/DataStructure.cpp b/third-party/DSA/DataStructure.cpp
index b72e88d..f1157ee 100644
--- a/third-party/DSA/DataStructure.cpp
+++ b/third-party/DSA/DataStructure.cpp
@@ -11,16 +11,19 @@
 //
 //===----------------------------------------------------------------------===//
 
+#define DEBUG_TYPE "ds"
+
 #include <iostream>
 #include <algorithm>
 
-#include "llvm/Constants.h"
-#include "llvm/Function.h"
-#include "llvm/GlobalVariable.h"
-#include "llvm/Instructions.h"
-#include "llvm/DerivedTypes.h"
-#include "llvm/Target/TargetData.h"
-#include "llvm/Assembly/Writer.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/GlobalVariable.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/DerivedTypes.h"
+#include "llvm/IR/DataLayout.h"
+#include "llvm/IR/AssemblyAnnotationWriter.h"
+#include "llvm/Support/raw_ostream.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/ADT/DepthFirstIterator.h"
@@ -52,7 +55,7 @@ bool DSNodeHandle::isForwarding() const {
 
 DSNode *DSNodeHandle::HandleForwarding() const {
   assert(N->isForwarding() && "Can only be invoked if forwarding!");
-  DEBUG(
+  LLVM_DEBUG(
     { //assert not looping
     DSNode* NH = N;
     svset<DSNode*> seen;
@@ -335,7 +338,7 @@ void DSNode::dumpFuncs() {
 void DSNode::markIntPtrFlags() {
   // check if the types merged have both int and pointer at the same offset,
 
-  const TargetData &TD = getParentGraph()->getTargetData();
+  const DataLayout &TD = getParentGraph()->getDataLayout();
   // check all offsets for that node.
   for(unsigned offset = 0; offset < getSize() ; offset++) {
     // if that Node has no Type information, skip
@@ -415,7 +418,7 @@ void DSNode::growSizeForType(Type *NewTy, unsigned Offset) {
   if (isArrayNode() && getSize() > 0) {
     Offset %= getSize();
   }
-  const TargetData &TD = getParentGraph()->getTargetData();
+  const DataLayout &TD = getParentGraph()->getDataLayout();
   if (Offset + TD.getTypeAllocSize(NewTy) >= getSize())
     growSize(Offset + TD.getTypeAllocSize(NewTy));
 
@@ -442,7 +445,7 @@ void DSNode::mergeTypeInfo(Type *NewTy, unsigned Offset) {
   // individually(at the appropriate offset), instead of the 
   // struct type.
   if(NewTy->isStructTy()) {
-    const TargetData &TD = getParentGraph()->getTargetData();
+    const DataLayout &TD = getParentGraph()->getDataLayout();
     StructType *STy = cast<StructType>(NewTy);
     const StructLayout *SL = TD.getStructLayout(cast<StructType>(STy));
     unsigned count = 0;
@@ -461,7 +464,7 @@ void DSNode::mergeTypeInfo(const TyMapTy::mapped_type TyIt, unsigned Offset) {
   if (isCollapsedNode()) return;
   if (isArrayNode()) Offset %= getSize();
 
-  const TargetData &TD = getParentGraph()->getTargetData();
+  const DataLayout &TD = getParentGraph()->getDataLayout();
   if (!TyMap[Offset]){
     TyMap[Offset] = TyIt;
     for (svset<Type*>::const_iterator ni = TyMap[Offset]->begin(),
@@ -673,7 +676,7 @@ void DSNode::mergeWith(const DSNodeHandle &NH, unsigned Offset) {
   if (N == this) {
     // We cannot merge two pieces of the same node together, collapse the node
     // completely.
-    DEBUG(errs() << "Attempting to merge two chunks of the same node together!\n");
+    LLVM_DEBUG(errs() << "Attempting to merge two chunks of the same node together!\n");
     foldNodeCompletely();
     return;
   }
@@ -1296,7 +1299,7 @@ DSGraph* DataStructures::getOrCreateGraph(const Function* F) {
       if (resetAuxCalls)
         G->getAuxFunctionCalls() = G->getFunctionCalls();
     } else {
-      G = new DSGraph(GlobalECs, GraphSource->getTargetData(), *TypeSS);
+      G = new DSGraph(GlobalECs, GraphSource->getDataLayout(), *TypeSS);
       G->spliceFrom(BaseGraph);
       if (resetAuxCalls)
         G->getAuxFunctionCalls() = G->getFunctionCalls();
@@ -1341,7 +1344,7 @@ void DataStructures::formGlobalECs() {
   svset<const GlobalValue*> ECGlobals;
   buildGlobalECs(ECGlobals);
   if (!ECGlobals.empty()) {
-    DEBUG(errs() << "Eliminating " << ECGlobals.size() << " EC Globals!\n");
+    LLVM_DEBUG(errs() << "Eliminating " << ECGlobals.size() << " EC Globals!\n");
     for (DSInfoTy::iterator I = DSInfo.begin(),
          E = DSInfo.end(); I != E; ++I)
       eliminateUsesOfECGlobals(*I->second, ECGlobals);
@@ -1385,7 +1388,7 @@ void DataStructures::buildGlobalECs(svset<const GlobalValue*> &ECGlobals) {
     I->addGlobal(First);
   }
 
-  DEBUG(GlobalsGraph->AssertGraphOK());
+  LLVM_DEBUG(GlobalsGraph->AssertGraphOK());
 }
 
 /// EliminateUsesOfECGlobals - Once we have determined that some globals are in
@@ -1432,7 +1435,7 @@ void DataStructures::eliminateUsesOfECGlobals(DSGraph &G,
     MadeChange = true;
   }
 
-  DEBUG(if(MadeChange) G.AssertGraphOK());
+  LLVM_DEBUG(if(MadeChange) G.AssertGraphOK());
 }
 
 //For Entry Points
@@ -1487,7 +1490,7 @@ void DataStructures::init(DataStructures* D, bool clone, bool useAuxCalls,
   if (!clone) D->DSGraphsStolen = true;
 }
 
-void DataStructures::init(TargetData* T) {
+void DataStructures::init(DataLayout* T) {
   assert (!TD && "Already init");
   GraphSource = 0;
   Clone = false;
diff --git a/third-party/DSA/DataStructureStats.cpp b/third-party/DSA/DataStructureStats.cpp
index 69aabd3..018fbc1 100644
--- a/third-party/DSA/DataStructureStats.cpp
+++ b/third-party/DSA/DataStructureStats.cpp
@@ -11,13 +11,15 @@
 //
 //===----------------------------------------------------------------------===//
 
+#define DEBUG_TYPE "dss"
+
 #include <ostream>
 
-#include "llvm/Constants.h"
-#include "llvm/Function.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/Pass.h"
-#include "llvm/Support/InstVisitor.h"
+#include "llvm/IR/InstVisitor.h"
 #include "llvm/ADT/Statistic.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/FormattedStream.h"
@@ -65,7 +67,7 @@ namespace {
   class DSGraphStats : public FunctionPass, public InstVisitor<DSGraphStats> {
     void countCallees(const Function &F);
     const TDDataStructures *DS;
-    const TargetData *TD;
+    const DataLayout *TD;
     const DSGraph *TDGraph;
     dsa::TypeSafety<TDDataStructures> *TS;
     DSNodeHandle getNodeHandleForValue(Value *V);
@@ -81,7 +83,7 @@ namespace {
     void getAnalysisUsage(AnalysisUsage &AU) const {
       AU.setPreservesAll();
       AU.addRequired<TDDataStructures>();
-      AU.addRequired<TargetData>();
+      //AU.addRequired<DataLayout>();
       AU.addRequired<dsa::TypeSafety<TDDataStructures> >();
     }
 
@@ -134,7 +136,7 @@ void DSGraphStats::countCallees(const Function& F) {
         totalNumCallees  += Callees.size();
         ++numIndirectCalls;
       } else {
-        DEBUG(errs() << "WARNING: No callee in Function '" 
+        LLVM_DEBUG(errs() << "WARNING: No callee in Function '" 
               << F.getName().str() << " at call: \n"
               << *I->getCallSite().getInstruction());
       }
@@ -144,7 +146,7 @@ void DSGraphStats::countCallees(const Function& F) {
   NumIndirectCalls += numIndirectCalls;
 
   if (numIndirectCalls) {
-    DEBUG(errs() << "  In function " << F.getName() << ":  "
+    LLVM_DEBUG(errs() << "  In function " << F.getName() << ":  "
           << (totalNumCallees / (double) numIndirectCalls)
           << " average callees per indirect call\n");
   }
@@ -214,7 +216,7 @@ bool DSGraphStats::isNodeForValueUntyped(Value *V, unsigned Offset, const Functi
       ++NumTypeCount3Accesses;
     else
       ++NumTypeCount4Accesses;
-    DEBUG(assert(TS->isTypeSafe(V,F)));
+    LLVM_DEBUG(assert(TS->isTypeSafe(V,F)));
   }
   return false;
 }
@@ -237,7 +239,7 @@ void DSGraphStats::visitStore(StoreInst &SI) {
 
 bool DSGraphStats::runOnFunction(Function& F) {
   DS = &getAnalysis<TDDataStructures>();
-  TD = &getAnalysis<TargetData>();
+  TD = const_cast<llvm::DataLayout*>(&F.getParent()->getDataLayout());
   TS = &getAnalysis<dsa::TypeSafety<TDDataStructures> >();
   TDGraph = DS->getDSGraph(F);
   countCallees(F);
diff --git a/third-party/DSA/EntryPointAnalysis.cpp b/third-party/DSA/EntryPointAnalysis.cpp
index 1ae56b3..4144269 100644
--- a/third-party/DSA/EntryPointAnalysis.cpp
+++ b/third-party/DSA/EntryPointAnalysis.cpp
@@ -18,8 +18,8 @@
 #include <set>
 
 #include "llvm/Pass.h"
-#include "llvm/Module.h"
-#include "llvm/Function.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/Function.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/FormattedStream.h"
 #include "llvm/Support/Debug.h"
@@ -54,8 +54,8 @@ EntryPointAnalysis::~EntryPointAnalysis() {}
 void EntryPointAnalysis::findEntryPoints(const Module& M,
                                          std::vector<const Function*>& dest) const {
   for (Module::const_iterator ii = M.begin(), ee = M.end(); ii != ee; ++ii)
-    if (isEntryPoint(ii))
-      dest.push_back(ii);
+    if (isEntryPoint(&*ii))
+      dest.push_back(&*ii);
 }
 
 void EntryPointAnalysis::print(llvm::raw_ostream& O, const Module* M) const {
diff --git a/third-party/DSA/EquivClassGraphs.cpp b/third-party/DSA/EquivClassGraphs.cpp
index 05494cb..ef3efe1 100644
--- a/third-party/DSA/EquivClassGraphs.cpp
+++ b/third-party/DSA/EquivClassGraphs.cpp
@@ -16,10 +16,10 @@
 
 #define DEBUG_TYPE "ECGraphs"
 #include <fstream>
-#include "llvm/DerivedTypes.h"
-#include "llvm/Module.h"
+#include "llvm/IR/DerivedTypes.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Pass.h"
-#include "llvm/Support/CallSite.h"
+#include "llvm/IR/CallSite.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/ADT/SCCIterator.h"
 #include "llvm/ADT/Statistic.h"
@@ -48,7 +48,7 @@ bool EquivBUDataStructures::runOnModule(Module &M) {
   for(Module::iterator F = M.begin(); F != M.end(); ++F) 
   {
     if(!(F->isDeclaration()))
-      graphList.insert(getOrCreateGraph(F));
+      graphList.insert(getOrCreateGraph(&*F));
   }
 
   //update the EQ class from indirect calls
@@ -60,20 +60,20 @@ bool EquivBUDataStructures::runOnModule(Module &M) {
   for(Module::iterator F = M.begin(); F != M.end(); ++F) 
   {
     if(!(F->isDeclaration()))
-      graphList.erase(getOrCreateGraph(F));
+      graphList.erase(getOrCreateGraph(&*F));
   }
   // free memory for the DSGraphs, no longer in use.
   for(std::set<DSGraph*>::iterator i = graphList.begin(),e = graphList.end();
       i!=e;i++) {
     delete (*i);
   }
-  DEBUG(verifyMerging());
+  LLVM_DEBUG(verifyMerging());
 
   formGlobalECs();
 
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())) {
-      if (DSGraph * Graph = getOrCreateGraph(F)) {
+      if (DSGraph * Graph = getOrCreateGraph(&*F)) {
         cloneGlobalsInto(Graph, DSGraph::DontCloneCallNodes |
                         DSGraph::DontCloneAuxCallNodes);
       }
diff --git a/third-party/DSA/GraphChecker.cpp b/third-party/DSA/GraphChecker.cpp
index 33b29ea..a8c266e 100644
--- a/third-party/DSA/GraphChecker.cpp
+++ b/third-party/DSA/GraphChecker.cpp
@@ -27,7 +27,7 @@
 
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/FormattedStream.h"
-#include "llvm/Value.h"
+#include "llvm/IR/Value.h"
 
 #include "dsa/DataStructure.h"
 #include "dsa/DSGraph.h"
@@ -40,8 +40,8 @@ namespace {
        cl::desc("Specify which DSA pass the -datastructure-gc pass should use"),
          cl::values(clEnumVal(local, "Local pass"),
                     clEnumVal(bu,    "Bottom-up pass"),
-                    clEnumVal(td,    "Top-down pass"),
-                    clEnumValEnd), cl::init(local));
+                    clEnumVal(td,    "Top-down pass")
+                   ), cl::init(local));
 
   cl::opt<bool>
   AbortIfAnyCollapsed("dsgc-abort-if-any-collapsed", cl::Hidden,
diff --git a/third-party/DSA/IPModRef.cpp b/third-party/DSA/IPModRef.cpp
index 8450d5f..3ace03c 100644
--- a/third-party/DSA/IPModRef.cpp
+++ b/third-party/DSA/IPModRef.cpp
@@ -13,8 +13,8 @@
 
 #include <vector>
 
-#include "llvm/Module.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/ADT/Statistic.h"
 #include "llvm/ADT/STLExtras.h"
 #include "llvm/ADT/StringExtras.h"
@@ -30,17 +30,17 @@ Function *gFunc = NULL;
 //----------------------------------------------------------------------------
 
 //----------------------------------------------------------------------------
-// class ModRefInfo
+// class DSAModRefInfo
 //----------------------------------------------------------------------------
 
-void ModRefInfo::print(raw_ostream &O,
+void DSAModRefInfo::print(raw_ostream &O,
                        const std::string& sprefix) const
 {
   O << sprefix << "Modified   nodes = " << modNodeSet;
   O << sprefix << "Referenced nodes = " << refNodeSet;
 }
 
-void ModRefInfo::dump() const
+void DSAModRefInfo::dump() const
 {
   print(errs());
 }
@@ -62,13 +62,13 @@ FunctionModRefInfo::FunctionModRefInfo(const Function& func,
   unsigned i = 0;
   for (DSGraph::node_const_iterator NI = funcTDGraph->node_begin(),
          E = funcTDGraph->node_end(); NI != E; ++NI)
-    NodeIds[NI] = i++;
+    NodeIds[&*NI] = i++;
 }
 
 
 FunctionModRefInfo::~FunctionModRefInfo()
 {
-  for(std::map<const Instruction*, ModRefInfo*>::iterator
+  for(std::map<const Instruction*, DSAModRefInfo*>::iterator
         I=callSiteModRefInfo.begin(), E=callSiteModRefInfo.end(); I != E; ++I)
     delete(I->second);
 
@@ -214,7 +214,7 @@ void
 FunctionModRefInfo::computeModRef(CallSite CS)
 {
   // Allocate the mod/ref info for the call site.  Bits automatically cleared.
-  ModRefInfo* callModRefInfo = new ModRefInfo(funcTDGraph->getGraphSize());
+  DSAModRefInfo* callModRefInfo = new DSAModRefInfo(funcTDGraph->getGraphSize());
   callSiteModRefInfo[CS.getInstruction()] = callModRefInfo;
 
   // Get a copy of the graph for the callee with the callee inlined
@@ -233,12 +233,12 @@ FunctionModRefInfo::computeModRef(CallSite CS)
   // For all nodes in the graph, extract the mod/ref information
   for (DSGraph::node_iterator NI = funcTDGraph->node_begin(),
          E = funcTDGraph->node_end(); NI != E; ++NI) { 
-    DSNode* csgNode = NodeMap[NI].getNode();
+    DSNode* csgNode = NodeMap[&*NI].getNode();
     assert(csgNode && "Inlined and original graphs do not correspond!");
     if (csgNode->isModifiedNode())
-      callModRefInfo->setNodeIsMod(getNodeId(NI));
+      callModRefInfo->setNodeIsMod(getNodeId(&*NI));
     if (csgNode->isReadNode())
-      callModRefInfo->setNodeIsRef(getNodeId(NI));
+      callModRefInfo->setNodeIsRef(getNodeId(&*NI));
   }
 
   // Drop nodemap before we delete the graph...
@@ -343,7 +343,7 @@ void FunctionModRefInfo::print(raw_ostream &O) const
 
   // Second: Print Globals and Locals modified at each call site in function
   // 
-  for (std::map<const Instruction *, ModRefInfo*>::const_iterator
+  for (std::map<const Instruction *, DSAModRefInfo*>::const_iterator
          CI = callSiteModRefInfo.begin(), CE = callSiteModRefInfo.end();
        CI != CE; ++CI)
     {
@@ -387,7 +387,7 @@ bool IPModRef::runOnModule(Module &theModule)
     return true;
   }
   for (Module::iterator FI = M->begin(), FE = M->end(); FI != FE; ++FI) {
-    Function *f = FI;
+    Function *f = &*FI;
     if (!f->isDeclaration() && f == gFunc) {
       getFuncInfo(*FI, true);
       break;
diff --git a/third-party/DSA/Local.cpp b/third-party/DSA/Local.cpp
index 452b683..0ad6729 100644
--- a/third-party/DSA/Local.cpp
+++ b/third-party/DSA/Local.cpp
@@ -15,23 +15,23 @@
 #define DEBUG_TYPE "dsa-local"
 #include <fstream>
 
-#include "llvm/Use.h"
-#include "llvm/InlineAsm.h"
-#include "llvm/Constants.h"
-#include "llvm/Intrinsics.h"
-#include "llvm/DerivedTypes.h"
-#include "llvm/Instructions.h"
-#include "llvm/Support/GetElementPtrTypeIterator.h"
-#include "llvm/Support/InstVisitor.h"
+#include "llvm/IR/Use.h"
+#include "llvm/IR/InlineAsm.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Intrinsics.h"
+#include "llvm/IR/DerivedTypes.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/GetElementPtrTypeIterator.h"
+#include "llvm/IR/InstVisitor.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/FormattedStream.h"
 #include "llvm/Support/Timer.h"
-#include "llvm/Target/TargetData.h"
+#include "llvm/IR/DataLayout.h"
 #include "llvm/ADT/Statistic.h"
 #include "llvm/ADT/DenseSet.h"
 #include "llvm/ADT/Triple.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Module.h"
 
 #include "dsa/DataStructure.h"
 #include "dsa/DSGraph.h"
@@ -74,7 +74,7 @@ namespace {
     DSGraph &G;
     Function* FB;
     LocalDataStructures* DS;
-    const TargetData& TD;
+    const DataLayout& TD;
 
     ////////////////////////////////////////////////////////////////////////////
     // Helper functions used to implement the visitation functions...
@@ -138,12 +138,12 @@ namespace {
 
     bool visitIntrinsic(CallSite CS, Function* F);
     void visitCallSite(CallSite CS);
-    void visitVAStartInst(CallSite CS);
+    void visitVAStartInst(VAStartInst &I);
     void visitVAStartNode(DSNode* N);
 
   public:
     GraphBuilder(Function &f, DSGraph &g, LocalDataStructures& DSi)
-      : G(g), FB(&f), DS(&DSi), TD(g.getTargetData()) {
+      : G(g), FB(&f), DS(&DSi), TD(g.getDataLayout()) {
       // Create scalar nodes for all pointer arguments...
       for (Function::arg_iterator I = f.arg_begin(), E = f.arg_end();
            I != E; ++I) {
@@ -205,7 +205,7 @@ namespace {
 
     // GraphBuilder ctor for working on the globals graph
     explicit GraphBuilder(DSGraph& g)
-      :G(g), FB(0), TD(g.getTargetData())
+      :G(g), FB(0), TD(g.getDataLayout())
     {}
 
     void mergeInGlobalInitializer(GlobalVariable *GV);
@@ -700,7 +700,7 @@ void GraphBuilder::visitGetElementPtrInst(User &GEP) {
   //
   for (gep_type_iterator I = gep_type_begin(GEP), E = gep_type_end(GEP);
        I != E; ++I)
-    if (StructType *STy = dyn_cast<StructType>(*I)) {
+    if (StructType *STy = dyn_cast<StructType>(I.getIndexedType())) {
       // indexing into a structure
       // next index must be a constant
       const ConstantInt* CUI = cast<ConstantInt>(I.getOperand());
@@ -725,7 +725,7 @@ void GraphBuilder::visitGetElementPtrInst(User &GEP) {
           // J is the type of the next index.
           // Uncomment the line below to get all the nested types.
           gep_type_iterator J = I;
-          while(isa<ArrayType>(*(++J))) {
+          while(isa<ArrayType>((++J).getIndexedType())) {
             //      Value.getNode()->mergeTypeInfo(AT1, Value.getOffset() + Offset);
             if((++I) == E) {
               break;
@@ -737,7 +737,7 @@ void GraphBuilder::visitGetElementPtrInst(User &GEP) {
           }
         }
       }
-    } else if(ArrayType *ATy = dyn_cast<ArrayType>(*I)) {
+    } else if(ArrayType *ATy = dyn_cast<ArrayType>(I.getIndexedType())) {
       // indexing into an array.
       Value.getNode()->setArrayMarker();
       Type *CurTy = ATy->getElementType();
@@ -763,7 +763,7 @@ void GraphBuilder::visitGetElementPtrInst(User &GEP) {
         Offset = 0;
         break;
       }
-    } else if (const PointerType *PtrTy = dyn_cast<PointerType>(*I)) {
+    } else if (const PointerType *PtrTy = dyn_cast<PointerType>(I.getIndexedType())) {
       Type *CurTy = PtrTy->getElementType();
 
       //
@@ -820,7 +820,9 @@ void GraphBuilder::visitInvokeInst(InvokeInst &II) {
   visitCallSite(&II);
 }
 
-void GraphBuilder::visitVAStartInst(CallSite CS) {
+void GraphBuilder::visitVAStartInst(VAStartInst &I) {
+  CallSite CS(&I);
+
   // Build out DSNodes for the va_list depending on the target arch
   // And assosiate the right node with the VANode for this function
   // so it can be merged with the right arguments from callsites
@@ -909,7 +911,7 @@ bool GraphBuilder::visitIntrinsic(CallSite CS, Function *F) {
   ++NumIntrinsicCall;
   switch (F->getIntrinsicID()) {
   case Intrinsic::vastart: {
-    visitVAStartInst(CS);
+    visitVAStartInst(*cast<VAStartInst>(CS.getInstruction()));
     return true;
   }
   case Intrinsic::vacopy: {
@@ -953,27 +955,6 @@ bool GraphBuilder::visitIntrinsic(CallSite CS, Function *F) {
       N->setModifiedMarker();
     return true;
 
-  case Intrinsic::eh_exception: {
-    DSNode * Node = createNode();
-    Node->setIncompleteMarker();
-    Node->foldNodeCompletely();
-    setDestTo (*(CS.getInstruction()), Node);
-    return true;
-  }
-
-  case Intrinsic::eh_selector: {
-    for (CallSite::arg_iterator I = CS.arg_begin(), E = CS.arg_end();
-         I != E; ++I) {
-      if (isa<PointerType>((*I)->getType())) {
-        DSNodeHandle Ptr = getValueDest(*I);
-        if(Ptr.getNode()) {
-          Ptr.getNode()->setReadMarker();
-          Ptr.getNode()->setIncompleteMarker();
-        }
-      }
-    }
-    return true;
-  }
   case Intrinsic::eh_typeid_for: {
     DSNodeHandle Ptr = getValueDest(*CS.arg_begin());
     Ptr.getNode()->setReadMarker();
@@ -1020,7 +1001,7 @@ bool GraphBuilder::visitIntrinsic(CallSite CS, Function *F) {
         return true;
     }
 
-    DEBUG(errs() << "[dsa:local] Unhandled intrinsic: " << F->getName() << "\n");
+    LLVM_DEBUG(errs() << "[dsa:local] Unhandled intrinsic: " << F->getName() << "\n");
     assert(0 && "Unhandled intrinsic");
     return false;
   }
@@ -1065,7 +1046,7 @@ void GraphBuilder::visitCallSite(CallSite CS) {
   if (!isa<Function>(Callee)) {
     CalleeNode = getValueDest(Callee).getNode();
     if (CalleeNode == 0) {
-      DEBUG(errs() << "WARNING: Program is calling through a null pointer?\n" << *I);
+      LLVM_DEBUG(errs() << "WARNING: Program is calling through a null pointer?\n" << *I);
       return;  // Calling a null pointer?
     }
   }
@@ -1231,7 +1212,7 @@ GraphBuilder::MergeConstantInitIntoNode(DSNodeHandle &NH,
         // If this is one of those cute structures that ends with a zero-length
         // array, just fold the DSNode now and get it over with.
         //
-        DEBUG(errs() << "Zero size element at end of struct\n" );
+        LLVM_DEBUG(errs() << "Zero size element at end of struct\n" );
         NHN->foldNodeCompletely();
       } else {
         assert(0 && "type was smaller than offsets of struct layout indicate");
@@ -1299,11 +1280,11 @@ void handleMagicSections(DSGraph* GlobalsGraph, Module& M) {
       for (Module::iterator MI = M.begin(), ME = M.end();
            MI != ME; ++MI)
         if (MI->hasSection() && MI->getSection() == section)
-          inSection.insert(MI);
+          inSection.insert(&*MI);
       for (Module::global_iterator MI = M.global_begin(), ME = M.global_end();
            MI != ME; ++MI)
         if (MI->hasSection() && MI->getSection() == section)
-          inSection.insert(MI);
+          inSection.insert(&*MI);
 
       for (unsigned x = 0; x < count; ++x) {
         std::string global;
@@ -1313,7 +1294,7 @@ void handleMagicSections(DSGraph* GlobalsGraph, Module& M) {
           DSNodeHandle& DHV = GlobalsGraph->getNodeForValue(V);
           for (svset<Value*>::iterator SI = inSection.begin(),
                SE = inSection.end(); SI != SE; ++SI) {
-            DEBUG(errs() << "Merging " << V->getName().str() << " with "
+            LLVM_DEBUG(errs() << "Merging " << V->getName().str() << " with "
                   << (*SI)->getName().str() << "\n");
             GlobalsGraph->getNodeForValue(*SI).mergeWith(DHV);
           }
@@ -1330,7 +1311,7 @@ void handleMagicSections(DSGraph* GlobalsGraph, Module& M) {
 char LocalDataStructures::ID;
 
 bool LocalDataStructures::runOnModule(Module &M) {
-  init(&getAnalysis<TargetData>());
+  init(const_cast<llvm::DataLayout*>(&M.getDataLayout()));
   addrAnalysis = &getAnalysis<AddressTakenAnalysis>();
 
   // First step, build the globals graph.
@@ -1342,14 +1323,14 @@ bool LocalDataStructures::runOnModule(Module &M) {
          I != E; ++I)
       if (!(I->hasSection() && I->getSection() == "llvm.metadata")) {
         if (I->isDeclaration())
-          GGB.mergeExternalGlobal(I);
+          GGB.mergeExternalGlobal(&*I);
         else
-          GGB.mergeInGlobalInitializer(I);
+          GGB.mergeInGlobalInitializer(&*I);
       }
     // Add Functions to the globals graph.
     for (Module::iterator FI = M.begin(), FE = M.end(); FI != FE; ++FI){
-      if(addrAnalysis->hasAddressTaken(FI)) {
-        GGB.mergeFunction(FI);
+      if(addrAnalysis->hasAddressTaken(&*FI)) {
+        GGB.mergeFunction(&*FI);
       }
     }
   }
@@ -1370,12 +1351,12 @@ bool LocalDataStructures::runOnModule(Module &M) {
   // Calculate all of the graphs...
   for (Module::iterator I = M.begin(), E = M.end(); I != E; ++I)
     if (!I->isDeclaration()) {
-      DSGraph* G = new DSGraph(GlobalECs, getTargetData(), *TypeSS, GlobalsGraph);
+      DSGraph* G = new DSGraph(GlobalECs, getDataLayout(), *TypeSS, GlobalsGraph);
       GraphBuilder GGB(*I, *G, *this);
       G->getAuxFunctionCalls() = G->getFunctionCalls();
       setDSGraph(*I, G);
       propagateUnknownFlag(G);
-      callgraph.insureEntry(I);
+      callgraph.insureEntry(&*I);
       G->buildCallGraph(callgraph, GlobalFunctionList, true);
       G->maskIncompleteMarkers();
       G->markIncompleteNodes(DSGraph::MarkFormalArgs
@@ -1384,7 +1365,7 @@ bool LocalDataStructures::runOnModule(Module &M) {
                        DSGraph::DontCloneAuxCallNodes |
                        DSGraph::StripAllocaBit);
       formGlobalECs();
-      DEBUG(G->AssertGraphOK());
+      LLVM_DEBUG(G->AssertGraphOK());
     }
 
   //GlobalsGraph->removeTriviallyDeadNodes();
@@ -1401,7 +1382,7 @@ bool LocalDataStructures::runOnModule(Module &M) {
   propagateUnknownFlag(GlobalsGraph);
   for (Module::iterator I = M.begin(), E = M.end(); I != E; ++I)
     if (!I->isDeclaration()) {
-      DSGraph *Graph = getOrCreateGraph(I);
+      DSGraph *Graph = getOrCreateGraph(&*I);
       Graph->maskIncompleteMarkers();
       cloneGlobalsInto(Graph, DSGraph::DontCloneCallNodes |
                        DSGraph::DontCloneAuxCallNodes);
diff --git a/third-party/DSA/Printer.cpp b/third-party/DSA/Printer.cpp
index 5e4d64e..22f6bf8 100644
--- a/third-party/DSA/Printer.cpp
+++ b/third-party/DSA/Printer.cpp
@@ -15,9 +15,10 @@
 
 #include <sstream>
 
-#include "llvm/Module.h"
-#include "llvm/Constants.h"
-#include "llvm/Assembly/Writer.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/AssemblyAnnotationWriter.h"
+#include "llvm/Support/raw_ostream.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/GraphWriter.h"
 #include "llvm/ADT/Statistic.h"
@@ -41,7 +42,7 @@ namespace {
 }
 
 void DSNode::dump() const { print(errs(), 0); }
-void DSNode::dumpParentGraph() const { getParentGraph()->dump(); }
+void DSNode::dumpParentGraph() const { getParentGraph()->print(llvm::errs()); llvm::errs() << "\n";; }
 
 static std::string getCaption(const DSNode *N, const DSGraph *G) {
   std::string empty;
@@ -118,7 +119,7 @@ static std::string getCaption(const DSNode *N, const DSGraph *G) {
 
   for (DSNode::globals_iterator i = N->globals_begin(), e = N->globals_end(); 
        i != e; ++i) {
-    WriteAsOperand(OS, *i, false, M);
+    (*i)->printAsOperand(OS, false, M);
 
     // Figure out how many globals are equivalent to this one.
     if (GlobalECs) {
@@ -216,7 +217,7 @@ struct DOTGraphTraits<const DSGraph*> : public DefaultDOTGraphTraits {
         if (!isa<GlobalValue>(I->first)) {
           std::string OS_str;
           llvm::raw_string_ostream OS(OS_str);
-          WriteAsOperand(OS, I->first, false, CurMod);
+          I->first->printAsOperand(OS, false, CurMod);
           GW.emitSimpleNode(I->first, "", OS.str());
           
           // Add edge from return node to real destination
@@ -292,9 +293,18 @@ struct DOTGraphTraits<const DSGraph*> : public DefaultDOTGraphTraits {
 };
 }   // end namespace llvm
 
-void DSNode::print(llvm::raw_ostream &O, const DSGraph *G) const {
-  GraphWriter<const DSGraph *> W(O, G, false);
-  W.writeNode(this);
+void DSNode::print(llvm::raw_ostream& O, const llvm::DSGraph* G) const {
+  struct PublicNodeWriter : public llvm::GraphWriter<const llvm::DSGraph*> {
+    PublicNodeWriter(llvm::raw_ostream &O, const llvm::DSGraph *G, bool ShortNames)
+      : llvm::GraphWriter<const llvm::DSGraph*>(O, G, ShortNames) {}
+
+    void printSingleNode(const llvm::DSNode *N) {
+      this->writeNode(N); 
+    }
+  };
+
+  PublicNodeWriter W(O, G, false);
+  W.printSingleNode(this);
 }
 
 void DSGraph::print(llvm::raw_ostream &O) const {
@@ -306,11 +316,11 @@ void DSGraph::writeGraphToFile(llvm::raw_ostream &O,
   std::string Filename = GraphName + ".dot";
   O << "Writing '" << Filename << "'...";
   if (!DontPrintGraphs) {
-    std::string Error;
-    llvm::raw_fd_ostream F(Filename.c_str(), Error);
+    std::error_code EC;
+    llvm::raw_fd_ostream F(Filename.c_str(), EC, llvm::sys::fs::OF_None);
 
-    if (Error.size()) {
-      O << "  error opening file for writing! " << Error << "\n";
+    if (EC) {
+      O << "  error opening file for writing! " << EC.message() << "\n";
       return;
     }
 
diff --git a/third-party/DSA/SanityCheck.cpp b/third-party/DSA/SanityCheck.cpp
index 73104a5..53dd7cf 100644
--- a/third-party/DSA/SanityCheck.cpp
+++ b/third-party/DSA/SanityCheck.cpp
@@ -13,8 +13,8 @@
 
 #define DEBUG_TYPE "dsa-check"
 
-#include "llvm/Instructions.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Module.h"
 
 #include "dsa/DataStructure.h"
 #include "dsa/DSGraph.h"
diff --git a/third-party/DSA/StdLibPass.cpp b/third-party/DSA/StdLibPass.cpp
index 5e0025c..72953d2 100644
--- a/third-party/DSA/StdLibPass.cpp
+++ b/third-party/DSA/StdLibPass.cpp
@@ -9,21 +9,22 @@
 // FIXME: Move table to separate analysis pass, so that even the Local Pass
 // may query it.
 //===----------------------------------------------------------------------===//
+#define DEBUG_TYPE "std-lib-pass"
 
 #include <iostream>
 
 #include "llvm/ADT/Statistic.h"
-#include "llvm/Constants.h"
-#include "llvm/DerivedTypes.h"
-#include "llvm/Instructions.h"
-#include "llvm/Intrinsics.h"
-#include "llvm/Support/GetElementPtrTypeIterator.h"
-#include "llvm/Target/TargetData.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/DerivedTypes.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Intrinsics.h"
+#include "llvm/IR/GetElementPtrTypeIterator.h"
+#include "llvm/IR/DataLayout.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/FormattedStream.h"
 #include "llvm/Support/Timer.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Module.h"
 
 #include "dsa/DataStructure.h"
 #include "dsa/AllocatorIdentification.h"
@@ -457,7 +458,7 @@ StdLibDataStructures::eraseCallsTo(Function* F) {
       if (CI->getCalledValue() == F) {
         DSGraph* Graph = getDSGraph(*CI->getParent()->getParent());
         //delete the call
-        DEBUG(errs() << "Removing " << F->getName().str() << " from "
+        LLVM_DEBUG(errs() << "Removing " << F->getName().str() << " from "
               << CI->getParent()->getParent()->getName().str() << "\n");
         ToRemove.insert(std::make_pair(Graph, F));
       }
@@ -465,7 +466,7 @@ StdLibDataStructures::eraseCallsTo(Function* F) {
       if (CI->getCalledValue() == F) {
         DSGraph* Graph = getDSGraph(*CI->getParent()->getParent());
         //delete the call
-        DEBUG(errs() << "Removing " << F->getName().str() << " from "
+        LLVM_DEBUG(errs() << "Removing " << F->getName().str() << " from "
               << CI->getParent()->getParent()->getName().str() << "\n");
         ToRemove.insert(std::make_pair(Graph, F));
       }
@@ -477,7 +478,7 @@ StdLibDataStructures::eraseCallsTo(Function* F) {
             if(CI->getCalledValue() == CE) {
               DSGraph* Graph = getDSGraph(*CI->getParent()->getParent());
               //delete the call
-              DEBUG(errs() << "Removing " << F->getName().str() << " from "
+              LLVM_DEBUG(errs() << "Removing " << F->getName().str() << " from "
                     << CI->getParent()->getParent()->getName().str() << "\n");
               ToRemove.insert(std::make_pair(Graph, F));
             }
@@ -565,7 +566,7 @@ StdLibDataStructures::runOnModule (Module &M) {
   for (Module::iterator I = M.begin(), E = M.end(); I != E; ++I) 
     if (I->isDeclaration() && I->doesNotAccessMemory() &&
         !isa<PointerType>(I->getReturnType()))
-      eraseCallsTo(I);
+      eraseCallsTo(&*I);
 
   //
   // Erase direct calls to external functions that are not varargs, do not
@@ -583,7 +584,7 @@ StdLibDataStructures::runOnModule (Module &M) {
           break;
         }
       if (!hasPtr)
-        eraseCallsTo(I);
+        eraseCallsTo(&*I);
     }
 
   if(!DisableStdLib) {
@@ -656,15 +657,15 @@ StdLibDataStructures::runOnModule (Module &M) {
       G->markIncompleteNodes(DSGraph::MarkFormalArgs
                              |DSGraph::IgnoreGlobals);
       G->computeExternalFlags(EFlags);
-      DEBUG(G->AssertGraphOK());
+      LLVM_DEBUG(G->AssertGraphOK());
     }
   GlobalsGraph->markIncompleteNodes(DSGraph::MarkFormalArgs
                                     |DSGraph::IgnoreGlobals);
   GlobalsGraph->computeExternalFlags(DSGraph::ProcessCallSites);
-  DEBUG(GlobalsGraph->AssertGraphOK());
+  LLVM_DEBUG(GlobalsGraph->AssertGraphOK());
   for (Module::iterator I = M.begin(), E = M.end(); I != E; ++I)
     if (!I->isDeclaration()) {
-      DSGraph *Graph = getOrCreateGraph(I);
+      DSGraph *Graph = getOrCreateGraph(&*I);
       Graph->maskIncompleteMarkers();
       cloneGlobalsInto(Graph, DSGraph::DontCloneCallNodes |
                        DSGraph::DontCloneAuxCallNodes);
diff --git a/third-party/DSA/TopDownClosure.cpp b/third-party/DSA/TopDownClosure.cpp
index 25a0d0c..3f0b543 100644
--- a/third-party/DSA/TopDownClosure.cpp
+++ b/third-party/DSA/TopDownClosure.cpp
@@ -15,8 +15,9 @@
 //===----------------------------------------------------------------------===//
 #define DEBUG_TYPE "td_dsa"
 
-#include "llvm/Module.h"
-#include "llvm/DerivedTypes.h"
+#include "llvm/Support/Debug.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/DerivedTypes.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/FormattedStream.h"
 #include "llvm/Support/Timer.h"
@@ -81,7 +82,7 @@ bool TDDataStructures::runOnModule(Module &M) {
 
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())){
-      DSGraph *G = getOrCreateGraph(F);
+      DSGraph *G = getOrCreateGraph(&*F);
       G->getAuxFunctionCalls().clear();
     }
   }
@@ -115,7 +116,7 @@ bool TDDataStructures::runOnModule(Module &M) {
   // Functions without internal linkage are definitely externally callable!
   for (Module::iterator I = M.begin(), E = M.end(); I != E; ++I)
     if (!I->isDeclaration() && !I->hasInternalLinkage() && !I->hasPrivateLinkage())
-      ExternallyCallable.insert(I);
+      ExternallyCallable.insert(&*I);
 
   // Debug code to print the functions that are externally callable
 #if 0
@@ -171,7 +172,7 @@ bool TDDataStructures::runOnModule(Module &M) {
   VisitedGraph.clear();
   for (Module::iterator F = M.begin(); F != M.end(); ++F) {
     if (!(F->isDeclaration())){
-      DSGraph *Graph  = getOrCreateGraph(F);
+      DSGraph *Graph  = getOrCreateGraph(&*F);
       if (!VisitedGraph.insert(Graph).second) continue;
 
       cloneGlobalsInto(Graph, DSGraph::DontCloneCallNodes |
@@ -254,7 +255,7 @@ void TDDataStructures::InlineCallersIntoGraph(DSGraph* DSG) {
   cloneGlobalsInto(DSG, DSGraph::DontCloneCallNodes |
                         DSGraph::DontCloneAuxCallNodes);
 
-  DEBUG(errs() << "[TD] Inlining callers into '"
+  LLVM_DEBUG(errs() << "[TD] Inlining callers into '"
         << DSG->getFunctionNames() << "'\n");
 
   DSG->maskIncompleteMarkers();
@@ -272,15 +273,15 @@ void TDDataStructures::InlineCallersIntoGraph(DSGraph* DSG) {
     do {
       const DSCallSite &CS = *EdgesFromCaller.back().CS;
       const Function &CF = *EdgesFromCaller.back().CalledFunction;
-      DEBUG(errs() << "   [TD] Inlining graph into Fn '"
+      LLVM_DEBUG(errs() << "   [TD] Inlining graph into Fn '"
             << CF.getName().str() << "' from ");
       if (CallerGraph->getReturnNodes().empty()) {
-        DEBUG(errs() << "SYNTHESIZED INDIRECT GRAPH");
+        LLVM_DEBUG(errs() << "SYNTHESIZED INDIRECT GRAPH");
       } else {
-        DEBUG(errs() << "Fn '" << CS.getCallSite().getInstruction()->
+        LLVM_DEBUG(errs() << "Fn '" << CS.getCallSite().getInstruction()->
               getParent()->getParent()->getName().str() << "'");
       }
-      DEBUG(errs() << ": " << CF.getFunctionType()->getNumParams()
+      LLVM_DEBUG(errs() << ": " << CF.getFunctionType()->getNumParams()
             << " args\n");
 
       // Get the formal argument and return nodes for the called function and
@@ -394,7 +395,7 @@ void TDDataStructures::InlineCallersIntoGraph(DSGraph* DSG) {
 
     // If we already have this graph, recycle it.
     if (IndCallRecI != IndCallMap.end() && IndCallRecI->first == Callees) {
-      DEBUG(errs() << "  [TD] *** Reuse of indcall graph for " << Callees.size()
+      LLVM_DEBUG(errs() << "  [TD] *** Reuse of indcall graph for " << Callees.size()
             << " callees!\n");
       DSGraph * IndCallGraph = IndCallRecI->second;
       assert(IndCallGraph->getFunctionCalls().size() == 1);
@@ -405,7 +406,7 @@ void TDDataStructures::InlineCallersIntoGraph(DSGraph* DSG) {
     } else {
       // Otherwise, create a new DSGraph to represent this.
       DSGraph* IndCallGraph = new DSGraph(DSG->getGlobalECs(),
-                                          DSG->getTargetData(), *TypeSS);
+                                          DSG->getDataLayout(), *TypeSS);
 
       // Clone over the call into the new DSGraph
       ReachabilityCloner RC(IndCallGraph, DSG, 0);
diff --git a/third-party/DSA/TypeSafety.cpp b/third-party/DSA/TypeSafety.cpp
index 04c8597..41da51e 100644
--- a/third-party/DSA/TypeSafety.cpp
+++ b/third-party/DSA/TypeSafety.cpp
@@ -15,8 +15,8 @@
 
 #define DEBUG_TYPE "type-safety"
 
-#include "llvm/Module.h"
-#include "llvm/DerivedTypes.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/DerivedTypes.h"
 #include "llvm/Support/Debug.h"
 #include "llvm/Support/FormattedStream.h"
 #include "llvm/Support/CommandLine.h"
@@ -236,7 +236,7 @@ TypeSafety<dsa>::typeFieldsOverlap (const DSNode * N) {
             }
           }
           if(overlaps) {
-            DEBUG(errs() << " Found overlap at " << offset << " with " << next_offset << "\n");
+            LLVM_DEBUG(errs() << " Found overlap at " << offset << " with " << next_offset << "\n");
             break;
           }
         }
@@ -322,7 +322,7 @@ TypeSafety<dsa>::findTypeSafeDSNodes (const DSGraph * Graph) {
   DSGraph::node_const_iterator N = Graph->node_begin();
   DSGraph::node_const_iterator NE = Graph->node_end();
   for (; N != NE; ++N) {
-    if (isTypeSafe (N)) {
+    if (isTypeSafe (&*N)) {
       TypeSafeNodes.insert (&*N);
     }
   }
@@ -333,7 +333,7 @@ TypeSafety<dsa>::runOnModule(Module & M) {
   //
   // Get access to prerequisite passes.
   //
-  TD      = &getAnalysis<TargetData>();
+  TD      = const_cast<llvm::DataLayout*>(&M.getDataLayout());
   dsaPass = &getAnalysis<dsa>();
 
   //
diff --git a/third-party/Dependence/DependenceGraph.cpp b/third-party/Dependence/DependenceGraph.cpp
index 8751fcb..e6360a0 100644
--- a/third-party/Dependence/DependenceGraph.cpp
+++ b/third-party/Dependence/DependenceGraph.cpp
@@ -21,8 +21,8 @@
 //===----------------------------------------------------------------------===//
 
 
-#include "llvm/Function.h"
-#include "llvm/Analysis/DebugInfo.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/DebugInfo.h"
 
 #include "dependence/DependenceGraph.h"
 
@@ -31,7 +31,7 @@ namespace llvm {
 unsigned getInstLine(const Instruction &I)
 {
   DebugLoc Loc = I.getDebugLoc();
-  if (Loc.isUnknown())
+  if (!Loc)
     return 0;
   return Loc.getLine();
 }
diff --git a/third-party/Dependence/PgmDependenceGraph.cpp b/third-party/Dependence/PgmDependenceGraph.cpp
index 90b0ae9..e4f22cc 100644
--- a/third-party/Dependence/PgmDependenceGraph.cpp
+++ b/third-party/Dependence/PgmDependenceGraph.cpp
@@ -27,13 +27,13 @@
 
 #include <map>
 #include <iostream>
-
+#include "llvm/Analysis/MemoryDependenceAnalysis.h"
 #include "llvm/Analysis/PostDominators.h"
-#include "llvm/Support/CFG.h"
-#include "llvm/Support/InstVisitor.h"
+#include "llvm/IR/CFG.h"
+#include "llvm/IR/InstVisitor.h"
 #include "llvm/ADT/SCCIterator.h"
-#include "llvm/Function.h"
-#include "llvm/IntrinsicInst.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/IntrinsicInst.h"
 
 #include "slicer/PDGSlicer.h"
 #include "dependence/PgmDependenceGraph.h"
@@ -70,7 +70,7 @@ class DependenceGraph *globalDPG = NULL;
 
 class ModRefTable {
 public:
-  typedef std::map<Instruction*, ModRefInfo> ModRefMap;
+  typedef std::map<Instruction*, DSAModRefInfo> ModRefMap;
   typedef ModRefMap::const_iterator                 const_map_iterator;
   typedef ModRefMap::iterator                       map_iterator;
   typedef std::vector<Instruction*>::const_iterator const_ref_iterator;
@@ -151,8 +151,8 @@ public:
   // list if it refs any nodes.
   // 
   void visitCallInst(CallInst& callInst) {
-    ModRefInfo safeModRef(funcGraph.getGraphSize());
-    const ModRefInfo* callModRef = funcModRef.getModRefInfo(callInst);
+    DSAModRefInfo safeModRef(funcGraph.getGraphSize());
+    const DSAModRefInfo* callModRef = funcModRef.getModRefInfo(callInst);
     if (callModRef == NULL) {
       // call to external/unknown function: mark all nodes as Mod and Ref
       safeModRef.getModSet().set();
@@ -161,7 +161,7 @@ public:
     }
 
     modRefTable.modRefMap.insert(std::make_pair(&callInst,
-                                                ModRefInfo(*callModRef)));
+                                                DSAModRefInfo(*callModRef)));
     if (callModRef->getModSet().any())
       modRefTable.AddDef(&callInst);
     if (callModRef->getRefSet().any())
@@ -176,10 +176,10 @@ public:
       funcGraph.getNodeForValue(storeInst.getPointerOperand());
     if (const DSNode* target = ptrNode.getNode()) {
       unsigned nodeId = funcModRef.getNodeId(target);
-      ModRefInfo& minfo =
+      DSAModRefInfo& minfo =
         modRefTable.modRefMap.insert(
           std::make_pair(&storeInst,
-                         ModRefInfo(funcGraph.getGraphSize()))).first->second;
+                         DSAModRefInfo(funcGraph.getGraphSize()))).first->second;
       minfo.setNodeIsMod(nodeId);
       modRefTable.AddDef(&storeInst);
     } else
@@ -194,10 +194,10 @@ public:
       funcGraph.getNodeForValue(loadInst.getPointerOperand());
     if (const DSNode* target = ptrNode.getNode()) {
       unsigned nodeId = funcModRef.getNodeId(target);
-      ModRefInfo& minfo =
+      DSAModRefInfo& minfo =
         modRefTable.modRefMap.insert(
           std::make_pair(&loadInst,
-                         ModRefInfo(funcGraph.getGraphSize()))).first->second;
+                         DSAModRefInfo(funcGraph.getGraphSize()))).first->second;
       minfo.setNodeIsRef(nodeId);
       modRefTable.AddUse(&loadInst);
     } else
@@ -606,7 +606,8 @@ bool PgmDependenceGraph::buildPDG2(Function& F)
   // outs() << "SCCs for Function " << F.getName() << " in PostOrder:";
   for (scc_iterator<Function*> I = scc_begin(&F), E = scc_end(&F); I != E; ++I) {
     // outs() << "\nSCC #" << ++sccNum << " : ";
-    ProcessSCC(*I, ModRefAfter, I.hasLoop());
+    std::vector<BasicBlock*> CurrentSCC = *I;
+    ProcessSCC(CurrentSCC, ModRefAfter, I.hasLoop());
   }
   return true;
 }
@@ -618,7 +619,7 @@ bool PgmDependenceGraph::buildPDG2(Function& F)
 //
 bool PgmDependenceGraph::buildPDG(Function& F)
 {
-  MemoryDependenceAnalysis &mda = getAnalysis<MemoryDependenceAnalysis>();
+  MemoryDependenceResults &mda = getAnalysis<MemoryDependenceWrapperPass>().getMemDep();
   for (Function::iterator FI = F.begin(), FE = F.end(); FI != FE; FI++) {
     for (BasicBlock::iterator BI = FI->begin(), BE = FI->end(); BI != BE; BI++) {
       Instruction *I = &(*BI);
@@ -696,7 +697,7 @@ void PgmDependenceGraph::testSlicing(Function &F)
   for (Function::iterator BB=F.begin(), FE=F.end(); BB != FE; ++BB)
     for (BasicBlock::iterator II=BB->begin(), IE=BB->end(); II !=IE; ++II)
     {
-      Instruction *I = II;
+      Instruction *I = &*II;
       if (isa<DbgInfoIntrinsic>(I)) { // Skip intrinsics 
         continue;
       }
diff --git a/third-party/LLVMSlicer/Languages/LLVM.cpp b/third-party/LLVMSlicer/Languages/LLVM.cpp
index dcf7ed0..0490387 100644
--- a/third-party/LLVMSlicer/Languages/LLVM.cpp
+++ b/third-party/LLVMSlicer/Languages/LLVM.cpp
@@ -1,11 +1,11 @@
 // This file is distributed under the University of Illinois Open Source
 // License. See LICENSE.TXT for details.
 
-#include "llvm/Constant.h"
-#include "llvm/Constants.h"
-#include "llvm/InlineAsm.h"
-#include "llvm/Instructions.h"
-#include "llvm/Value.h"
+#include "llvm/IR/Constant.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/InlineAsm.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Value.h"
 
 #include "llvmslicer/LLVM.h"
 
diff --git a/third-party/LLVMSlicer/Modifies/ModStats.cpp b/third-party/LLVMSlicer/Modifies/ModStats.cpp
index 2cbfd6a..346f82a 100644
--- a/third-party/LLVMSlicer/Modifies/ModStats.cpp
+++ b/third-party/LLVMSlicer/Modifies/ModStats.cpp
@@ -1,17 +1,19 @@
 // This file is distributed under the University of Illinois Open Source
 // License. See LICENSE.TXT for details.
 
-#include "llvm/Instructions.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/Pass.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Analysis/LoopInfo.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/InstIterator.h"
 #include "llvm/Support/raw_ostream.h"
 
 #include "llvmslicer/Callgraph.h"
 #include "llvmslicer/PointsTo.h"
 #include "llvmslicer/Prepare.h"
 
+#include <tuple>
+
 using namespace llvm;
 
 namespace {
@@ -25,7 +27,7 @@ namespace {
 
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
       AU.setPreservesAll();
-      AU.addRequired<LoopInfo>();
+      AU.addRequired<LoopInfoWrapperPass>();
     }
   };
 }
@@ -276,7 +278,7 @@ void StatsComputer::run() {
     Function &F = *I;
     if (F.isDeclaration())
       continue;
-    handleFun(modInfo, F, modPass.getAnalysis<LoopInfo>(F));
+    handleFun(modInfo, F, modPass.getAnalysis<LoopInfoWrapperPass>(F).getLoopInfo());
   }
 
   const ConstantArray *initFuns = getInitFuns(M);
@@ -289,7 +291,7 @@ void StatsComputer::run() {
     const Function &F = *cast<Function>(CE->getOperand(0));
     FunInfo *funInfo = modInfo.getFunInfo(&F);
     callgraph::Callgraph::const_iterator II, EE;
-    llvm::tie(II, EE) = CG.calls(&F);
+    std::tie(II, EE) = CG.calls(&F);
 #ifdef DEBUG_NESTED
     errs() << "at " << F.getName() << " flags [" << getFlags(funInfo) << "]\n";
 #endif
diff --git a/third-party/LLVMSlicer/Modifies/Modifies.cpp b/third-party/LLVMSlicer/Modifies/Modifies.cpp
index 717a528..06b16e2 100644
--- a/third-party/LLVMSlicer/Modifies/Modifies.cpp
+++ b/third-party/LLVMSlicer/Modifies/Modifies.cpp
@@ -4,10 +4,10 @@
 #include <algorithm>
 #include <iterator>
 
-#include "llvm/Constant.h"
-#include "llvm/Instruction.h"
-#include "llvm/Instructions.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Constant.h"
+#include "llvm/IR/Instruction.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Module.h"
 
 #include "llvmslicer/Callgraph.h"
 #include "llvmslicer/PointsTo.h"
diff --git a/third-party/LLVMSlicer/PointsTo/PointsTo.cpp b/third-party/LLVMSlicer/PointsTo/PointsTo.cpp
index 7bab39a..65d3901 100644
--- a/third-party/LLVMSlicer/PointsTo/PointsTo.cpp
+++ b/third-party/LLVMSlicer/PointsTo/PointsTo.cpp
@@ -1,9 +1,9 @@
 // This file is distributed under the University of Illinois Open Source
 // License. See LICENSE.TXT for details.
 
-#include "llvm/BasicBlock.h"
-#include "llvm/Instruction.h"
-#include "llvm/Module.h"
+#include "llvm/IR/BasicBlock.h"
+#include "llvm/IR/Instruction.h"
+#include "llvm/IR/Module.h"
 
 #include "llvmslicer/PointsTo.h"
 #include "llvmslicer/RuleExpressions.h"
diff --git a/third-party/LLVMSlicer/PointsTo/RuleExpressions.cpp b/third-party/LLVMSlicer/PointsTo/RuleExpressions.cpp
index 48f36bf..a769bad 100644
--- a/third-party/LLVMSlicer/PointsTo/RuleExpressions.cpp
+++ b/third-party/LLVMSlicer/PointsTo/RuleExpressions.cpp
@@ -1,9 +1,9 @@
 // This file is distributed under the University of Illinois Open Source
 // License. See LICENSE.TXT for details.
 
-#include "llvm/BasicBlock.h"
-#include "llvm/Instructions.h"
-#include "llvm/Module.h"
+#include "llvm/IR/BasicBlock.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Module.h"
 
 #include "llvmslicer/RuleExpressions.h"
 
diff --git a/third-party/LLVMSlicer/Slicing/FunctionStaticSlicer.cpp b/third-party/LLVMSlicer/Slicing/FunctionStaticSlicer.cpp
index 78e4d8e..2507b82 100644
--- a/third-party/LLVMSlicer/Slicing/FunctionStaticSlicer.cpp
+++ b/third-party/LLVMSlicer/Slicing/FunctionStaticSlicer.cpp
@@ -15,18 +15,17 @@
 #include <set>
 #include <list>
 
-#include "llvm/IntrinsicInst.h"
-#include "llvm/Constants.h"
-#include "llvm/Function.h"
-#include "llvm/GlobalVariable.h"
-#include "llvm/Instructions.h"
-#include "llvm/Module.h"
+#include "llvm/IR/IntrinsicInst.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/GlobalVariable.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Pass.h"
-#include "llvm/Support/TypeBuilder.h"
 #include "llvm/ADT/SmallVector.h"
 #include "llvm/Analysis/PostDominators.h"
-#include "llvm/Support/CFG.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/CFG.h"
+#include "llvm/IR/InstIterator.h"
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/Transforms/Utils/BasicBlockUtils.h"
 
@@ -275,7 +274,7 @@ namespace {
       virtual bool runOnModule(Module &M);
 
       void getAnalysisUsage(AnalysisUsage &AU) const {
-        AU.addRequired<PostDominatorTree>();
+        AU.addRequired<PostDominatorTreeWrapperPass>();
         AU.addRequired<PostDominanceFrontier>();
       }
     private:
@@ -682,13 +681,15 @@ static bool canSlice(const Instruction &i) {
 // a simple function 'prototype' printer
 void printFuncProtoType(Function *F)
 {
-  F->getReturnType()->dump();
+  F->getReturnType()->print(llvm::errs());
+  llvm::errs() << "\n";
   errs() << " " << cpp_demangle(F->getName().data()) << "(";
   FunctionType * FTY = F->getFunctionType();
   unsigned params = FTY->getNumParams();
   unsigned i;
   for (i = 0; i < params; i++) {
-    FTY->getParamType(i)->dump();
+    FTY->getParamType(i)->print(llvm::errs());
+    llvm::errs() << "\n";
     if (i != params - 1)
       errs() << ",";
   }
@@ -718,7 +719,8 @@ void FunctionStaticSlicer::dump(bool outputline) {
       }
     }
     else
-      i.dump();
+      i.print(llvm::errs());
+      llvm::errs() << "\n";;
 #ifdef DEBUG_DUMP
     errs() << "    DEF:\n";
     for (ValSet::const_iterator II = ii->DEF_begin(), EE = ii->DEF_end();
@@ -816,7 +818,7 @@ void FunctionStaticSlicer::removeUndefBranches(ModulePass *MP, Function &F) {
 #ifdef DEBUG_SLICE
   errs() << __func__ << " ============ Removing unused branches\n";
 #endif
-  PostDominatorTree &PDT = MP->getAnalysis<PostDominatorTree>(F);
+  PostDominatorTree &PDT = MP->getAnalysis<llvm::PostDominatorTreeWrapperPass>(F).getPostDomTree();
   typedef llvm::SmallVector<const BasicBlock *, 10> Unsafe;
   Unsafe unsafe;
 
@@ -942,7 +944,8 @@ bool llvm::slicing::findInitialCriterion(Function &F,
 
   for (inst_iterator I = inst_begin(F), E = inst_end(F); I != E; ++I) {
     const Instruction *i = &*I;
-    i->dump();
+    i->print(llvm::errs());
+    llvm::errs() << "\n";;
     if (const StoreInst *SI = dyn_cast<StoreInst>(i)) {
       const Value *LHS = SI->getPointerOperand();
       if (LHS->hasName() && LHS->getName().startswith("product")) {
diff --git a/third-party/LLVMSlicer/Slicing/Kleerer.cpp b/third-party/LLVMSlicer/Slicing/Kleerer.cpp
index d0dc84a..ee64b74 100644
--- a/third-party/LLVMSlicer/Slicing/Kleerer.cpp
+++ b/third-party/LLVMSlicer/Slicing/Kleerer.cpp
@@ -1,20 +1,24 @@
-#include "llvm/Attributes.h"
-#include "llvm/Constants.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/Attributes.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/Pass.h"
-#include "llvm/PassManager.h"
-#include "llvm/Module.h"
-#include "llvm/Target/TargetData.h"
-#include "llvm/Support/TypeBuilder.h"
-#include "llvm/Analysis/Verifier.h"
-#include "llvm/Bitcode/ReaderWriter.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/PassManager.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/DataLayout.h"
+#include "llvm/IR/Verifier.h"
+#include "llvm/Bitcode/BitcodeWriter.h"
+#include "llvm/Bitcode/BitcodeReader.h"
+#include "llvm/IR/InstIterator.h"
 #include "llvm/Support/raw_ostream.h"
+#include "llvm/IR/LegacyPassManager.h"
 
 #include "llvmslicer/Callgraph.h"
 #include "llvmslicer/PointsTo.h"
 #include "llvmslicer/Prepare.h"
 
+#include <cstddef>
+#include <tuple>
+
 using namespace llvm;
 
 namespace {
@@ -28,20 +32,20 @@ namespace {
 
     virtual void getAnalysisUsage(AnalysisUsage &AU) const {
       AU.setPreservesAll();
-      AU.addRequired<TargetData>();
+      //AU.addRequired<DataLayout>();
     }
   };
 }
 
 class Kleerer {
 public:
-  Kleerer(ModulePass &modPass, Module &M, TargetData &TD,
+  Kleerer(ModulePass &modPass, Module &M, DataLayout &TD,
           callgraph::Callgraph &CG) : modPass(modPass),
       M(M), TD(TD), CG(CG), C(M.getContext()), intPtrTy(TD.getIntPtrType(C)),
       done(false) {
-    voidPtrType = TypeBuilder<void *, false>::get(C);
-    intType = TypeBuilder<int, false>::get(C);
-    uintType = TypeBuilder<unsigned, false>::get(C);
+    voidPtrType = Type::getInt8PtrTy(C);
+    intType = Type::getInt32Ty(C);
+    uintType = Type::getInt32Ty(C);
   }
 
   bool run();
@@ -49,7 +53,7 @@ public:
 private:
   ModulePass &modPass;
   Module &M;
-  TargetData &TD;
+  DataLayout &TD;
   callgraph::Callgraph &CG;
   LLVMContext &C;
   IntegerType *intPtrTy;
@@ -95,9 +99,11 @@ static void check(Value *Func, ArrayRef<Value *> Args) {
   for (unsigned i = 0; i != Args.size(); ++i) {
     if (!(FTy->getParamType(i) == Args[i]->getType())) {
       errs() << "types:\n  ";
-      FTy->getParamType(i)->dump();
+      FTy->getParamType(i)->print(llvm::errs());
+      llvm::errs() << "\n";;
       errs() << "\n  ";
-      Args[i]->getType()->dump();
+      Args[i]->getType()->print(llvm::errs());
+      llvm::errs() << "\n";;
       errs() << "\n";
     }
     assert((i >= FTy->getNumParams() ||
@@ -106,7 +112,7 @@ static void check(Value *Func, ArrayRef<Value *> Args) {
   }
 }
 
-static unsigned getTypeSize(TargetData &TD, Type *type) {
+static unsigned getTypeSize(DataLayout &TD, Type *type) {
   if (type->isFunctionTy()) /* it is not sized, weird */
     return TD.getPointerSize();
 
@@ -123,23 +129,23 @@ Instruction *Kleerer::createMalloc(BasicBlock *BB, Type *type,
                                    unsigned typeSize, Value *arraySize) {
   return CallInst::CreateMalloc(BB, intPtrTy, type,
                                 ConstantInt::get(intPtrTy, typeSize),
-                                arraySize);
+                                arraySize, nullptr, "");
 }
 
 static Constant *getGlobalString(LLVMContext &C, Module &M,
                                  const StringRef &str) {
-  Constant *strArray = ConstantArray::get(C, str);
+  Constant *strArray = ConstantDataArray::getString(C, str);
   GlobalVariable *strVar =
         new GlobalVariable(M, strArray->getType(), true,
                            GlobalValue::PrivateLinkage, strArray, "");
-  strVar->setUnnamedAddr(true);
+  strVar->setUnnamedAddr(GlobalValue::UnnamedAddr::Global);
   strVar->setAlignment(1);
 
   std::vector<Value *> params;
-  params.push_back(ConstantInt::get(TypeBuilder<types::i<32>, true>::get(C), 0));
-  params.push_back(ConstantInt::get(TypeBuilder<types::i<32>, true>::get(C), 0));
+  params.push_back(ConstantInt::get(Type::getInt32Ty(C), 0));
+  params.push_back(ConstantInt::get(Type::getInt32Ty(C), 0));
 
-  return ConstantExpr::getInBoundsGetElementPtr(strVar, params);
+  return ConstantExpr::getInBoundsGetElementPtr(strArray->getType(), strVar, params);
 }
 
 Instruction *Kleerer::call_klee_make_symbolic(Constant *name, BasicBlock *BB,
@@ -190,11 +196,21 @@ void Kleerer::makeGlobalsSymbolic(Module &M, BasicBlock *BB) {
 
 Constant *Kleerer::get_assert_fail()
 {
-  Type *constCharPtrTy = TypeBuilder<const char *, false>::get(C);
-  AttrListPtr attrs = attrs.addAttr(~0, Attribute::NoReturn);
-  return M.getOrInsertFunction("__assert_fail", attrs, Type::getVoidTy(C),
-                               constCharPtrTy, constCharPtrTy, uintType,
-                               constCharPtrTy, NULL);
+  Type *constCharPtrTy = Type::getInt8PtrTy(C);
+  std::vector<Type*> Args = { 
+      constCharPtrTy, // assertion
+      constCharPtrTy, // file
+      uintType,       // line (uintType 是类成员变量，前面应该已经初始化了)
+      constCharPtrTy  // function
+  };
+  
+  FunctionType *FT = FunctionType::get(Type::getVoidTy(C), Args, false);
+  FunctionCallee FC = M.getOrInsertFunction("__assert_fail", FT);
+  if (Function *F = dyn_cast<Function>(FC.getCallee())) {
+      F->setDoesNotReturn(); // 等同于旧版的 Attribute::NoReturn
+  }
+
+  return cast<Constant>(FC.getCallee());
 }
 
 BasicBlock *Kleerer::checkAiState(Function *mainFun, BasicBlock *BB,
@@ -296,7 +312,7 @@ Value *Kleerer::handlePtrArg(BasicBlock *mainBB, Constant *name,
     if (cast)
       insList.push_back(ins = new BitCastInst(ins, voidPtrType));
     ins = GetElementPtrInst::CreateInBounds(ins,
-           ConstantInt::get(TypeBuilder<types::i<64>, true>::get(C), 2048));
+           ConstantInt::get(Type::getInt64Ty(C), 2048));
     insList.push_back(ins);
     if (cast)
       insList.push_back(ins = new BitCastInst(ins, PT));
@@ -327,7 +343,7 @@ void Kleerer::prepareArguments(Function &F, BasicBlock *mainBB,
       val = handlePtrArg(mainBB, name, PT);
     } else if (IntegerType *IT = dyn_cast<IntegerType>(type)) {
       Instruction *ins;
-      insList.push_front(ins = new AllocaInst(IT));
+      insList.push_front(ins = new AllocaInst(IT, 0));
       insList.push_back(call_klee_make_symbolic(name, mainBB, type, ins));
       insList.push_back(ins = new LoadInst(ins));
       val = ins;
@@ -338,14 +354,19 @@ void Kleerer::prepareArguments(Function &F, BasicBlock *mainBB,
 }
 
 void Kleerer::writeMain(Function &F) {
-  std::string name = M.getModuleIdentifier() + ".main." + F.getNameStr() + ".o";
-  Function *mainFun = Function::Create(TypeBuilder<int(), false>::get(C),
-                    GlobalValue::ExternalLinkage, "main", &M);
+  std::string name = M.getModuleIdentifier() + ".main." + F.getName().str() + ".o";
+  FunctionType *FT = FunctionType::get(Type::getInt32Ty(C), false);
+  Function *mainFun = Function::Create(FT, GlobalValue::ExternalLinkage, "main", &M);
   BasicBlock *mainBB = BasicBlock::Create(C, "entry", mainFun);
 
+  std::vector<Type*> KMSArgs = {
+    Type::getInt8PtrTy(C),
+    Type::getInt32Ty(C),
+    Type::getInt8PtrTy(C)
+  };
+  FunctionType *KMSFT = FunctionType::get(Type::getVoidTy(C), KMSArgs, false);
   klee_make_symbolic = Function::Create(
-              TypeBuilder<void(void *, unsigned, const char *), false>::get(C),
-              GlobalValue::ExternalLinkage, "klee_make_symbolic", &M);
+    KMSFT, GlobalValue::ExternalLinkage, "klee_make_symbolic", &M);
 /*  Function *klee_int = Function::Create(
               TypeBuilder<int(const char *), false>::get(C),
               GlobalValue::ExternalLinkage, "klee_int", &M);*/
@@ -374,20 +395,20 @@ void Kleerer::writeMain(Function &F) {
   mainFun->viewCFG();
 #endif
 
-  std::string ErrorInfo;
-  raw_fd_ostream out(name.c_str(), ErrorInfo);
-  if (!ErrorInfo.empty()) {
+  std::error_code EC;
+  raw_fd_ostream out(name.c_str(), EC, sys::fs::OF_None);
+  if (EC) {
     errs() << __func__ << ": cannot write '" << name << "'!\n";
     return;
   }
 
 //  errs() << mainMod;
 
-  PassManager Passes;
+  llvm::legacy::PassManager Passes;
   Passes.add(createVerifierPass());
   Passes.run(M);
 
-  WriteBitcodeToFile(&M, out);
+  WriteBitcodeToFile(M, out);
   errs() << __func__ << ": written: '" << name << "'\n";
   mainFun->eraseFromParent();
   klee_make_symbolic->eraseFromParent();
@@ -414,7 +435,7 @@ bool Kleerer::run() {
     Function &F = *cast<Function>(CE->getOperand(0));
 
     callgraph::Callgraph::const_iterator II, EE;
-    llvm::tie(II, EE) = CG.calls(&F);
+    std::tie(II, EE) = CG.calls(&F);
     for (; II != EE; ++II) {
       const Function *callee = (*II).second;
       if (callee == F__assert_fail) {
@@ -429,7 +450,7 @@ bool Kleerer::run() {
 }
 
 bool KleererPass::runOnModule(Module &M) {
-  TargetData &TD = getAnalysis<TargetData>();
+  DataLayout &TD = *(const_cast<llvm::DataLayout*>(&M.getDataLayout()));
   ptr::PointsToSets PS;
   {
     ptr::ProgramStructure P(M);
diff --git a/third-party/LLVMSlicer/Slicing/PostDominanceFrontier.cpp b/third-party/LLVMSlicer/Slicing/PostDominanceFrontier.cpp
index 5c85fc9..d996460 100644
--- a/third-party/LLVMSlicer/Slicing/PostDominanceFrontier.cpp
+++ b/third-party/LLVMSlicer/Slicing/PostDominanceFrontier.cpp
@@ -4,10 +4,12 @@
 // This file is distributed under the University of Illinois Open Source
 // License. See LICENSE.TXT for details.
 
-#include "llvm/Constants.h"
-#include "llvm/Function.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/Pass.h"
+#include "llvm/Analysis/LoopInfo.h"
+#include "llvm/Analysis/LoopIterator.h"
 
 #include "llvm/Transforms/Utils/BasicBlockUtils.h"
 
@@ -23,7 +25,7 @@ bool CreateHammockCFG::runOnFunction(Function &F) {
   if (F.front().getName() == "start")
 	  return false;
 
-  LoopInfo &LI = getAnalysis<LoopInfo>();
+  LoopInfo &LI = getAnalysis<LoopInfoWrapperPass>().getLoopInfo();
   LLVMContext &ctx = F.getContext();
   Function::BasicBlockListType &BBL = F.getBasicBlockList();
   ConstantInt *Ctrue = ConstantInt::getTrue(ctx);
@@ -36,7 +38,7 @@ bool CreateHammockCFG::runOnFunction(Function &F) {
   BranchInst::Create(&entry, BBend, Ctrue, BBstart);
 
   for (Function::iterator I = BBL.begin(), E = BBL.end(); I != E; ++I) {
-    BasicBlock *BB = I;
+    BasicBlock *BB = &*I;
     if (LI.isLoopHeader(BB)) {
       BasicBlock *BBLoopBody = BB->splitBasicBlock(BB->begin(), "body");
       ReplaceInstWithInst(&BB->back(),
@@ -107,53 +109,53 @@ PostDominanceFrontier::findNearestCommonDominator(const PostDominatorTree &DT,
   return NULL;
 }
 
-void
-PostDominanceFrontier::calculate(const PostDominatorTree &DT, Function &F) {
-  Stype S;
-  constructS(DT, F, S);
-  for (Stype::const_iterator I = S.begin(), E = S.end(); I != E; ++I) {
-    DomTreeNode *mNode = I->first;
-    DomTreeNode *nNode = I->second;
-    findNearestCommonDominator(DT, mNode, nNode);
-  }
-}
+// void
+// PostDominanceFrontier::calculate(const PostDominatorTree &DT, Function &F) {
+//   Stype S;
+//   constructS(DT, F, S);
+//   for (Stype::const_iterator I = S.begin(), E = S.end(); I != E; ++I) {
+//     DomTreeNode *mNode = I->first;
+//     DomTreeNode *nNode = I->second;
+//     findNearestCommonDominator(DT, mNode, nNode);
+//   }
+// }
 
 #else /* CONTROL_DEPENDENCE_GRAPH */
 
-const DominanceFrontier::DomSetType &
-PostDominanceFrontier::calculate(const PostDominatorTree &DT,
-                                 const DomTreeNode *Node) {
-  // Loop over CFG successors to calculate DFlocal[Node]
-  BasicBlock *BB = Node->getBlock();
-  DomSetType &S = Frontiers[BB];       // The new set to fill in...
-  if (getRoots().empty()) return S;
-
-  if (BB)
-    for (pred_iterator SI = pred_begin(BB), SE = pred_end(BB);
-         SI != SE; ++SI) {
-      BasicBlock *P = *SI;
-      // Does Node immediately dominate this predecessor?
-      DomTreeNode *SINode = DT[P];
-      if (SINode && SINode->getIDom() != Node)
-        S.insert(P);
-    }
-
-  // At this point, S is DFlocal.  Now we union in DFup's of our children...
-  // Loop through and visit the nodes that Node immediately dominates (Node's
-  // children in the IDomTree)
-  //
-  for (DomTreeNode::const_iterator
-         NI = Node->begin(), NE = Node->end(); NI != NE; ++NI) {
-    DomTreeNode *IDominee = *NI;
-    const DomSetType &ChildDF = calculate(DT, IDominee);
-
-    DomSetType::const_iterator CDFI = ChildDF.begin(), CDFE = ChildDF.end();
-    for (; CDFI != CDFE; ++CDFI) {
-      if (!DT.properlyDominates(Node, DT[*CDFI]))
-        S.insert(*CDFI);
-    }
-  }
-
-  return S;
-}
+// const DominanceFrontier::DomSetType &
+// PostDominanceFrontier::calculate(const PostDominatorTree &DT,
+//                                  const DomTreeNode *Node) {
+//   // Loop over CFG successors to calculate DFlocal[Node]
+//   BasicBlock *BB = Node->getBlock();
+//   DomSetType &S = Frontiers[BB];       // The new set to fill in...
+//   if (getRoots().empty()) return S;
+
+//   if (BB)
+//     for (pred_iterator SI = pred_begin(BB), SE = pred_end(BB);
+//          SI != SE; ++SI) {
+//       BasicBlock *P = *SI;
+//       // Does Node immediately dominate this predecessor?
+//       DomTreeNode *SINode = DT[P];
+//       if (SINode && SINode->getIDom() != Node)
+//         S.insert(P);
+//     }
+
+//   // At this point, S is DFlocal.  Now we union in DFup's of our children...
+//   // Loop through and visit the nodes that Node immediately dominates (Node's
+//   // children in the IDomTree)
+//   //
+//   for (DomTreeNode::const_iterator
+//          NI = Node->begin(), NE = Node->end(); NI != NE; ++NI) {
+//     DomTreeNode *IDominee = *NI;
+//     const DomSetType &ChildDF = calculate(DT, IDominee);
+
+//     DomSetType::const_iterator CDFI = ChildDF.begin(), CDFE = ChildDF.end();
+//     for (; CDFI != CDFE; ++CDFI) {
+//       if (!DT.properlyDominates(Node, DT[*CDFI]))
+//         S.insert(*CDFI);
+//     }
+//   }
+
+//   return S;
+// }
 #endif
diff --git a/third-party/LLVMSlicer/Slicing/Prepare.cpp b/third-party/LLVMSlicer/Slicing/Prepare.cpp
index 375b092..3ad7134 100644
--- a/third-party/LLVMSlicer/Slicing/Prepare.cpp
+++ b/third-party/LLVMSlicer/Slicing/Prepare.cpp
@@ -6,18 +6,17 @@
 #include <assert.h>
 #include <cstring>
 
-#include "llvm/BasicBlock.h"
-#include "llvm/Constants.h"
-#include "llvm/Function.h"
-#include "llvm/GlobalVariable.h"
-#include "llvm/InlineAsm.h"
-#include "llvm/Instructions.h"
-#include "llvm/Module.h"
+#include "llvm/IR/BasicBlock.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/GlobalVariable.h"
+#include "llvm/IR/InlineAsm.h"
+#include "llvm/IR/Instructions.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Pass.h"
-#include "llvm/Support/TypeBuilder.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/InstIterator.h"
 #include "llvm/Support/raw_ostream.h"
-#include "llvm/Type.h"
+#include "llvm/IR/Type.h"
 #include "llvm/Transforms/Utils/BasicBlockUtils.h"
 
 #include "llvmslicer/Callgraph.h"
@@ -61,26 +60,27 @@ static GlobalVariable *getAiVar(Function &F, const CallInst *CI) {
   assert(strVar && strVar->hasInitializer());
   const ConstantArray *str =
     dyn_cast<const ConstantArray>(strVar->getInitializer());
-  assert(str && str->isCString());
-  std::string id = str->getAsCString();
+  const ConstantDataArray *CDA = dyn_cast<ConstantDataArray>(str);
+  assert(CDA && CDA->isCString());
+  std::string id = CDA->getAsCString();
   char *cstr = new char[11 + id.size() + 1];
   strcpy(cstr, "__ai_state_"); /* len=11 */
   strcpy(cstr + 11, id.c_str());
   for (size_t i = 11; i < 11 + id.size(); i++)
     if (cstr[i] != '_' && !isupper(cstr[i]) && !islower(cstr[i]))
       cstr[i] = 'X';
-  Type *intType = TypeBuilder<int, false>::get(F.getContext());
+  Type *intType = Type::getInt32Ty(F.getContext());
   GlobalVariable *glob =
     dyn_cast<GlobalVariable>(F.getParent()->getOrInsertGlobal(cstr, intType));
   delete cstr;
   glob->setInitializer(ConstantInt::get(
-                            TypeBuilder<int, false>::get(F.getContext()), 0));
+                            Type::getInt32Ty(F.getContext()), 0));
   return glob;
 }
 
 void Prepare::replaceInsLoad(Function &F, CallInst *CI) {
   GlobalVariable *glob = getAiVar(F, CI);
-  LoadInst *LI = new LoadInst(glob, 0, true);
+  LoadInst *LI = new LoadInst(glob, "", true);
   LI->setDebugLoc(CI->getDebugLoc());
   ReplaceInstWithInst(CI, LI);
 }
@@ -144,7 +144,8 @@ bool Prepare::handleAsm(Function &F, CallInst *CI) {
     return true;
   }
   errs() << "ASM str (" << F.getName() << "): " << ASM << " from:\n";
-  CI->dump();
+  CI->print(llvm::errs());
+  llvm::errs() << "\n";;
   errs() << "===========\n";
   return false;
 }
@@ -268,7 +269,7 @@ void Prepare::findInitFuns(Module &M, const ptr::PointsToSets &PS) {
   callgraph::Callgraph CG(M, PS);
 
   SmallVector<Constant *, 10> initFns;
-  Type *ETy = TypeBuilder<void *, false>::get(M.getContext());
+  Type *ETy = Type::getInt8PtrTy(M.getContext());
   const char *initFun = getenv("SLICE_INITIAL_FUNCTION");
 
   if (initFun) {
diff --git a/third-party/LLVMSlicer/Slicing/StaticSlicer.cpp b/third-party/LLVMSlicer/Slicing/StaticSlicer.cpp
index 66ada25..52f2ed3 100644
--- a/third-party/LLVMSlicer/Slicing/StaticSlicer.cpp
+++ b/third-party/LLVMSlicer/Slicing/StaticSlicer.cpp
@@ -3,12 +3,12 @@
 
 #include <sys/time.h>
 
-#include "llvm/LLVMContext.h"
-#include "llvm/Instructions.h"
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/Instructions.h"
 #include "llvm/Analysis/CallGraph.h"
-#include "llvm/Function.h"
+#include "llvm/IR/Function.h"
 #include "llvm/Pass.h"
-#include "llvm/Value.h"
+#include "llvm/IR/Value.h"
 #include "llvm/Support/CommandLine.h"
 
 #include "commons/handy.h"
@@ -20,7 +20,7 @@
 
 using namespace llvm;
 
-#define DEBUG_STATIC_SLICER
+//#define DEBUG_STATIC_SLICER
 
 namespace llvm { namespace slicing { namespace detail {
 
@@ -119,7 +119,7 @@ namespace llvm {namespace slicing {
     gettimeofday(&atim, NULL);
     at1 = atim.tv_sec * 1000.0 + (atim.tv_usec/1000.0);
 
-    CallGraph * tcg = &getAnalysis<CallGraph>();
+    CallGraph * tcg = &getAnalysis<CallGraphWrapperPass>().getCallGraph();
 
     gettimeofday(&atim, NULL);
     at2 = atim.tv_sec * 1000.0 + (atim.tv_usec/1000.0);
@@ -185,7 +185,7 @@ namespace llvm {namespace slicing {
     gettimeofday(&atim, NULL);
     at1 = atim.tv_sec * 1000.0 + (atim.tv_usec/1000.0);
     for (Module::iterator MI = M.begin(), ME = M.end(); MI != ME; ++MI) {
-      Function * f = MI;
+      Function * f = &*MI;
       if (f->isIntrinsic())
         continue;
       // errs() << "\t|" << f << "@" << f->getName() << "|\n";
diff --git a/third-party/LLVMSlicer/tmp/Matcher.cpp b/third-party/LLVMSlicer/tmp/Matcher.cpp
index c411a22..e87890a 100644
--- a/third-party/LLVMSlicer/tmp/Matcher.cpp
+++ b/third-party/LLVMSlicer/tmp/Matcher.cpp
@@ -277,7 +277,7 @@ bool skipFunction(Function *F)
 unsigned ScopeInfoFinder::getInstLine(const Instruction *I)
 {
   DebugLoc Loc = I->getDebugLoc();
-  if (Loc.isUnknown()) {
+  if (!Loc) {
     if (DEBUG_MATCHER) {
       errs() << "Unknown LOC" << "\n";
     }
@@ -293,7 +293,7 @@ unsigned ScopeInfoFinder::getLastLine(Function *F)
   const BasicBlock & BB = F->back();
   const Instruction & I = BB.back();
   DebugLoc Loc = I.getDebugLoc();
-  if (Loc.isUnknown()) 
+  if (!Loc) 
     return 0;
   return Loc.getLine();
 }
@@ -326,7 +326,7 @@ void Matcher::processInst(Function *F)
     /** Get each instruction's scope information **/
     for (BasicBlock::iterator BI = FI->begin(), BE = FI->end(); BI != BE; BI++) {
       DebugLoc Loc = BI->getDebugLoc();
-      if (Loc.isUnknown())
+      if (!Loc)
         continue;
       LLVMContext & Ctx = BI->getContext();
 
@@ -350,13 +350,13 @@ void Matcher::processBasicBlock(Function *F)
       continue;
     }
     DebugLoc Loc = first->getDebugLoc();
-    if (Loc.isUnknown()) {
+    if (!Loc) {
       errs() << "Unknown LOC information" << "\n";
       continue;
     }
     errs() << "Block :" << Loc.getLine();
     Loc = last->getDebugLoc();
-    if (Loc.isUnknown()) {
+    if (!Loc) {
       errs() << "Unknown LOC information" << "\n";
       continue;
     }
diff --git a/tools/ListFiles/ListFiles.cpp b/tools/ListFiles/ListFiles.cpp
index 4f45274..4dfebc4 100644
--- a/tools/ListFiles/ListFiles.cpp
+++ b/tools/ListFiles/ListFiles.cpp
@@ -1,14 +1,14 @@
 #define DEBUG_TYPE "listfiles"
 #include "llvm/Pass.h"
-#include "llvm/Function.h"
-#include "llvm/Module.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/Module.h"
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/ADT/Statistic.h"
 
-#include "llvm/Analysis/DebugInfo.h"
-#include "llvm/Metadata.h"
-#include "llvm/Support/Dwarf.h"
-#include "llvm/Constants.h"
+#include "llvm/IR/DebugInfo.h"
+#include "llvm/IR/Metadata.h"
+#include "llvm/BinaryFormat/Dwarf.h"
+#include "llvm/IR/Constants.h"
 
 #include <stdlib.h>
 #include <cxxabi.h>
@@ -66,25 +66,25 @@ namespace {
             char *buf = (char *) malloc(buf_len);
             strcpy(buf, "");
             for (unsigned i = 0, e = CU_Nodes->getNumOperands(); i != e; ++i) {
-                DICompileUnit CU1(CU_Nodes->getOperand(i));
-                errs() << CU1.getDirectory() << "#" << CU1.getFilename() << "\n";
+                auto *CU1 = cast<DICompileUnit>(CU_Nodes->getOperand(i));
+                errs() << CU1->getDirectory() << "#" << CU1->getFilename() << "\n";
                 if (i == 0) {
                     if (i + 1 != e) {
                         i++;
-                        DICompileUnit CU2(CU_Nodes->getOperand(i));
-                        buf = common_prefix(buf, buf_len, CU1.getDirectory().data(), CU2.getDirectory().data());
+                        auto *CU2 = cast<DICompileUnit>(CU_Nodes->getOperand(i));
+                        buf = common_prefix(buf, buf_len, CU1->getDirectory().data(), CU2->getDirectory().data());
                     }
                     else {
-                        size_t cu_len = CU1.getDirectory().size();
+                        size_t cu_len = CU1->getDirectory().size();
                         if (cu_len > buf_len) {
                             buf = (char *) realloc(buf, cu_len);
                             buf_len = cu_len;
                         }
-                        strcpy(buf, CU1.getDirectory().data()); 
+                        strcpy(buf, CU1->getDirectory().data()); 
                     }
                 }
                 else {
-                    buf = common_prefix(buf, buf_len, buf, CU1.getDirectory().data());
+                    buf = common_prefix(buf, buf_len, buf, CU1->getDirectory().data());
                 }
             }
             errs() << buf << "\n";
diff --git a/tools/PerfDiff/Makefile b/tools/PerfDiff/Makefile
index a09f101..b54e707 100644
--- a/tools/PerfDiff/Makefile
+++ b/tools/PerfDiff/Makefile
@@ -17,4 +17,7 @@ USEDLIBS=diffengine.a
 
 LINK_COMPONENTS = all
 
+LDFLAGS += -L../../lib/analyzer/DiffEngine
+LIBS    += -ldiffengine
+
 include $(LEVEL)/Makefile.common
diff --git a/tools/PerfDiff/perf-diff.cpp b/tools/PerfDiff/perf-diff.cpp
index 305034a..f747a17 100644
--- a/tools/PerfDiff/perf-diff.cpp
+++ b/tools/PerfDiff/perf-diff.cpp
@@ -16,14 +16,14 @@
 #include <stdio.h>
 #include <sys/time.h>
 
-#include "llvm/LLVMContext.h"
-#include "llvm/Module.h"
-#include "llvm/Type.h"
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/Type.h"
 #include "llvm/ADT/DenseMap.h"
 #include "llvm/ADT/SmallVector.h"
 #include "llvm/ADT/StringRef.h"
 #include "llvm/Support/CommandLine.h"
-#include "llvm/Support/IRReader.h"
+#include "llvm/IRReader/IRReader.h"
 #include "llvm/Support/MemoryBuffer.h"
 #include "llvm/Support/raw_ostream.h"
 #include "llvm/Support/SourceMgr.h"
@@ -37,9 +37,9 @@ using namespace llvm;
 /// and null is returned.
 static Module *ReadModule(LLVMContext &Context, StringRef Name) {
     SMDiagnostic Diag;
-    Module *M = ParseIRFile(Name, Diag, Context);
+    Module *M = parseIRFile(Name, Diag, Context).release();
     if (!M)
-        Diag.Print("llvmdiff", errs());
+        Diag.print("llvmdiff", errs());
     return M;
 }
 
diff --git a/tools/PerfScope/Makefile b/tools/PerfScope/Makefile
index 907bf5c..555029e 100644
--- a/tools/PerfScope/Makefile
+++ b/tools/PerfScope/Makefile
@@ -18,6 +18,38 @@ USEDLIBS=parser.a riskeval.a costmodel.a diffengine.a mappercore.a slicer.a llvm
 # LLVMLIBS=LLVMSupport.a LLVMCore.a LLVMSupport.a LLVMCore.a LLVMBitReader.a LLVMAsmParser.a \
 				LLVMAnalysis.a LLVMTransformUtils.a LLVMScalarOpts.a LLVMTarget.a
 
+LDFLAGS += -L../../lib/slicer \
+           -L../../lib/mapper \
+           -L../../lib/analyzer/CostModel \
+		   -L../../lib/analyzer/DiffEngine \
+           -L../../lib/analyzer/RiskEvaluator \
+           -L../../lib/commons \
+		   -L../../lib/dependence \
+		   -L../../lib/graph \
+		   -L../../lib/parser/PatchIRDecoder \
+		   -L../../third-party/LLVMSlicer/Callgraph \
+		   -L../../third-party/LLVMSlicer/Slicing \
+		   -L../../third-party/LLVMSlicer/Modifies \
+		   -L../../third-party/LLVMSlicer/PointsTo \
+		   -L../../third-party/LLVMSlicer/Languages \
+		   -L../../third-party/patch
+
+LIBS    += -lparser \
+		   -lriskeval \
+		   -lcostmodel \
+		   -ldiffengine \
+		   -lmappercore \
+		   -lslicer \
+		   -lllvmslicer \
+		   -lcallgraph \
+		   -lmods \
+		   -lpoints \
+		   -ldependence \
+		   -lcommons \
+		   -lgraph \
+		   -llanguage \
+		   -lgnulib \
+
 LINK_COMPONENTS = all
 
 include $(LEVEL)/Makefile.common
diff --git a/tools/PerfScope/PerfScope.cpp b/tools/PerfScope/PerfScope.cpp
index 333e5a6..9cbb333 100644
--- a/tools/PerfScope/PerfScope.cpp
+++ b/tools/PerfScope/PerfScope.cpp
@@ -8,31 +8,32 @@
 #include <sys/time.h>
 #include <vector>
 #include <list>
+#include <memory>
 
-#include "llvm/LLVMContext.h"
-#include "llvm/IntrinsicInst.h"
-#include "llvm/Module.h"
-#include "llvm/PassManager.h"
-#include "llvm/Type.h"
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/IntrinsicInst.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/LegacyPassManager.h"
+#include "llvm/IR/Type.h"
 
 #include "llvm/ADT/StringRef.h"
 #include "llvm/ADT/DenseMap.h"
 #include "llvm/ADT/Triple.h"
 
-#include "llvm/Target/TargetData.h"
+#include "llvm/IR/DataLayout.h"
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetLibraryInfo.h"
-#include "llvm/Target/TargetLowering.h"
-#include "llvm/Target/TargetSubtargetInfo.h"
-#include "llvm/Target/TargetLowering.h"
+#include "llvm/Analysis/TargetLibraryInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
+#include "llvm/CodeGen/TargetSubtargetInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
 
 #include "llvm/Support/Host.h"
 #include "llvm/Support/SourceMgr.h"
-#include "llvm/Support/IRReader.h"
+#include "llvm/IRReader/IRReader.h"
 #include "llvm/Support/ManagedStatic.h"
-#include "llvm/Support/CallSite.h"
-#include "llvm/Support/InstVisitor.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/CallSite.h"
+#include "llvm/IR/InstVisitor.h"
+#include "llvm/IR/InstIterator.h"
 #include "llvm/Support/TargetSelect.h"
 #include "llvm/Support/TargetRegistry.h"
 
@@ -71,7 +72,7 @@ static char * program_name;
 
 static char * id_fname = NULL;
 
-static LLVMContext & Context = getGlobalContext();
+static LLVMContext Context;
 
 static vector<ModuleArg> newmods;
 static vector<ModuleArg> oldmods;
@@ -89,7 +90,7 @@ X86CostModel * XCM = NULL;
 void runevaluator(Module * module, InstMapTy & instmap)
 {
   slicing::StaticSlicer * slicer = NULL;
-  PassManager Passes;
+  legacy::PassManager Passes;
   if (analysis_level > 1) {
     slicer = new slicing::StaticSlicer(true);
     Passes.add(slicer);
@@ -97,7 +98,7 @@ void runevaluator(Module * module, InstMapTy & instmap)
   }
   assert(XCM && "requires cost model");
   if (instmap.size()) {
-    OwningPtr<FunctionPassManager> FPasses(new FunctionPassManager(module));
+    std::unique_ptr<legacy::FunctionPassManager> FPasses(new legacy::FunctionPassManager(module));
     FPasses->add(new RiskEvaluator(instmap, slicer, XCM, &profile, module, analysis_level));
     FPasses->doInitialization();
     for (InstMapTy::iterator map_it = instmap.begin(), map_ie = instmap.end();
diff --git a/tools/StaticProfiler/Makefile b/tools/StaticProfiler/Makefile
index ebf4660..7f16ab3 100644
--- a/tools/StaticProfiler/Makefile
+++ b/tools/StaticProfiler/Makefile
@@ -14,6 +14,26 @@ TOOLNAME=staticprofiler
 # USEDLIBS=parser.a mappercore.a riskeval.a diffengine.a slicer.a dependence.a commons.a 
 USEDLIBS=riskeval.a costmodel.a commons.a 
 
+LDFLAGS += -L../../lib/slicer \
+           -L../../lib/mapper \
+           -L../../lib/analyzer/CostModel \
+		   -L../../lib/analyzer/DiffEngine \
+           -L../../lib/analyzer/RiskEvaluator \
+           -L../../lib/commons \
+		   -L../../lib/dependence \
+		   -L../../lib/graph \
+		   -L../../lib/parser/PatchIRDecoder \
+		   -L../../third-party/LLVMSlicer/Callgraph \
+		   -L../../third-party/LLVMSlicer/Slicing \
+		   -L../../third-party/LLVMSlicer/Modifies \
+		   -L../../third-party/LLVMSlicer/PointsTo \
+		   -L../../third-party/LLVMSlicer/Languages \
+		   -L../../third-party/patch
+
+LIBS    += -lriskeval \
+		   -lcostmodel \
+		   -lcommons \
+
 LINK_COMPONENTS = all
 
 include $(LEVEL)/Makefile.common
diff --git a/tools/StaticProfiler/StaticProfiler.cpp b/tools/StaticProfiler/StaticProfiler.cpp
index bef8eaa..097f282 100644
--- a/tools/StaticProfiler/StaticProfiler.cpp
+++ b/tools/StaticProfiler/StaticProfiler.cpp
@@ -10,30 +10,30 @@
 #include <vector>
 #include <list>
 
-#include "llvm/LLVMContext.h"
-#include "llvm/IntrinsicInst.h"
-#include "llvm/Module.h"
-#include "llvm/PassManager.h"
-#include "llvm/Type.h"
+#include "llvm/IR/LLVMContext.h"
+#include "llvm/IR/IntrinsicInst.h"
+#include "llvm/IR/Module.h"
+#include "llvm/IR/PassManager.h"
+#include "llvm/IR/Type.h"
 
 #include "llvm/ADT/StringRef.h"
 #include "llvm/ADT/DenseMap.h"
 #include "llvm/ADT/Triple.h"
 
-#include "llvm/Target/TargetData.h"
+#include "llvm/IR/DataLayout.h"
 #include "llvm/Target/TargetMachine.h"
-#include "llvm/Target/TargetLibraryInfo.h"
-#include "llvm/Target/TargetLowering.h"
-#include "llvm/Target/TargetSubtargetInfo.h"
-#include "llvm/Target/TargetLowering.h"
+#include "llvm/Analysis/TargetLibraryInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
+#include "llvm/CodeGen/TargetSubtargetInfo.h"
+#include "llvm/CodeGen/TargetLowering.h"
 
 #include "llvm/Support/Host.h"
 #include "llvm/Support/SourceMgr.h"
-#include "llvm/Support/IRReader.h"
+#include "llvm/IRReader/IRReader.h"
 #include "llvm/Support/ManagedStatic.h"
-#include "llvm/Support/CallSite.h"
-#include "llvm/Support/InstVisitor.h"
-#include "llvm/Support/InstIterator.h"
+#include "llvm/IR/CallSite.h"
+#include "llvm/IR/InstVisitor.h"
+#include "llvm/IR/InstIterator.h"
 #include "llvm/Support/TargetSelect.h"
 #include "llvm/Support/TargetRegistry.h"
 
@@ -56,7 +56,7 @@ static int topk_hot = TOPKHOTFUNCS;
 
 FILE * fout = stdout;
 
-bool detail = false;
+bool showdetail = false;
 bool printall = false;
 
 
@@ -70,7 +70,7 @@ gen_dbg_impl(profile)
 gen_dbg_nop(profile)
 #endif
 
-static LLVMContext & Context = getGlobalContext();
+static LLVMContext Context;
 
 typedef struct FuncCost {
   string name;
@@ -116,7 +116,7 @@ void static_profile(Module * module, CostModel * model)
   unsigned i = 0;
   for (Module::iterator MI = module->begin(), ME = module->end(); 
       MI != ME; ++MI, ++i) {
-    Function * F = MI;
+    Function * F = &*MI;
     if (F->getName().startswith("llvm.")) // Skip llvm functions
       continue;
     const char * name = cpp_demangle(F->getName().data());
@@ -140,7 +140,7 @@ void static_profile(Module * module, CostModel * model)
   size_t k = (!printall && topk_cost > 0) ? topk_cost : size;
   for (i = 0; i < k ; i++) {
     fprintf(fout, "%s", func_cost[i].name.c_str());
-    if (detail)
+    if (showdetail)
       fprintf(fout, ": %u", func_cost[i].cost);
     fprintf(fout, "\n");
   }
@@ -152,7 +152,7 @@ void static_profile(Module * module, CostModel * model)
   k = (!printall && topk_hot > 0) ? topk_hot: size;
   for (i = 0; i < k; i++) {
     fprintf(fout, "%s", func_hot[i].name.c_str());
-    if (detail)
+    if (showdetail)
       fprintf(fout, ": %u", func_hot[i].hotness);
     fprintf(fout, "\n");
   }
@@ -205,7 +205,7 @@ int main(int argc, char *argv[])
   while((opt = getopt(argc, argv, "adn:m:o:h")) != -1) {
     switch(opt) {
       case 'd':
-        detail = true;
+        showdetail = true;
         break;
       case 'a':
         printall = true;
@@ -246,7 +246,7 @@ int main(int argc, char *argv[])
     exit(1);
   }
 
-  LLVMContext & Context = getGlobalContext();
+  LLVMContext Context;
 
   Module * module(ReadModule(Context, argv[optind]));
   if (module == NULL)  {
