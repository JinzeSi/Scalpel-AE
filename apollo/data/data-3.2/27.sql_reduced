SET profiling=1;
EXPLAIN ANALYZE
select  
  1
from 
  test_bd.user_profiles as ref_0
    left join (select  
          subq_0.c1 as c1, 
          (select comment from test_bd.comments limit 1 offset 9) as c2,
          1
        from 
          (select  
                ref_1.user_id as c0, 
                ref_1.profile_picture as c1
              from 
                test_bd.user_profiles as ref_1
              where ((ref_1.profile_picture is not NULL) 
                  or (false)) 
                and ((false) 
                  or ((ref_1.user_id is not NULL) 
                    or (ref_1.user_id is NULL)))
              ) as subq_0
        where true
        ) as subq_1
    on (subq_1.c2 is not NULL)
where (ref_0.profile_picture is not NULL) 
  and (((false) 
      or (true)) 
    or (false));
SHOW profiles;
/* Elapsed old:0.093517 new:0.100549 ratio:1.075198 */


--reduced actual_latest = 0.492724
